<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="theme-color" content="#fefdf8" />
    <title>不要鸽我 · Don't Ghost Me</title>
    <style>
@import url("https://fonts.googleapis.com/css2?family=ZCOOL+KuaiLe&display=swap");

:root {
  --ink: #2d3e59;
  --ink-soft: #53657d;
  --cream: #fefdf8;
  --paper: #f7f4ea;
  --paper-blue: #e9f3f5;
  --sky: #a2d9f0;
  --powder: #c7d9f3;
  --blue: #869bc0;
  --blue-deep: #667c9f;
  --butter: #f5e6a8;
  --mint: #c9e3d8;
  --peach: #f3cfb7;
  --line: rgba(45, 62, 89, 0.3);
  --shadow: rgba(45, 62, 89, 0.12);
  --font-sans: "PingFang SC", "Microsoft YaHei", "Noto Sans CJK SC", system-ui, sans-serif;
  --font-display: "ZCOOL KuaiLe", "Kaiti SC", "STKaiti", "Comic Sans MS", cursive;
}

* {
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  margin: 0;
  color: var(--ink);
  background:
    radial-gradient(circle at 13% 8%, rgba(162, 217, 240, 0.38), transparent 25rem),
    radial-gradient(circle at 90% 25%, rgba(245, 230, 168, 0.35), transparent 22rem),
    #edf4f3;
  font-family: var(--font-sans);
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
}

button,
a {
  -webkit-tap-highlight-color: transparent;
}

button {
  color: inherit;
  font: inherit;
}

button:focus-visible,
a:focus-visible {
  outline: 3px solid var(--butter);
  outline-offset: 4px;
}

.svg-defs {
  position: absolute;
  width: 0;
  height: 0;
  overflow: hidden;
}

symbol .symbol-shape,
symbol .symbol-wing,
symbol .symbol-beak {
  fill: var(--powder);
  stroke: var(--ink);
  stroke-width: 3;
  stroke-linecap: round;
  stroke-linejoin: round;
}

#mini-pigeon .symbol-shape {
  fill: var(--cream);
}

#mini-pigeon .symbol-wing {
  fill: #edf3f1;
}

#flying-pigeon-up .flight-silhouette,
#flying-pigeon-down .flight-silhouette {
  fill: var(--cream);
  stroke: var(--blue-deep);
  stroke-width: 2.2;
  stroke-linecap: round;
  stroke-linejoin: round;
  vector-effect: non-scaling-stroke;
}

symbol .symbol-beak {
  fill: var(--butter);
}

symbol .symbol-line {
  fill: none;
  stroke: var(--ink);
  stroke-width: 3;
  stroke-linecap: round;
}

symbol .pigeon-eye {
  fill: var(--ink);
  stroke: none;
}

.intro {
  position: fixed;
  z-index: 1000;
  inset: 0;
  display: grid;
  place-items: center;
  overflow: hidden;
  background:
    radial-gradient(circle, rgba(134, 155, 192, 0.3) 2.2px, transparent 2.8px) 0 0 / 28px 28px,
    linear-gradient(135deg, #fefdf8 0%, #f5f7f2 48%, #edf4f5 100%);
  transition: opacity 0.75s ease, visibility 0.75s ease;
}

.intro::before,
.intro::after {
  position: absolute;
  width: min(40vw, 520px);
  height: min(40vw, 520px);
  border: 1px solid rgba(102, 124, 159, 0.16);
  border-radius: 46% 54% 43% 57% / 51% 43% 57% 49%;
  background:
    radial-gradient(circle, rgba(254, 253, 248, 0.92) 3.2px, transparent 3.8px) 0 0 / 25px 25px,
    var(--powder);
  content: "";
  filter: blur(2px);
  opacity: 0.5;
}

.intro::before {
  top: -23vw;
  left: -15vw;
  transform: rotate(-8deg);
}

.intro::after {
  right: -18vw;
  bottom: -25vw;
  transform: rotate(12deg);
}

.intro.is-hidden {
  visibility: hidden;
  opacity: 0;
  pointer-events: none;
}

.skip-intro {
  position: absolute;
  z-index: 10;
  top: 26px;
  right: 32px;
  padding: 8px 14px;
  border: 1px dashed var(--line);
  border-radius: 999px;
  color: var(--ink-soft);
  background: rgba(254, 253, 248, 0.72);
  cursor: pointer;
}

.intro-dots {
  position: absolute;
  left: 4vw;
  bottom: 5vh;
  width: 190px;
  height: 120px;
  background: radial-gradient(circle, var(--blue) 3.5px, transparent 4.2px) 0 0 / 24px 24px;
  opacity: 0.52;
  transform: rotate(-7deg);
}

.intro-dots::before {
  position: fixed;
  top: 8vh;
  right: 5vw;
  width: 145px;
  height: 95px;
  background: radial-gradient(circle, var(--sky) 4px, transparent 4.6px) 0 0 / 23px 23px;
  content: "";
  transform: rotate(8deg);
}

.flock {
  position: absolute;
  inset: 0;
  pointer-events: none;
}

.flying-pigeon {
  --size: 100px;
  position: absolute;
  top: var(--top);
  left: -180px;
  width: var(--size);
  color: var(--blue);
  opacity: 0;
  animation: fly-across var(--duration) cubic-bezier(0.35, 0.07, 0.45, 0.94) var(--delay) forwards;
}

.flying-pigeon svg {
  display: block;
  width: 100%;
  overflow: visible;
  filter: none;
  transform: scale(var(--scale)) rotate(var(--rotate));
}

.flying-pigeon use {
  fill: var(--cream);
  stroke: var(--blue-deep);
}

@keyframes fly-across {
  0% { left: -180px; opacity: 0; transform: translateY(16px); }
  10% { opacity: 0.95; }
  47% { transform: translateY(-13px); }
  80% { opacity: 0.9; }
  100% { left: calc(100vw + 190px); opacity: 0; transform: translateY(9px); }
}

.intro-brand {
  position: relative;
  z-index: 4;
  width: min(90vw, 520px);
  padding: 30px;
  text-align: center;
}

.intro-mark {
  width: 208px;
  margin: 0 auto -5px;
  opacity: 0;
  transform: scale(0.72) translateY(18px) rotate(-4deg);
  transition: opacity 0.85s ease, transform 0.95s cubic-bezier(0.18, 0.9, 0.25, 1.25);
}

.intro-mark svg {
  width: 100%;
  overflow: visible;
  filter: drop-shadow(5px 8px 0 rgba(45, 62, 89, 0.08));
}

.logo-body,
.logo-wing {
  fill: var(--cream);
  stroke: var(--ink);
  stroke-width: 6;
  stroke-linecap: round;
  stroke-linejoin: round;
  filter: url(#rough-line);
}

.intro-mark .logo-body {
  fill: #fefdf8 !important;
}

.logo-wing {
  fill: #edf3f1;
}

.intro-mark .logo-wing {
  fill: #edf3f1 !important;
}

.logo-eye {
  fill: var(--ink);
}

.logo-beak {
  fill: var(--butter);
  stroke: var(--ink);
  stroke-width: 5;
  stroke-linejoin: round;
}

.logo-leg {
  fill: none;
  stroke: var(--ink);
  stroke-width: 5;
  stroke-linecap: round;
}

.logo-letter path {
  fill: var(--cream);
  stroke: var(--ink);
  stroke-width: 4;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.intro-brand.is-logo-visible .intro-mark {
  opacity: 1;
  transform: scale(1) translateY(0) rotate(-1deg);
}

.brand-cn,
.intro-brand h1,
.intro-copy,
.enter-button {
  opacity: 0;
  transform: translateY(12px);
  transition: opacity 0.55s ease, transform 0.55s ease;
}

.intro-brand.is-copy-visible .brand-cn,
.intro-brand.is-copy-visible h1,
.intro-brand.is-copy-visible .intro-copy,
.intro-brand.is-copy-visible .enter-button {
  opacity: 1;
  transform: translateY(0);
}

.brand-cn {
  margin: 0;
  color: var(--ink);
  font-size: 29px;
  font-weight: 800;
  letter-spacing: 0.12em;
}

.intro-brand h1 {
  margin: 3px 0 12px;
  font-family: var(--font-display);
  font-size: clamp(22px, 4vw, 36px);
  letter-spacing: 0.15em;
  transition-delay: 0.06s;
}

.intro-copy {
  margin: 0 auto 21px;
  color: var(--ink-soft);
  font-size: 14px;
  transition-delay: 0.12s;
}

.enter-button {
  padding: 11px 20px;
  border: 1.5px solid var(--ink);
  border-radius: 999px;
  background: var(--cream);
  box-shadow: 3px 4px 0 var(--powder);
  cursor: pointer;
  transition-delay: 0.18s;
}

.app {
  min-height: 100vh;
  opacity: 0;
  transform: translateY(10px);
  transition: opacity 0.65s ease, transform 0.65s ease;
}

.app.is-ready {
  opacity: 1;
  transform: translateY(0);
}

.topbar {
  position: sticky;
  z-index: 50;
  top: 16px;
  display: grid;
  grid-template-columns: auto 1fr auto;
  align-items: center;
  width: min(1240px, calc(100% - 48px));
  min-height: 70px;
  margin: 16px auto 0;
  padding: 10px 14px 10px 18px;
  border: 1px solid rgba(45, 62, 89, 0.18);
  border-radius: 22px;
  background: rgba(254, 253, 248, 0.88);
  box-shadow: 0 10px 35px rgba(45, 62, 89, 0.08);
  backdrop-filter: blur(14px);
}

.brand {
  display: inline-flex;
  gap: 9px;
  align-items: center;
  color: var(--ink);
  text-decoration: none;
}

.brand-icon {
  display: grid;
  width: 46px;
  height: 46px;
  place-items: center;
  border-radius: 50%;
  background: var(--powder);
}

.brand-icon svg,
.footer-pigeon svg,
.mobile-nav-item:first-child svg {
  width: 40px;
  overflow: visible;
}

.brand-icon use,
.footer-pigeon use,
.mobile-nav-item:first-child use {
  fill: var(--cream);
  stroke: var(--ink);
  stroke-width: 3;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.brand-icon .pigeon-eye,
.footer-pigeon .pigeon-eye,
.mobile-nav-item:first-child .pigeon-eye {
  fill: var(--ink);
}

.brand-words {
  display: flex;
  flex-direction: column;
  line-height: 1.2;
}

.brand-words strong {
  font-size: 16px;
  letter-spacing: 0.08em;
}

.brand-words small {
  color: var(--ink-soft);
  font-family: var(--font-display);
  font-size: 8px;
  letter-spacing: 0.13em;
}

.desktop-nav {
  display: flex;
  justify-self: center;
  gap: 7px;
}

.nav-link {
  padding: 9px 13px;
  border: 0;
  border-radius: 999px;
  color: var(--ink-soft);
  background: transparent;
  cursor: pointer;
}

.nav-link:hover,
.nav-link.is-active {
  color: var(--ink);
  background: var(--paper-blue);
}

.wallet-button {
  display: inline-flex;
  gap: 8px;
  align-items: center;
  padding: 10px 14px;
  border: 1px dashed var(--line);
  border-radius: 999px;
  background: var(--cream);
  cursor: pointer;
}

.wallet-dot {
  width: 9px;
  height: 9px;
  border: 2px solid var(--ink);
  border-radius: 50%;
  background: var(--mint);
}

main {
  width: min(1240px, calc(100% - 48px));
  margin: 28px auto 0;
}

.paper-surface {
  position: relative;
  background:
    linear-gradient(rgba(254, 253, 248, 0.96), rgba(254, 253, 248, 0.96)),
    repeating-linear-gradient(0deg, transparent, transparent 4px, rgba(45, 62, 89, 0.03) 5px);
  box-shadow: 0 24px 70px rgba(45, 62, 89, 0.11);
}

.hero {
  display: grid;
  grid-template-columns: minmax(0, 1.08fr) minmax(360px, 0.92fr);
  gap: 30px;
  align-items: center;
  min-height: 570px;
  padding: 64px 60px;
  border: 1.5px solid rgba(45, 62, 89, 0.25);
  border-radius: 18px 25px 16px 22px;
  overflow: hidden;
}

.hero::after {
  position: absolute;
  right: -22px;
  bottom: -20px;
  width: 145px;
  height: 90px;
  background: radial-gradient(circle, rgba(134, 155, 192, 0.55) 3px, transparent 3.6px) 0 0 / 21px 21px;
  content: "";
  transform: rotate(-6deg);
}

.tape {
  position: absolute;
  z-index: 3;
  width: 104px;
  height: 30px;
  background: rgba(245, 230, 168, 0.75);
  box-shadow: inset 0 0 0 1px rgba(45, 62, 89, 0.04);
}

.tape-top {
  top: 10px;
  left: 50%;
  transform: translateX(-50%) rotate(-2deg);
}

.eyebrow {
  margin: 0 0 12px;
  color: var(--blue-deep);
  font-family: var(--font-display);
  font-size: 12px;
  font-weight: 800;
  letter-spacing: 0.16em;
  text-transform: uppercase;
}

.eyebrow span {
  display: inline-block;
  padding: 4px 9px;
  border-radius: 4px;
  background: var(--butter);
  transform: rotate(-1deg);
}

.hero h2 {
  max-width: 660px;
  margin: 0;
  font-size: clamp(42px, 5vw, 72px);
  font-weight: 850;
  line-height: 1.16;
  letter-spacing: -0.045em;
}

.hero-description {
  max-width: 610px;
  margin: 24px 0 0;
  color: var(--ink-soft);
  font-size: 17px;
  line-height: 1.9;
}

.hero-actions {
  display: flex;
  gap: 14px;
  align-items: center;
  margin-top: 31px;
}

.primary-button {
  display: inline-flex;
  gap: 9px;
  align-items: center;
  min-height: 50px;
  padding: 0 20px;
  border: 1.5px solid var(--ink);
  border-radius: 15px 18px 14px 17px;
  background: var(--sky);
  box-shadow: 4px 5px 0 var(--ink);
  font-weight: 750;
  cursor: pointer;
  transition: transform 0.18s ease, box-shadow 0.18s ease;
}

.primary-button:hover {
  box-shadow: 2px 3px 0 var(--ink);
  transform: translate(2px, 2px);
}

.primary-button svg {
  width: 25px;
}

.primary-button svg path,
.mobile-nav-item:not(:first-child) svg path {
  fill: none;
  stroke: currentColor;
  stroke-width: 2.5;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.rescue-stamp {
  position: relative;
  display: inline-flex;
  gap: 9px;
  align-items: center;
  min-width: 158px;
  min-height: 58px;
  padding: 7px 15px 7px 9px;
  overflow: hidden;
  border: 1.5px solid var(--blue-deep);
  border-radius: 2px;
  background:
    linear-gradient(90deg, rgba(199, 217, 243, 0.34), transparent 52%),
    var(--cream);
  box-shadow:
    inset 0 0 0 3px var(--cream),
    inset 0 0 0 4px rgba(102, 124, 159, 0.55),
    3px 4px 0 rgba(134, 155, 192, 0.28);
  cursor: pointer;
  transform: rotate(-0.65deg);
  transition: transform 0.18s ease, box-shadow 0.18s ease;
}

.rescue-stamp:hover {
  box-shadow:
    inset 0 0 0 3px var(--cream),
    inset 0 0 0 4px rgba(102, 124, 159, 0.62),
    1px 2px 0 rgba(134, 155, 192, 0.3);
  transform: translate(2px, 2px) rotate(0.15deg);
}

.rescue-stamp::before,
.rescue-stamp::after {
  position: absolute;
  z-index: 4;
  top: 0;
  bottom: 0;
  width: 7px;
  background: radial-gradient(circle, var(--paper) 2.4px, transparent 2.8px) center -1px / 7px 11px repeat-y;
  content: "";
  pointer-events: none;
}

.rescue-stamp::before { left: -3px; }
.rescue-stamp::after { right: -3px; }

.stamp-art {
  position: relative;
  z-index: 2;
  display: grid;
  flex: 0 0 52px;
  width: 52px;
  height: 42px;
  place-items: center;
  border: 1px dashed rgba(45, 62, 89, 0.42);
  background: var(--blue);
  transform: rotate(1.2deg);
}

.stamp-art svg {
  width: 48px;
  overflow: visible;
}

.stamp-bird-body,
.stamp-bird-head,
.stamp-bird-wing {
  fill: var(--cream);
  stroke: var(--ink);
  stroke-width: 2.2;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.stamp-bird-wing {
  fill: #edf3f1;
}

.stamp-bird-beak {
  fill: var(--butter);
  stroke: var(--ink);
  stroke-width: 2;
  stroke-linejoin: round;
}

.stamp-bird-eye {
  fill: var(--ink);
}

.stamp-note,
.stamp-motion {
  fill: var(--cream);
  stroke: var(--ink);
  stroke-width: 1.8;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.stamp-motion {
  fill: none;
  opacity: 0.6;
}

.stamp-copy {
  position: relative;
  z-index: 2;
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  line-height: 1.08;
}

.stamp-copy strong {
  font-size: 15px;
  font-weight: 750;
  letter-spacing: 0.02em;
}

.stamp-copy b {
  color: var(--blue-deep);
  font-family: var(--font-display);
  font-size: 10px;
  letter-spacing: 0.15em;
}

.stamp-postmark {
  position: absolute;
  z-index: 1;
  right: -9px;
  bottom: -16px;
  width: 49px;
  height: 49px;
  border: 1.4px solid rgba(102, 124, 159, 0.38);
  border-radius: 50%;
  transform: rotate(-14deg);
}

.stamp-postmark::before,
.stamp-postmark::after,
.stamp-postmark i {
  position: absolute;
  right: 32px;
  width: 33px;
  height: 1px;
  background: rgba(102, 124, 159, 0.32);
  content: "";
}

.stamp-postmark::before { top: 11px; }
.stamp-postmark::after { top: 17px; }
.stamp-postmark i { top: 23px; }

.micro-note {
  margin: 18px 0 0;
  color: #7d8998;
  font-size: 12px;
}

.hero-art {
  position: relative;
  display: grid;
  min-height: 440px;
  place-items: center;
}

.polka-card {
  position: absolute;
  inset: 12px 5px 24px 15px;
  border: 1.5px solid var(--ink);
  border-radius: 48% 52% 46% 54% / 46% 44% 56% 54%;
  background:
    radial-gradient(circle, rgba(254, 253, 248, 0.9) 3px, transparent 3.5px) 0 0 / 25px 25px,
    var(--powder);
  transform: rotate(2deg);
}

.mail-stamp {
  position: absolute;
  z-index: 2;
  top: 29px;
  right: 19px;
  width: 82px;
  height: 82px;
  padding-top: 23px;
  border: 2px solid var(--blue-deep);
  border-radius: 50%;
  color: var(--blue-deep);
  font-family: var(--font-display);
  font-size: 9px;
  font-weight: 900;
  line-height: 1.2;
  text-align: center;
  letter-spacing: 0.1em;
  transform: rotate(10deg);
}

.mail-stamp::before,
.mail-stamp::after {
  position: absolute;
  inset: 7px;
  border: 1px dashed var(--blue-deep);
  border-radius: 50%;
  content: "";
}

.pigeon-letter {
  position: relative;
  z-index: 1;
  width: min(100%, 445px);
  overflow: visible;
  filter: drop-shadow(9px 13px 0 rgba(45, 62, 89, 0.1));
  transform: rotate(-1deg);
}

.bird-body,
.bird-head,
.bird-wing,
.bird-tail,
.bird-beak,
.money-bag path {
  stroke: var(--ink);
  stroke-width: 6;
  stroke-linecap: round;
  stroke-linejoin: round;
  filter: url(#rough-line);
}

.bird-body,
.bird-head {
  fill: #fefdf8;
}

.bird-wing {
  fill: #e9f0f1;
}

.bird-tail {
  fill: #d7e3eb;
}

.bird-beak {
  fill: var(--butter);
}

.bird-eye {
  fill: var(--ink);
}

.bird-leg,
.bird-foot,
.ground-line,
.motion-line {
  fill: none;
  stroke: var(--ink);
  stroke-width: 5;
  stroke-linecap: round;
  opacity: 0.82;
}

.ground-line {
  stroke-width: 3;
  opacity: 0.4;
}

.motion-line {
  stroke-width: 4;
  opacity: 0.5;
}

.letter path {
  fill: var(--cream);
  stroke: var(--ink);
  stroke-width: 5;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.art-caption {
  position: absolute;
  z-index: 3;
  right: 35px;
  bottom: 27px;
  margin: 0;
  padding: 7px 12px;
  border: 1px dashed var(--line);
  background: var(--cream);
  font-family: var(--font-display);
  font-size: 12px;
  transform: rotate(-2deg);
}

.mechanism-strip {
  display: grid;
  grid-template-columns: 230px 1fr;
  gap: 30px;
  align-items: center;
  margin: 28px 0 100px;
  padding: 24px 28px;
  border: 1.5px solid rgba(45, 62, 89, 0.24);
  border-radius: 7px 10px 8px 9px;
  background:
    linear-gradient(90deg, rgba(254, 253, 248, 0.38), transparent 36%),
    #dcebef;
  color: var(--ink);
  box-shadow: 7px 8px 0 rgba(199, 217, 243, 0.66);
  transform: rotate(-0.25deg);
}

.strip-title {
  display: flex;
  flex-direction: column;
}

.strip-title span {
  width: fit-content;
  padding: 2px 7px;
  color: var(--blue-deep);
  background: rgba(254, 253, 248, 0.72);
  font-family: var(--font-display);
  font-size: 11px;
  letter-spacing: 0.18em;
}

.strip-title strong {
  font-size: 18px;
}

.steps {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  margin: 0;
  padding: 0;
  list-style: none;
}

.steps li {
  position: relative;
  display: flex;
  gap: 8px;
  align-items: center;
  min-height: 48px;
  padding: 0 14px;
  border-left: 1px dashed rgba(45, 62, 89, 0.2);
  font-size: 13px;
}

.steps b {
  color: var(--blue-deep);
  font-family: var(--font-display);
  font-size: 11px;
}

.demo-scene-switcher {
  display: grid;
  grid-template-columns: minmax(220px, 0.7fr) minmax(420px, 1.3fr);
  gap: 24px;
  align-items: center;
  margin: 0 0 42px;
  padding: 19px 22px;
  border: 1px dashed rgba(45, 62, 89, 0.3);
  border-radius: 5px 10px 6px 9px;
  background:
    linear-gradient(90deg, rgba(254, 253, 248, 0.68), transparent 42%),
    rgba(199, 217, 243, 0.34);
  box-shadow: 6px 7px 0 rgba(162, 217, 240, 0.2);
}

.demo-scene-title {
  display: flex;
  flex-direction: column;
}

.demo-scene-title span {
  width: fit-content;
  padding: 2px 7px;
  color: var(--blue-deep);
  background: var(--butter);
  font-family: var(--font-display);
  font-size: 10px;
  font-weight: 900;
  letter-spacing: 0.16em;
  transform: rotate(-1deg);
}

.demo-scene-title strong {
  margin-top: 5px;
  font-size: 14px;
}

.demo-scene-options {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 11px;
}

.demo-scene-button {
  position: relative;
  display: flex;
  gap: 11px;
  align-items: center;
  min-height: 68px;
  padding: 8px 13px;
  border: 1px dashed rgba(45, 62, 89, 0.32);
  border-radius: 4px 8px 5px 7px;
  color: var(--ink-soft);
  background: rgba(254, 253, 248, 0.74);
  text-align: left;
  cursor: pointer;
  transition: transform 0.2s ease, box-shadow 0.2s ease, background 0.2s ease;
}

.demo-scene-button::after {
  position: absolute;
  top: -7px;
  right: -7px;
  width: 18px;
  height: 18px;
  border-radius: 50%;
  background: var(--paper-blue);
  content: "";
}

.demo-scene-button.is-active {
  border-style: solid;
  border-color: var(--ink);
  color: var(--ink);
  background: var(--cream);
  box-shadow: 3px 4px 0 var(--sky);
  transform: rotate(-0.3deg);
}

.demo-scene-button.is-active::before {
  position: absolute;
  top: 5px;
  right: 8px;
  color: var(--blue-deep);
  content: "✓";
  font-family: var(--font-display);
  font-weight: 900;
}

.demo-scene-button svg {
  flex: 0 0 47px;
  width: 47px;
  height: 40px;
  fill: var(--cream);
  stroke: var(--ink);
  stroke-width: 2.2;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.demo-scene-button.is-active svg {
  fill: var(--paper-blue);
}

.demo-scene-button > span {
  display: flex;
  flex-direction: column;
}

.demo-scene-button b {
  font-size: 13px;
}

.demo-scene-button small {
  color: var(--ink-soft);
  font-size: 10px;
}

.section-heading {
  display: flex;
  justify-content: space-between;
  gap: 30px;
  align-items: end;
  margin-bottom: 28px;
}

.section-heading h2 {
  margin: 0;
  font-size: clamp(31px, 4vw, 46px);
  line-height: 1.25;
  letter-spacing: -0.04em;
}

.section-heading > p {
  max-width: 390px;
  margin: 0 0 5px;
  color: var(--ink-soft);
  font-size: 14px;
  text-align: right;
}

.workspace-section,
.rescue-section {
  scroll-margin-top: 110px;
}

.workbench {
  display: grid;
  grid-template-columns: minmax(0, 1.45fr) minmax(300px, 0.55fr);
  gap: 28px;
  padding: 36px;
  border: 1px solid rgba(45, 62, 89, 0.12);
  border-radius: 18px;
  background:
    radial-gradient(circle at 92% 12%, rgba(245, 230, 168, 0.65), transparent 18rem),
    var(--paper-blue);
}

.workspace-section[data-scene="travel"] .workbench {
  background:
    radial-gradient(circle at 92% 12%, rgba(243, 207, 183, 0.58), transparent 18rem),
    #eef4ee;
}

.workspace-section[data-scene="travel"] .tiny-label {
  background: var(--mint);
}

.workspace-section[data-scene="travel"] .fund-pocket {
  background:
    radial-gradient(circle, rgba(254, 253, 248, 0.95) 2.5px, transparent 3px) 0 0 / 20px 20px,
    #d8e6dc;
}

.project-paper {
  position: relative;
  padding: 38px 40px 32px;
  border: 1px solid rgba(45, 62, 89, 0.25);
  border-radius: 6px 12px 5px 9px;
  background:
    repeating-linear-gradient(0deg, transparent, transparent 32px, rgba(45, 62, 89, 0.055) 33px),
    var(--cream);
  box-shadow: 8px 10px 0 rgba(134, 155, 192, 0.2);
  transform: rotate(-0.35deg);
}

.paper-clip {
  position: absolute;
  top: -17px;
  left: 52px;
  width: 17px;
  height: 57px;
  border: 3px solid var(--blue-deep);
  border-radius: 12px;
  transform: rotate(8deg);
}

.paper-clip::after {
  position: absolute;
  inset: 5px 3px 15px;
  border: 2px solid var(--blue-deep);
  border-radius: 9px;
  content: "";
}

.paper-header {
  display: flex;
  justify-content: space-between;
  gap: 20px;
  align-items: flex-start;
}

.tiny-label {
  display: inline-block;
  margin: 0 0 8px;
  padding: 3px 8px;
  background: var(--butter);
  font-size: 11px;
  transform: rotate(-1deg);
}

.paper-header h3 {
  margin: 0;
  font-size: clamp(24px, 3vw, 36px);
  line-height: 1.3;
}

.status-stamp {
  position: relative;
  flex: 0 0 auto;
  display: grid;
  width: 92px;
  height: 70px;
  padding: 10px 12px 8px;
  place-items: center;
  border: 2px solid #c47f75;
  border-radius: 46% 54% 48% 52% / 53% 46% 54% 47%;
  color: #ad675f;
  background: #fff7ef;
  box-shadow:
    inset 0 0 0 4px #fff7ef,
    inset 0 0 0 5px rgba(196, 127, 117, 0.62),
    2px 3px 0 rgba(196, 127, 117, 0.12);
  font-family: var(--font-display);
  line-height: 1;
  text-align: center;
  transform: rotate(6deg);
}

.status-stamp::before,
.status-stamp::after {
  position: absolute;
  width: 12px;
  height: 2px;
  border-radius: 99px;
  background: #c47f75;
  content: "";
  opacity: 0.72;
}

.status-stamp::before {
  top: 10px;
  left: -8px;
  box-shadow: -2px 7px 0 -0.3px #c47f75;
  transform: rotate(18deg);
}

.status-stamp::after {
  right: -8px;
  bottom: 12px;
  box-shadow: 1px -7px 0 -0.3px #c47f75;
  transform: rotate(-18deg);
}

.status-stamp small {
  align-self: end;
  color: #c47f75;
  font-family: var(--font-display);
  font-size: 8px;
  font-weight: 900;
  letter-spacing: 0.13em;
}

.status-stamp strong {
  align-self: start;
  font-family: "Kaiti SC", "KaiTi", "STKaiti", var(--font-display);
  font-size: 17px;
  font-weight: 900;
  letter-spacing: 0.08em;
}

.project-summary {
  max-width: 780px;
  margin: 22px 0;
  color: var(--ink-soft);
}

.promise-meta {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  margin: 26px 0 30px;
  border-top: 1px dashed var(--line);
  border-bottom: 1px dashed var(--line);
}

.promise-meta div {
  display: flex;
  flex-direction: column;
  gap: 3px;
  padding: 16px 12px;
  border-left: 1px dashed var(--line);
}

.promise-meta div:first-child {
  border-left: 0;
}

.promise-meta span {
  color: var(--ink-soft);
  font-size: 12px;
}

.promise-meta strong {
  font-size: 18px;
}

.timeline {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 0;
  margin: 18px 0 30px;
}

.timeline-item {
  position: relative;
  display: flex;
  flex-direction: column;
  gap: 8px;
  color: #82909f;
  font-size: 12px;
}

.timeline-item::after {
  position: absolute;
  z-index: 0;
  top: 8px;
  left: 8px;
  width: 100%;
  border-top: 2px dashed rgba(45, 62, 89, 0.22);
  content: "";
}

.timeline-item:last-child::after {
  display: none;
}

.timeline-item i {
  position: relative;
  z-index: 1;
  display: block;
  width: 18px;
  height: 18px;
  border: 2px solid var(--ink-soft);
  border-radius: 50%;
  background: var(--cream);
}

.timeline-item.is-done i { background: var(--mint); }
.timeline-item.is-alert i { background: var(--peach); }
.timeline-item.is-current i { background: var(--butter); }
.timeline-item.is-current { color: var(--ink); font-weight: 700; }

.text-button {
  padding: 0;
  border: 0;
  border-bottom: 1px dashed var(--ink);
  background: transparent;
  font-weight: 700;
  cursor: pointer;
}

.text-button span {
  display: inline-block;
  margin-left: 7px;
  transition: transform 0.18s ease;
}

.text-button:hover span {
  transform: translateX(5px);
}

.fund-pocket {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  min-height: 510px;
  padding: 32px 22px 20px;
  border: 1.5px solid var(--ink);
  border-radius: 8px 8px 35px 35px;
  background:
    radial-gradient(circle, rgba(254, 253, 248, 0.86) 2.5px, transparent 3px) 0 0 / 23px 23px,
    var(--powder);
  box-shadow: inset 0 -20px 0 rgba(134, 155, 192, 0.2);
  overflow: hidden;
}

.pocket-label {
  padding: 5px 12px;
  border: 1px dashed var(--ink);
  background: var(--cream);
  font-family: var(--font-display);
  font-size: 11px;
  font-weight: 800;
  letter-spacing: 0.14em;
  transform: rotate(1deg);
}

.money-amount {
  display: flex;
  gap: 7px;
  align-items: baseline;
  margin-top: 16px;
}

.money-amount strong {
  font-family: var(--font-display);
  font-size: 54px;
  line-height: 1;
}

.money-amount span {
  font-weight: 800;
}

.fund-pocket > p {
  margin: 10px 0 -5px;
  color: var(--ink-soft);
  font-size: 13px;
  text-align: center;
}

.pigeon-money {
  width: min(100%, 260px);
  margin-top: auto;
  overflow: visible;
}

.money-bag path {
  fill: var(--butter);
  stroke-width: 5;
}

.verified-stamp {
  position: absolute;
  right: 14px;
  bottom: 15px;
  width: 65px;
  height: 65px;
  padding-top: 13px;
  border: 2px solid var(--blue-deep);
  border-radius: 50%;
  color: var(--blue-deep);
  background: rgba(254, 253, 248, 0.72);
  font-family: var(--font-display);
  font-size: 10px;
  font-weight: 800;
  line-height: 1.25;
  text-align: center;
  transform: rotate(-11deg);
}

.rescue-section {
  margin: 110px 0 90px;
}

.ticket-list {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 22px;
}

.rescue-ticket {
  position: relative;
  display: grid;
  grid-template-columns: auto 1fr auto;
  gap: 20px;
  align-items: center;
  min-height: 175px;
  padding: 27px 34px 27px 27px;
  border: 1.5px solid var(--ink);
  background: var(--cream);
  box-shadow: 6px 7px 0 var(--powder);
  text-align: left;
  cursor: pointer;
  transition: transform 0.2s ease, box-shadow 0.2s ease;
  clip-path: polygon(0 0, 96% 0, 100% 8%, 97% 18%, 100% 29%, 97% 40%, 100% 51%, 97% 62%, 100% 73%, 97% 84%, 100% 94%, 96% 100%, 0 100%, 3% 91%, 0 80%, 3% 68%, 0 56%, 3% 44%, 0 32%, 3% 20%, 0 9%);
}

.rescue-ticket:nth-child(2) {
  background: #f8f3df;
  box-shadow-color: var(--mint);
  transform: rotate(0.35deg);
}

.rescue-ticket:hover,
.rescue-ticket.is-selected {
  box-shadow: 3px 4px 0 var(--blue-deep);
  transform: translate(3px, 3px) rotate(-0.2deg);
}

.rescue-ticket.is-completed {
  border-color: var(--blue-deep);
  background:
    linear-gradient(90deg, rgba(201, 227, 216, 0.52), transparent 58%),
    var(--cream);
  box-shadow: 6px 7px 0 var(--mint);
}

.rescue-ticket.is-completed .ticket-status {
  border-style: solid;
  color: var(--ink);
  background: var(--mint);
  font-family: var(--font-display);
  font-weight: 900;
}

.ticket-number {
  align-self: start;
  color: var(--blue-deep);
  font-family: var(--font-display);
  font-size: 10px;
  font-weight: 900;
  letter-spacing: 0.12em;
  writing-mode: vertical-rl;
}

.ticket-copy {
  display: flex;
  flex-direction: column;
  gap: 7px;
}

.ticket-copy small {
  color: var(--blue-deep);
}

.ticket-copy strong {
  font-size: 19px;
  line-height: 1.4;
}

.ticket-copy em {
  color: var(--ink-soft);
  font-size: 12px;
  font-style: normal;
}

.ticket-reward {
  display: flex;
  flex-direction: column;
  align-items: center;
  line-height: 1.1;
}

.ticket-reward b {
  font-family: var(--font-display);
  font-size: 35px;
}

.ticket-reward {
  font-size: 11px;
  font-weight: 800;
}

.ticket-status {
  position: absolute;
  right: 22px;
  bottom: 13px;
  padding: 3px 8px;
  border: 1px dashed var(--ink-soft);
  color: var(--ink-soft);
  font-size: 10px;
  transform: rotate(-2deg);
}

footer {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 45px 24px 55px;
  color: var(--ink-soft);
  text-align: center;
}

.footer-pigeon {
  display: grid;
  width: 58px;
  height: 58px;
  margin-bottom: 9px;
  place-items: center;
  border-radius: 50%;
  background: var(--cream);
}

footer p,
footer small {
  margin: 0;
}

footer p {
  font-weight: 700;
}

footer small {
  font-family: var(--font-display);
  font-size: 10px;
  letter-spacing: 0.12em;
}

/* 创建承诺：给开发队友看的可交互设计稿 */
body.modal-open {
  overflow: hidden;
}

.promise-modal {
  position: fixed;
  z-index: 900;
  inset: 0;
  display: grid;
  padding: 24px;
  place-items: center;
  visibility: hidden;
  opacity: 0;
  pointer-events: none;
  transition: opacity 0.25s ease, visibility 0.25s ease;
}

.promise-modal.is-open {
  visibility: visible;
  opacity: 1;
  pointer-events: auto;
}

.promise-backdrop {
  position: absolute;
  inset: 0;
  border: 0;
  background: rgba(45, 62, 89, 0.42);
  backdrop-filter: blur(8px);
  cursor: pointer;
}

.promise-dialog {
  position: relative;
  width: min(1160px, 100%);
  max-height: calc(100vh - 48px);
  overflow: auto;
  border: 1.5px solid rgba(45, 62, 89, 0.36);
  border-radius: 11px 16px 10px 14px;
  background:
    radial-gradient(circle, rgba(134, 155, 192, 0.09) 1px, transparent 1.6px) 0 0 / 24px 24px,
    var(--cream);
  box-shadow: 14px 17px 0 rgba(199, 217, 243, 0.55), 0 28px 80px rgba(45, 62, 89, 0.28);
  transform: translateY(18px) rotate(-0.15deg);
  transition: transform 0.32s cubic-bezier(0.2, 0.8, 0.2, 1);
}

.promise-modal.is-open .promise-dialog {
  transform: translateY(0) rotate(-0.15deg);
}

.promise-dialog-tape {
  position: absolute;
  z-index: 3;
  top: -8px;
  left: calc(50% - 48px);
  width: 96px;
  height: 27px;
  background: rgba(245, 230, 168, 0.85);
  clip-path: polygon(3% 8%, 98% 0, 96% 90%, 1% 100%);
  transform: rotate(-1.5deg);
}

.promise-modal-close {
  position: absolute;
  z-index: 6;
  top: 22px;
  right: 23px;
  width: 42px;
  height: 42px;
  border: 1px dashed rgba(45, 62, 89, 0.36);
  border-radius: 50%;
  background: rgba(254, 253, 248, 0.86);
  cursor: pointer;
}

.promise-modal-close span {
  position: absolute;
  top: 19px;
  left: 11px;
  width: 19px;
  height: 2px;
  border-radius: 99px;
  background: var(--ink);
}

.promise-modal-close span:first-child {
  transform: rotate(45deg);
}

.promise-modal-close span:last-child {
  transform: rotate(-45deg);
}

.promise-editor {
  padding: 47px 48px 0;
}

.promise-modal-heading {
  padding: 0 62px 26px 0;
  border-bottom: 1px dashed rgba(45, 62, 89, 0.25);
}

.modal-kicker,
.form-number,
.preview-label,
.success-kicker {
  width: fit-content;
  margin: 0;
  color: var(--blue-deep);
  font-family: var(--font-display);
  font-size: 11px;
  font-weight: 900;
  letter-spacing: 0.16em;
  text-transform: uppercase;
}

.modal-kicker {
  padding: 3px 8px;
  background: var(--butter);
  transform: rotate(-1deg);
}

.promise-modal-heading h2 {
  margin: 12px 0 7px;
  font-size: clamp(31px, 4vw, 48px);
  line-height: 1.2;
  letter-spacing: -0.04em;
}

.promise-modal-heading > p:last-child {
  margin: 0;
  color: var(--ink-soft);
  font-size: 14px;
}

.promise-modal-layout {
  display: grid;
  grid-template-columns: minmax(0, 1.42fr) minmax(310px, 0.58fr);
  gap: 36px;
  padding: 32px 0 34px;
}

.promise-form {
  min-width: 0;
}

.scene-switcher {
  min-width: 0;
  margin: 0 0 28px;
  padding: 0;
  border: 0;
}

.scene-switcher legend {
  margin-bottom: 14px;
  color: var(--ink);
  font-family: var(--font-display);
  font-size: 17px;
  font-weight: 400;
  letter-spacing: 0.04em;
}

.scene-options {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 13px;
}

.scene-option {
  position: relative;
  display: flex;
  gap: 8px;
  align-items: center;
  min-height: 98px;
  padding: 11px 10px;
  overflow: hidden;
  border: 1.5px dashed rgba(45, 62, 89, 0.32);
  border-radius: 4px 10px 5px 8px;
  color: var(--ink-soft);
  background: rgba(247, 244, 234, 0.72);
  text-align: left;
  cursor: pointer;
  transition: transform 0.2s ease, border-color 0.2s ease, background 0.2s ease;
}

.scene-option::after {
  position: absolute;
  top: -8px;
  right: -7px;
  width: 28px;
  height: 28px;
  border-radius: 50%;
  background: var(--cream);
  content: "";
}

.scene-option:hover {
  transform: translateY(-2px) rotate(-0.3deg);
}

.scene-option.is-selected {
  border-style: solid;
  border-color: var(--blue-deep);
  color: var(--ink);
  background: var(--paper-blue);
  box-shadow: 4px 5px 0 rgba(199, 217, 243, 0.65);
}

.scene-option.is-selected::before {
  position: absolute;
  top: 8px;
  right: 11px;
  color: var(--blue-deep);
  content: "✓";
  font-family: var(--font-display);
  font-weight: 900;
}

.scene-option svg {
  flex: 0 0 54px;
  width: 54px;
  height: 50px;
}

.scene-paper {
  fill: var(--cream);
  stroke: var(--ink);
  stroke-width: 2.3;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.scene-line {
  fill: none;
  stroke: var(--blue-deep);
  stroke-width: 2.3;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.scene-tape {
  fill: var(--butter);
  stroke: var(--ink);
  stroke-width: 1.3;
  stroke-linejoin: round;
}

.scene-option span {
  display: flex;
  min-width: 0;
  flex-direction: column;
}

.scene-option strong {
  font-size: 13px;
}

.scene-option small {
  margin-top: 2px;
  color: var(--ink-soft);
  font-size: 10px;
  line-height: 1.45;
}

.custom-scene-field[hidden] {
  display: none;
}

.custom-scene-field {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  gap: 18px;
  align-items: end;
  margin-top: 13px;
  padding: 12px 14px;
  border-left: 4px solid var(--butter);
  background: rgba(245, 230, 168, 0.16);
  animation: custom-scene-in 0.24s ease both;
}

.custom-scene-field small {
  padding-bottom: 7px;
  color: var(--ink-soft);
  font-size: 10px;
}

@keyframes custom-scene-in {
  from {
    opacity: 0;
    transform: translateY(-5px) rotate(-0.2deg);
  }
  to {
    opacity: 1;
    transform: translateY(0) rotate(0);
  }
}

.promise-field-block {
  margin-bottom: 28px;
  padding: 21px 22px 22px;
  border-top: 1px dashed rgba(45, 62, 89, 0.28);
  border-bottom: 1px dashed rgba(45, 62, 89, 0.28);
  background: rgba(247, 244, 234, 0.38);
}

.promise-field-block .form-number {
  margin-bottom: 13px;
}

.form-number {
  color: var(--ink);
  font-size: 17px;
  font-weight: 400;
  letter-spacing: 0.04em;
  line-height: 1.4;
  text-transform: none;
}

.promise-field-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 20px;
  margin-top: 18px;
}

.promise-field {
  display: flex;
  min-width: 0;
  flex-direction: column;
  gap: 5px;
}

.promise-field > span:first-child,
.member-row label > span {
  color: var(--ink-soft);
  font-size: 11px;
}

.promise-field input,
.member-row input {
  width: 100%;
  min-width: 0;
  padding: 7px 2px 8px;
  border: 0;
  border-bottom: 1.5px solid rgba(45, 62, 89, 0.38);
  border-radius: 0;
  outline: 0;
  color: var(--ink);
  background: transparent;
  font: 600 15px/1.4 var(--font-sans);
}

.promise-field input:focus,
.member-row input:focus {
  border-color: var(--blue-deep);
  box-shadow: 0 3px 0 rgba(199, 217, 243, 0.75);
}

.amount-input {
  display: flex;
  gap: 9px;
  align-items: center;
}

.amount-input input {
  min-width: 0;
}

.amount-input b {
  color: var(--blue-deep);
  font-family: var(--font-display);
  font-size: 12px;
  letter-spacing: 0.08em;
}

.member-section {
  margin-top: 4px;
}

.member-section-heading {
  display: flex;
  justify-content: space-between;
  gap: 16px;
  align-items: end;
  margin-bottom: 12px;
}

.member-section-heading > div > span {
  color: var(--ink-soft);
  font-size: 11px;
}

.add-member-button {
  flex: 0 0 auto;
  padding: 6px 10px;
  border: 1px dashed var(--blue-deep);
  border-radius: 4px;
  color: var(--blue-deep);
  background: var(--cream);
  font-size: 11px;
  font-weight: 700;
  cursor: pointer;
  transform: rotate(-1deg);
}

.member-list {
  border-top: 1.5px solid rgba(45, 62, 89, 0.28);
}

.member-row {
  position: relative;
  display: grid;
  grid-template-columns: 42px minmax(100px, 0.42fr) minmax(170px, 1fr) 28px;
  gap: 11px;
  align-items: end;
  min-height: 74px;
  padding: 10px 5px;
  border-bottom: 1px dashed rgba(45, 62, 89, 0.24);
}

.member-row::after {
  position: absolute;
  right: -4px;
  bottom: -7px;
  width: 14px;
  height: 14px;
  border-radius: 50%;
  background: var(--cream);
  content: "";
}

.member-tag {
  display: grid;
  width: 34px;
  height: 34px;
  place-items: center;
  border: 1px solid var(--ink);
  border-radius: 48% 52% 44% 56%;
  color: var(--blue-deep);
  background: var(--butter);
  font-family: var(--font-display);
  font-size: 11px;
  font-weight: 900;
  transform: rotate(-4deg);
}

.member-row label {
  display: flex;
  min-width: 0;
  flex-direction: column;
}

.remove-member {
  width: 27px;
  height: 27px;
  margin-bottom: 7px;
  border: 0;
  border-radius: 50%;
  color: #a36761;
  background: rgba(243, 207, 183, 0.5);
  cursor: pointer;
}

.promise-rule {
  display: grid;
  grid-template-columns: 20px 1fr;
  gap: 10px;
  align-items: start;
  margin-top: 24px;
  padding: 13px 15px;
  border-left: 4px solid var(--butter);
  color: var(--ink-soft);
  background: rgba(245, 230, 168, 0.2);
  font-size: 12px;
  cursor: pointer;
}

.promise-rule input {
  position: absolute;
  opacity: 0;
}

.checkmark {
  position: relative;
  width: 18px;
  height: 18px;
  margin-top: 1px;
  border: 1.5px solid var(--ink);
  border-radius: 3px;
  background: var(--cream);
}

.promise-rule input:checked + .checkmark {
  background: var(--sky);
}

.promise-rule input:checked + .checkmark::after {
  position: absolute;
  top: 2px;
  left: 5px;
  width: 5px;
  height: 9px;
  border: solid var(--ink);
  border-width: 0 2px 2px 0;
  content: "";
  transform: rotate(45deg);
}

.promise-preview {
  position: sticky;
  top: 30px;
  align-self: start;
  min-height: 580px;
  padding: 28px 25px 24px;
  border: 1.5px solid var(--blue-deep);
  border-radius: 5px 11px 6px 9px;
  background:
    radial-gradient(circle, rgba(254, 253, 248, 0.95) 2px, transparent 2.5px) 0 0 / 18px 18px,
    var(--powder);
  box-shadow: 7px 9px 0 rgba(134, 155, 192, 0.22);
  transform: rotate(0.45deg);
}

.promise-preview::after {
  position: absolute;
  right: -8px;
  bottom: 34px;
  width: 18px;
  height: 60px;
  background:
    radial-gradient(circle at 0 10px, var(--cream) 8px, transparent 8.5px) 0 0 / 18px 20px repeat-y;
  content: "";
}

.preview-pin {
  position: absolute;
  top: 17px;
  right: 20px;
  width: 15px;
  height: 42px;
  border: 2px solid var(--blue-deep);
  border-radius: 10px;
  transform: rotate(13deg);
}

.preview-pin::after {
  position: absolute;
  inset: 4px 3px 11px;
  border: 1.5px solid var(--blue-deep);
  border-radius: 8px;
  content: "";
}

.preview-label {
  padding: 2px 7px;
  color: var(--ink);
  background: var(--cream);
  transform: rotate(-1deg);
}

.preview-pigeon {
  display: block;
  width: min(100%, 260px);
  margin: 8px auto -2px;
  overflow: visible;
}

.preview-bird-body,
.preview-bird-head {
  fill: var(--cream);
  stroke: var(--ink);
  stroke-width: 4;
}

.preview-bird-wing {
  fill: #edf3f1;
  stroke: var(--ink);
  stroke-width: 4;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.preview-bird-tail {
  fill: var(--blue);
  stroke: var(--ink);
  stroke-width: 4;
  stroke-linejoin: round;
}

.preview-bird-beak {
  fill: var(--butter);
  stroke: var(--ink);
  stroke-width: 4;
  stroke-linejoin: round;
}

.preview-bird-eye {
  fill: var(--ink);
}

.preview-bird-leg,
.preview-motion {
  fill: none;
  stroke: var(--ink);
  stroke-width: 4;
  stroke-linecap: round;
}

.preview-contract path:first-child {
  fill: var(--cream);
  stroke: var(--ink);
  stroke-width: 3.5;
  stroke-linejoin: round;
}

.preview-contract path:not(:first-child) {
  fill: none;
  stroke: var(--blue-deep);
  stroke-width: 2.4;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.preview-copy {
  position: relative;
  padding: 22px 20px 18px;
  border: 1px solid rgba(45, 62, 89, 0.27);
  background:
    repeating-linear-gradient(0deg, transparent, transparent 29px, rgba(45, 62, 89, 0.05) 30px),
    rgba(254, 253, 248, 0.95);
  box-shadow: 4px 5px 0 rgba(45, 62, 89, 0.08);
}

.preview-copy > span {
  display: inline-block;
  padding: 2px 7px;
  color: var(--ink-soft);
  background: var(--butter);
  font-size: 10px;
  transform: rotate(-1.5deg);
}

.preview-copy h3 {
  margin: 10px 0 15px;
  font-size: 21px;
  line-height: 1.35;
}

.preview-copy dl {
  margin: 0;
}

.preview-copy dl div {
  display: flex;
  justify-content: space-between;
  gap: 16px;
  padding: 7px 0;
  border-top: 1px dashed rgba(45, 62, 89, 0.22);
}

.preview-copy dt {
  color: var(--ink-soft);
  font-size: 11px;
}

.preview-copy dd {
  margin: 0;
  font-size: 12px;
  font-weight: 700;
  text-align: right;
}

.preview-copy dd b {
  font-family: var(--font-display);
  font-size: 17px;
}

.preview-stamp {
  position: absolute;
  right: 15px;
  bottom: 71px;
  display: grid;
  width: 72px;
  height: 65px;
  place-items: center;
  border: 2px solid #b9746d;
  border-radius: 45% 55% 48% 52%;
  color: #a45f5a;
  background: rgba(254, 253, 248, 0.76);
  box-shadow: inset 0 0 0 4px rgba(254, 253, 248, 0.72), inset 0 0 0 5px rgba(185, 116, 109, 0.7);
  font-family: var(--font-display);
  font-size: 8px;
  font-weight: 900;
  line-height: 1.15;
  text-align: center;
  transform: rotate(-8deg);
}

.preview-stamp strong {
  display: block;
  font-size: 14px;
}

.preview-note {
  max-width: 220px;
  margin: 18px 0 0;
  color: var(--ink-soft);
  font-size: 11px;
}

.promise-modal-actions {
  position: sticky;
  z-index: 5;
  bottom: 0;
  display: flex;
  justify-content: flex-end;
  gap: 12px;
  margin: 0 -48px;
  padding: 16px 48px 20px;
  border-top: 1px solid rgba(45, 62, 89, 0.2);
  background: rgba(254, 253, 248, 0.94);
  backdrop-filter: blur(12px);
}

.promise-secondary-action,
.promise-primary-action {
  min-height: 48px;
  padding: 10px 20px;
  cursor: pointer;
}

.promise-secondary-action {
  border: 1px dashed var(--blue-deep);
  border-radius: 5px;
  color: var(--ink-soft);
  background: var(--cream);
  font-weight: 700;
}

.promise-primary-action {
  display: inline-flex;
  gap: 9px;
  align-items: center;
  justify-content: center;
  border: 1.5px solid var(--ink);
  border-radius: 5px 9px 5px 8px;
  background: var(--sky);
  box-shadow: 4px 5px 0 var(--ink);
  font-weight: 800;
}

.promise-primary-action:hover {
  box-shadow: 2px 3px 0 var(--ink);
  transform: translate(2px, 2px);
}

.promise-primary-action svg {
  width: 25px;
  height: 22px;
  fill: none;
  stroke: var(--ink);
  stroke-width: 2.4;
  stroke-linejoin: round;
}

.promise-success[hidden],
.promise-editor[hidden] {
  display: none;
}

.promise-success {
  min-height: min(760px, calc(100vh - 48px));
  padding: 52px 28px 42px;
  text-align: center;
}

.success-kicker {
  margin: 0 auto;
  padding: 3px 8px;
  background: var(--butter);
  transform: rotate(-2deg);
}

.success-illustration {
  position: relative;
  width: min(100%, 370px);
  margin: 4px auto -15px;
}

.success-illustration svg {
  position: relative;
  z-index: 2;
  display: block;
  width: 100%;
  overflow: visible;
}

.success-rays {
  position: absolute;
  inset: 28px 27px 23px;
  border-radius: 50%;
  background:
    radial-gradient(circle, rgba(254, 253, 248, 0.94) 2.5px, transparent 3px) 0 0 / 22px 22px,
    var(--powder);
  transform: rotate(-4deg);
}

.success-bird-body,
.success-bird-head {
  fill: var(--cream);
  stroke: var(--ink);
  stroke-width: 5;
}

.success-bird-wing {
  fill: #edf3f1;
  stroke: var(--ink);
  stroke-width: 5;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.success-bird-tail {
  fill: var(--blue);
  stroke: var(--ink);
  stroke-width: 5;
  stroke-linejoin: round;
}

.success-bird-beak {
  fill: var(--butter);
  stroke: var(--ink);
  stroke-width: 5;
  stroke-linejoin: round;
}

.success-bird-eye {
  fill: var(--ink);
}

.success-bird-leg {
  fill: none;
  stroke: var(--ink);
  stroke-width: 5;
  stroke-linecap: round;
}

.success-letter path:first-child {
  fill: var(--cream);
  stroke: var(--ink);
  stroke-width: 4;
  stroke-linejoin: round;
}

.success-letter path:last-child {
  fill: none;
  stroke: var(--ink);
  stroke-width: 4;
  stroke-linejoin: round;
}

.success-done-stamp {
  position: absolute;
  z-index: 3;
  right: -13px;
  bottom: 38px;
  display: grid;
  width: 86px;
  height: 72px;
  place-items: center;
  border: 3px double #ae6a62;
  border-radius: 48% 52% 46% 54%;
  color: #a85f58;
  background: var(--cream);
  font-family: var(--font-display);
  font-size: 18px;
  font-weight: 900;
  letter-spacing: 0.06em;
  transform: rotate(-10deg);
}

.promise-success h2 {
  margin: 7px 0 8px;
  font-size: clamp(34px, 5vw, 54px);
  line-height: 1.2;
}

.promise-success > p:not(.success-kicker) {
  max-width: 580px;
  margin: 0 auto 24px;
  color: var(--ink-soft);
  font-size: 14px;
}

.promise-receipt {
  position: relative;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 0 28px;
  width: min(100%, 600px);
  margin: 0 auto 28px;
  padding: 20px 26px 23px;
  border: 1px dashed var(--blue-deep);
  background:
    repeating-linear-gradient(0deg, transparent, transparent 29px, rgba(45, 62, 89, 0.05) 30px),
    var(--paper);
  text-align: left;
  transform: rotate(-0.5deg);
}

.promise-receipt::before,
.promise-receipt::after {
  position: absolute;
  top: 50%;
  width: 17px;
  height: 34px;
  border-radius: 50%;
  background: var(--cream);
  content: "";
  transform: translateY(-50%);
}

.promise-receipt::before {
  left: -9px;
}

.promise-receipt::after {
  right: -9px;
}

.promise-receipt > span {
  grid-column: 1 / -1;
  color: var(--blue-deep);
  font-family: var(--font-display);
  font-size: 10px;
  font-weight: 900;
  letter-spacing: 0.14em;
}

.promise-receipt > strong {
  grid-column: 1 / -1;
  margin: 6px 0 13px;
  font-size: 20px;
}

.promise-receipt div {
  display: flex;
  flex-direction: column;
  padding-top: 10px;
  border-top: 1px dashed rgba(45, 62, 89, 0.24);
}

.promise-receipt small {
  color: var(--ink-soft);
  font-size: 10px;
}

.promise-receipt b {
  font-size: 13px;
}

.success-actions {
  display: flex;
  justify-content: center;
  gap: 12px;
}

/* 救场票：领取、提交、验收与结算 */
.rescue-flow-modal {
  position: fixed;
  z-index: 920;
  inset: 0;
  display: grid;
  padding: 24px;
  place-items: center;
  visibility: hidden;
  opacity: 0;
  pointer-events: none;
  transition: opacity 0.25s ease, visibility 0.25s ease;
}

.rescue-flow-modal.is-open {
  visibility: visible;
  opacity: 1;
  pointer-events: auto;
}

.rescue-flow-backdrop {
  position: absolute;
  inset: 0;
  border: 0;
  background: rgba(45, 62, 89, 0.44);
  backdrop-filter: blur(8px);
  cursor: pointer;
}

.rescue-flow-dialog {
  position: relative;
  width: min(1120px, 100%);
  max-height: calc(100vh - 48px);
  overflow: auto;
  border: 1.5px solid rgba(45, 62, 89, 0.4);
  border-radius: 9px 15px 8px 13px;
  background:
    radial-gradient(circle, rgba(134, 155, 192, 0.1) 1px, transparent 1.6px) 0 0 / 23px 23px,
    var(--cream);
  box-shadow: 13px 16px 0 rgba(162, 217, 240, 0.35), 0 28px 80px rgba(45, 62, 89, 0.3);
  transform: translateY(18px) rotate(0.12deg);
  transition: transform 0.32s cubic-bezier(0.2, 0.8, 0.2, 1);
}

.rescue-flow-modal.is-open .rescue-flow-dialog {
  transform: translateY(0) rotate(0.12deg);
}

.rescue-ticket-tape {
  position: absolute;
  z-index: 4;
  top: -6px;
  left: calc(50% - 50px);
  width: 100px;
  height: 27px;
  background: rgba(245, 230, 168, 0.86);
  clip-path: polygon(2% 5%, 98% 0, 96% 94%, 0 100%);
  transform: rotate(1.2deg);
}

.rescue-flow-close {
  position: absolute;
  z-index: 7;
  top: 20px;
  right: 22px;
  width: 42px;
  height: 42px;
  border: 1px dashed rgba(45, 62, 89, 0.38);
  border-radius: 50%;
  background: rgba(254, 253, 248, 0.9);
  cursor: pointer;
}

.rescue-flow-close span {
  position: absolute;
  top: 19px;
  left: 11px;
  width: 19px;
  height: 2px;
  border-radius: 99px;
  background: var(--ink);
}

.rescue-flow-close span:first-child {
  transform: rotate(45deg);
}

.rescue-flow-close span:last-child {
  transform: rotate(-45deg);
}

.rescue-flow-heading {
  display: flex;
  justify-content: space-between;
  gap: 28px;
  align-items: end;
  padding: 45px 90px 24px 42px;
  border-bottom: 1px dashed rgba(45, 62, 89, 0.24);
}

.rescue-flow-heading p,
.rescue-panel-kicker,
.submission-number,
.paid-kicker {
  width: fit-content;
  margin: 0;
  color: var(--blue-deep);
  font-family: var(--font-display);
  font-size: 10px;
  font-weight: 900;
  letter-spacing: 0.16em;
}

.rescue-flow-heading p {
  padding: 3px 8px;
  background: var(--butter);
  transform: rotate(-1deg);
}

.rescue-flow-heading h2 {
  margin: 10px 0 0;
  font-size: clamp(29px, 4vw, 45px);
  line-height: 1.2;
  letter-spacing: -0.035em;
}

.rescue-heading-label {
  flex: 0 0 auto;
  margin-bottom: 6px;
  padding: 5px 10px;
  border: 1px dashed var(--blue-deep);
  color: var(--ink-soft);
  background: var(--paper-blue);
  font-size: 11px;
  transform: rotate(1.4deg);
}

.rescue-progress {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  margin: 0;
  padding: 0 42px;
  border-bottom: 1px solid rgba(45, 62, 89, 0.18);
  background: rgba(233, 243, 245, 0.6);
  list-style: none;
}

.rescue-progress li {
  position: relative;
  display: flex;
  gap: 8px;
  align-items: center;
  min-height: 64px;
  padding: 0 14px;
  border-left: 1px dashed rgba(45, 62, 89, 0.22);
  color: #8793a2;
  font-size: 12px;
}

.rescue-progress li:first-child {
  border-left: 0;
}

.rescue-progress li::after {
  position: absolute;
  right: -5px;
  bottom: -3px;
  left: -5px;
  height: 4px;
  border-radius: 99px;
  background: transparent;
  content: "";
}

.rescue-progress b {
  color: inherit;
  font-family: var(--font-display);
  font-size: 10px;
}

.rescue-progress li.is-done {
  color: var(--blue-deep);
}

.rescue-progress li.is-done b::after {
  margin-left: 3px;
  content: "✓";
}

.rescue-progress li.is-current {
  color: var(--ink);
  font-weight: 800;
}

.rescue-progress li.is-current::after {
  background: var(--sky);
}

.rescue-flow-panel[hidden] {
  display: none;
}

.rescue-flow-panel {
  display: grid;
  grid-template-columns: minmax(0, 1.45fr) minmax(290px, 0.55fr);
  gap: 27px;
  padding: 32px 42px 0;
}

.rescue-task-paper,
.submission-paper,
.review-delivery-paper {
  position: relative;
  padding: 34px 34px 30px;
  border: 1px solid rgba(45, 62, 89, 0.27);
  border-radius: 5px 10px 5px 8px;
  background:
    repeating-linear-gradient(0deg, transparent, transparent 31px, rgba(45, 62, 89, 0.05) 32px),
    rgba(254, 253, 248, 0.96);
  box-shadow: 6px 8px 0 rgba(134, 155, 192, 0.16);
}

.rescue-task-paper::after,
.submission-paper::after,
.review-delivery-paper::after {
  position: absolute;
  right: -8px;
  bottom: 38px;
  width: 17px;
  height: 68px;
  background: radial-gradient(circle at 0 9px, var(--cream) 7px, transparent 7.5px) 0 0 / 17px 18px repeat-y;
  content: "";
}

.rescue-paper-clip {
  position: absolute;
  top: -18px;
  left: 45px;
  width: 17px;
  height: 54px;
  border: 3px solid var(--blue-deep);
  border-radius: 11px;
  transform: rotate(8deg);
}

.rescue-paper-clip::after {
  position: absolute;
  inset: 5px 3px 14px;
  border: 2px solid var(--blue-deep);
  border-radius: 8px;
  content: "";
}

.rescue-task-paper h3,
.submission-paper h3,
.review-delivery-paper h3 {
  margin: 9px 0 10px;
  font-size: clamp(25px, 3.2vw, 36px);
  line-height: 1.3;
}

.rescue-task-copy,
.submission-paper > p,
.review-delivery-paper > p {
  margin: 0;
  color: var(--ink-soft);
  font-size: 13px;
}

.rescue-task-meta {
  display: grid;
  grid-template-columns: 1fr 1fr;
  margin: 24px 0;
  border-top: 1px dashed rgba(45, 62, 89, 0.27);
  border-bottom: 1px dashed rgba(45, 62, 89, 0.27);
}

.rescue-task-meta div {
  display: flex;
  min-height: 68px;
  flex-direction: column;
  justify-content: center;
  padding: 10px 12px;
  border-left: 1px dashed rgba(45, 62, 89, 0.24);
}

.rescue-task-meta div:first-child,
.rescue-task-meta div:nth-child(3) {
  border-left: 0;
}

.rescue-task-meta div:nth-child(3) {
  grid-column: 1 / -1;
  border-top: 1px dashed rgba(45, 62, 89, 0.24);
}

.rescue-task-meta dt,
.claimed-receipt dt,
.paid-receipt small {
  color: var(--ink-soft);
  font-size: 10px;
}

.rescue-task-meta dd,
.claimed-receipt dd {
  margin: 2px 0 0;
  font-size: 13px;
  font-weight: 700;
}

.rescue-checklist {
  padding: 16px 18px;
  border-left: 4px solid var(--butter);
  background: rgba(245, 230, 168, 0.2);
}

.rescue-checklist > p {
  margin: 0 0 8px;
  font-size: 12px;
  font-weight: 800;
}

.rescue-checklist label {
  display: grid;
  grid-template-columns: 18px 1fr;
  gap: 8px;
  align-items: start;
  margin: 8px 0;
  color: var(--ink-soft);
  font-size: 12px;
}

.rescue-checklist em {
  font-style: normal;
}

.rescue-checklist input {
  position: absolute;
  opacity: 0;
}

.rescue-checklist label span {
  position: relative;
  width: 16px;
  height: 16px;
  border: 1.5px solid var(--ink);
  border-radius: 3px;
  background: var(--sky);
}

.rescue-checklist label span::after {
  position: absolute;
  top: 1px;
  left: 5px;
  width: 4px;
  height: 8px;
  border: solid var(--ink);
  border-width: 0 2px 2px 0;
  content: "";
  transform: rotate(45deg);
}

.rescue-paper-note {
  margin: 18px 0 0;
  color: var(--ink-soft);
  font-size: 11px;
}

.rescue-reward-pocket {
  position: relative;
  min-height: 510px;
  padding: 27px 22px;
  overflow: hidden;
  border: 1.5px solid var(--blue-deep);
  border-radius: 5px 11px 6px 9px;
  background:
    radial-gradient(circle, rgba(254, 253, 248, 0.94) 2.1px, transparent 2.7px) 0 0 / 18px 18px,
    var(--powder);
  text-align: center;
  box-shadow: 6px 8px 0 rgba(134, 155, 192, 0.2);
  transform: rotate(0.4deg);
}

.rescue-reward-pocket .rescue-panel-kicker {
  margin: 0 auto;
  padding: 2px 7px;
  color: var(--ink);
  background: var(--cream);
}

.rescue-reward-number {
  display: flex;
  justify-content: center;
  gap: 8px;
  align-items: baseline;
  margin-top: 17px;
}

.rescue-reward-number strong {
  font-family: var(--font-display);
  font-size: 64px;
  line-height: 1;
}

.rescue-reward-number span {
  font-family: var(--font-display);
  font-size: 13px;
  font-weight: 900;
}

.rescue-reward-pocket > p:not(.rescue-panel-kicker) {
  margin: 9px 0 0;
  color: var(--ink-soft);
  font-size: 12px;
}

.flow-money-pigeon {
  position: absolute;
  right: 5px;
  bottom: 17px;
  left: 3px;
  width: calc(100% - 8px);
}

.flow-bird-body,
.flow-bird-head {
  fill: var(--cream);
  stroke: var(--ink);
  stroke-width: 4.5;
}

.flow-bird-wing {
  fill: #edf3f1;
  stroke: var(--ink);
  stroke-width: 4.5;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.flow-bird-tail {
  fill: var(--blue);
  stroke: var(--ink);
  stroke-width: 4.5;
  stroke-linejoin: round;
}

.flow-bird-beak {
  fill: var(--butter);
  stroke: var(--ink);
  stroke-width: 4.5;
  stroke-linejoin: round;
}

.flow-bird-eye {
  fill: var(--ink);
}

.flow-bird-leg {
  fill: none;
  stroke: var(--ink);
  stroke-width: 4;
  stroke-linecap: round;
}

.flow-money-bag path:first-child {
  fill: var(--butter);
  stroke: var(--ink);
  stroke-width: 4;
  stroke-linejoin: round;
}

.flow-money-bag path:nth-child(2) {
  fill: var(--peach);
  stroke: var(--ink);
  stroke-width: 4;
  stroke-linejoin: round;
}

.flow-money-bag path:last-child {
  fill: none;
  stroke: var(--ink);
  stroke-width: 3.5;
  stroke-linecap: round;
}

.reward-source-stamp {
  position: absolute;
  right: 16px;
  bottom: 20px;
  display: grid;
  width: 73px;
  height: 65px;
  place-items: center;
  border: 2px solid var(--blue-deep);
  border-radius: 48% 52% 46% 54%;
  color: var(--blue-deep);
  background: rgba(254, 253, 248, 0.88);
  box-shadow: inset 0 0 0 4px rgba(254, 253, 248, 0.8), inset 0 0 0 5px rgba(102, 124, 159, 0.65);
  font-family: var(--font-display);
  font-size: 8px;
  line-height: 1.15;
  text-align: center;
  transform: rotate(-8deg);
}

.reward-source-stamp strong {
  display: block;
  font-size: 12px;
}

.rescue-panel-actions {
  position: sticky;
  z-index: 5;
  bottom: 0;
  grid-column: 1 / -1;
  display: flex;
  justify-content: flex-end;
  gap: 12px;
  margin: 0 -42px;
  padding: 16px 42px 20px;
  border-top: 1px solid rgba(45, 62, 89, 0.2);
  background: rgba(254, 253, 248, 0.95);
  backdrop-filter: blur(12px);
}

.flow-secondary-button,
.flow-primary-button {
  min-height: 48px;
  padding: 10px 20px;
  cursor: pointer;
}

.flow-secondary-button {
  border: 1px dashed var(--blue-deep);
  border-radius: 4px;
  color: var(--ink-soft);
  background: var(--cream);
  font-weight: 700;
}

.flow-primary-button {
  border: 1.5px solid var(--ink);
  border-radius: 5px 9px 5px 8px;
  background: var(--sky);
  box-shadow: 4px 5px 0 var(--ink);
  font-weight: 800;
}

.flow-primary-button:hover {
  box-shadow: 2px 3px 0 var(--ink);
  transform: translate(2px, 2px);
}

.rescue-submit-panel,
.rescue-review-panel {
  grid-template-columns: minmax(0, 1.3fr) minmax(290px, 0.7fr);
}

.submission-number {
  display: inline-block;
  padding: 3px 8px;
  background: var(--butter);
  transform: rotate(-1deg);
}

.submission-field {
  display: flex;
  flex-direction: column;
  gap: 5px;
  margin-top: 22px;
}

.submission-field > span {
  color: var(--ink-soft);
  font-size: 11px;
}

.submission-field input,
.submission-field textarea {
  width: 100%;
  padding: 10px 3px;
  border: 0;
  border-bottom: 1.5px solid rgba(45, 62, 89, 0.4);
  border-radius: 0;
  outline: 0;
  color: var(--ink);
  background: transparent;
  font: 600 14px/1.6 var(--font-sans);
  resize: vertical;
}

.submission-field input:focus,
.submission-field textarea:focus {
  border-color: var(--blue-deep);
  box-shadow: 0 3px 0 rgba(199, 217, 243, 0.72);
}

.submission-attachment {
  display: flex;
  gap: 12px;
  align-items: center;
  margin-top: 24px;
  padding: 12px 15px;
  border: 1px dashed rgba(45, 62, 89, 0.32);
  background: var(--paper-blue);
}

.submission-attachment svg {
  flex: 0 0 48px;
  width: 48px;
  fill: var(--cream);
  stroke: var(--ink);
  stroke-width: 2.4;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.submission-attachment span {
  display: flex;
  flex-direction: column;
}

.submission-attachment strong {
  font-size: 13px;
}

.submission-attachment small {
  color: var(--ink-soft);
  font-size: 10px;
}

.claimed-receipt {
  position: relative;
  min-height: 480px;
  padding: 27px 24px 170px;
  overflow: hidden;
  border: 1.5px dashed var(--blue-deep);
  background:
    repeating-linear-gradient(0deg, transparent, transparent 30px, rgba(45, 62, 89, 0.05) 31px),
    var(--paper);
  box-shadow: 6px 8px 0 rgba(134, 155, 192, 0.18);
  transform: rotate(0.4deg);
}

.claimed-receipt > p {
  margin: 0 0 14px;
  color: var(--blue-deep);
  font-family: var(--font-display);
  font-size: 10px;
  font-weight: 900;
  letter-spacing: 0.14em;
}

.claimed-receipt dl {
  margin: 0;
}

.claimed-receipt dl div {
  display: flex;
  justify-content: space-between;
  gap: 16px;
  padding: 10px 0;
  border-top: 1px dashed rgba(45, 62, 89, 0.24);
}

.claimed-receipt dd {
  max-width: 60%;
  text-align: right;
}

.claimed-receipt dd b {
  font-family: var(--font-display);
  font-size: 17px;
}

.claimed-stamp {
  position: absolute;
  right: 18px;
  bottom: 148px;
  display: grid;
  width: 92px;
  height: 72px;
  place-items: center;
  border: 3px double #b66e66;
  border-radius: 48% 52% 46% 54%;
  color: #aa625c;
  background: rgba(254, 253, 248, 0.88);
  font-family: var(--font-display);
  font-size: 13px;
  font-weight: 900;
  line-height: 1.1;
  text-align: center;
  transform: rotate(-8deg);
}

.claimed-stamp strong {
  display: block;
  font-size: 14px;
}

.claimed-pigeon {
  position: absolute;
  right: 6px;
  bottom: 3px;
  width: 175px;
}

.review-link-slip {
  display: flex;
  flex-direction: column;
  margin: 24px 0;
  padding: 17px 19px;
  border: 1px dashed var(--blue-deep);
  background: var(--paper-blue);
  transform: rotate(-0.4deg);
}

.review-link-slip span {
  color: var(--ink-soft);
  font-size: 10px;
}

.review-link-slip strong {
  margin: 4px 0 8px;
  overflow-wrap: anywhere;
  font-size: 14px;
}

.review-link-slip small {
  color: var(--ink-soft);
  font-size: 11px;
}

.review-checks {
  display: grid;
  gap: 12px;
}

.review-checks span {
  display: flex;
  gap: 9px;
  align-items: center;
  font-size: 12px;
}

.review-checks i {
  position: relative;
  width: 19px;
  height: 19px;
  border: 1.5px solid var(--ink);
  border-radius: 50%;
  background: var(--mint);
}

.review-checks i::after {
  position: absolute;
  top: 2px;
  left: 6px;
  width: 5px;
  height: 9px;
  border: solid var(--ink);
  border-width: 0 2px 2px 0;
  content: "";
  transform: rotate(45deg);
}

.reviewer-paper {
  position: relative;
  min-height: 470px;
  padding: 27px 23px;
  overflow: hidden;
  border: 1.5px solid var(--blue-deep);
  border-radius: 6px 10px 5px 9px;
  background:
    radial-gradient(circle, rgba(254, 253, 248, 0.92) 2px, transparent 2.6px) 0 0 / 18px 18px,
    var(--powder);
  text-align: center;
  box-shadow: 6px 8px 0 rgba(134, 155, 192, 0.2);
}

.reviewer-label {
  display: inline-block;
  padding: 2px 7px;
  background: var(--cream);
  font-family: var(--font-display);
  font-size: 10px;
  font-weight: 900;
  letter-spacing: 0.14em;
  transform: rotate(1deg);
}

.review-pigeon {
  display: block;
  width: 210px;
  max-width: 100%;
  margin: 14px auto 0;
}

.flow-rubber-stamp path {
  fill: var(--peach);
  stroke: var(--ink);
  stroke-width: 3.2;
  stroke-linejoin: round;
}

.reviewer-paper h4 {
  margin: 7px 0 4px;
  font-size: 18px;
}

.reviewer-paper > p {
  margin: 0 auto;
  color: var(--ink-soft);
  font-size: 11px;
}

.reviewer-sign {
  position: absolute;
  right: 17px;
  bottom: 18px;
  padding: 6px 10px;
  border: 1px dashed var(--blue-deep);
  color: var(--blue-deep);
  background: var(--cream);
  font-family: var(--font-display);
  font-size: 9px;
  font-weight: 900;
  transform: rotate(-4deg);
}

.rescue-paid-panel {
  display: block;
  padding: 32px 42px 0;
  text-align: center;
}

.paid-kicker {
  margin: 0 auto;
  padding: 3px 8px;
  background: var(--butter);
  transform: rotate(-1deg);
}

.paid-illustration {
  position: relative;
  width: min(100%, 360px);
  margin: 0 auto -8px;
}

.paid-illustration svg {
  position: relative;
  z-index: 2;
  display: block;
  width: 100%;
  overflow: visible;
}

.paid-polka {
  position: absolute;
  inset: 32px 20px 19px;
  border-radius: 48% 52% 46% 54%;
  background:
    radial-gradient(circle, rgba(254, 253, 248, 0.94) 2.4px, transparent 3px) 0 0 / 21px 21px,
    var(--powder);
  transform: rotate(3deg);
}

.paid-receipt-art path:first-child {
  fill: var(--cream);
  stroke: var(--ink);
  stroke-width: 4;
  stroke-linejoin: round;
}

.paid-receipt-art path:not(:first-child) {
  fill: none;
  stroke: var(--blue-deep);
  stroke-width: 3;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.paid-done-stamp {
  position: absolute;
  z-index: 3;
  right: -8px;
  bottom: 32px;
  display: grid;
  width: 91px;
  height: 75px;
  place-items: center;
  border: 3px double #ad655f;
  border-radius: 47% 53% 45% 55%;
  color: #a45c57;
  background: var(--cream);
  font-family: var(--font-display);
  font-size: 16px;
  font-weight: 900;
  line-height: 1.1;
  transform: rotate(-9deg);
}

.paid-done-stamp strong {
  display: block;
  font-size: 12px;
}

.rescue-paid-panel > h3 {
  margin: 7px 0 7px;
  font-size: clamp(28px, 4vw, 42px);
}

.rescue-paid-panel > p:not(.paid-kicker) {
  max-width: 570px;
  margin: 0 auto 22px;
  color: var(--ink-soft);
  font-size: 13px;
}

.paid-receipt {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 0 18px;
  width: min(100%, 720px);
  margin: 0 auto 27px;
  padding: 18px 24px 21px;
  border: 1.5px dashed var(--blue-deep);
  background:
    repeating-linear-gradient(0deg, transparent, transparent 28px, rgba(45, 62, 89, 0.05) 29px),
    var(--paper);
  text-align: left;
  transform: rotate(-0.35deg);
}

.paid-receipt > span {
  grid-column: 1 / -1;
  color: var(--blue-deep);
  font-family: var(--font-display);
  font-size: 9px;
  font-weight: 900;
  letter-spacing: 0.14em;
}

.paid-receipt > strong {
  grid-column: 1 / -1;
  margin: 5px 0 12px;
  font-size: 19px;
}

.paid-receipt div {
  display: flex;
  flex-direction: column;
  padding-top: 8px;
  border-top: 1px dashed rgba(45, 62, 89, 0.24);
}

.paid-receipt b {
  font-size: 12px;
}

.paid-receipt em {
  font-family: var(--font-display);
  font-size: 18px;
  font-style: normal;
}

.paid-actions {
  margin-right: -42px;
  margin-left: -42px;
}

.mobile-nav {
  display: none;
}

.toast {
  position: fixed;
  z-index: 1100;
  left: 50%;
  bottom: 28px;
  max-width: calc(100% - 36px);
  padding: 11px 17px;
  border: 1.5px solid var(--ink);
  border-radius: 8px;
  background: var(--cream);
  box-shadow: 4px 5px 0 var(--powder);
  font-size: 13px;
  opacity: 0;
  pointer-events: none;
  transform: translate(-50%, 12px);
  transition: opacity 0.25s ease, transform 0.25s ease;
}

.toast.is-visible {
  opacity: 1;
  transform: translate(-50%, 0);
}

@media (max-width: 1050px) {
  .topbar,
  main {
    width: min(94%, 940px);
  }

  .desktop-nav {
    gap: 2px;
  }

  .nav-link {
    padding-inline: 9px;
    font-size: 13px;
  }

  .wallet-button {
    font-size: 12px;
  }

  .hero {
    grid-template-columns: 1.05fr 0.95fr;
    padding: 52px 38px;
  }

  .hero h2 {
    font-size: clamp(41px, 5.5vw, 59px);
  }

  .workbench {
    grid-template-columns: minmax(0, 1.25fr) minmax(280px, 0.75fr);
  }

  .project-paper {
    padding-inline: 28px;
  }

  .ticket-list {
    grid-template-columns: 1fr;
  }
}

@media (max-width: 820px) {
  body {
    padding-bottom: 96px;
  }

  .topbar {
    position: relative;
    top: 0;
    grid-template-columns: 1fr auto;
    width: calc(100% - 28px);
    margin-top: 12px;
  }

  .desktop-nav {
    display: none;
  }

  main {
    width: calc(100% - 28px);
    margin-top: 16px;
  }

  .hero {
    grid-template-columns: 1fr 0.82fr;
    gap: 18px;
    min-height: 520px;
    padding: 46px 28px;
  }

  .hero h2 {
    font-size: clamp(38px, 6.5vw, 54px);
  }

  .hero-description {
    font-size: 15px;
  }

  .hero-art {
    min-height: 350px;
  }

  .mechanism-strip {
    grid-template-columns: 1fr;
    gap: 14px;
    margin-bottom: 80px;
  }

  .steps li {
    padding: 0 8px;
    font-size: 11px;
  }

  .workbench {
    grid-template-columns: 1fr;
    padding: 25px;
  }

  .fund-pocket {
    min-height: 390px;
  }

  .pigeon-money {
    max-height: 230px;
  }

  .mobile-nav {
    position: fixed;
    z-index: 70;
    right: 14px;
    bottom: 14px;
    left: 14px;
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    max-width: 560px;
    height: 70px;
    margin: auto;
    padding: 7px;
    border: 1px solid rgba(45, 62, 89, 0.22);
    border-radius: 999px;
    background: rgba(254, 253, 248, 0.92);
    box-shadow: 0 12px 35px rgba(45, 62, 89, 0.18);
    backdrop-filter: blur(14px);
  }

  .mobile-nav-item {
    display: flex;
    flex-direction: column;
    gap: 1px;
    align-items: center;
    justify-content: center;
    border: 0;
    border-radius: 999px;
    color: var(--ink-soft);
    background: transparent;
    font-size: 10px;
    cursor: pointer;
  }

  .mobile-nav-item svg {
    width: 25px;
    height: 25px;
  }

  .mobile-nav-item:first-child svg {
    width: 27px;
  }

  .mobile-nav-item.is-active {
    color: var(--ink);
    background: var(--powder);
  }

  .toast {
    bottom: 96px;
  }
}

@media (max-width: 620px) {
  .skip-intro {
    top: 18px;
    right: 18px;
  }

  .intro-mark {
    width: 178px;
  }

  .brand-cn {
    font-size: 25px;
  }

  .wallet-button {
    max-width: 128px;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .brand-words small {
    display: none;
  }

  .brand-icon {
    width: 42px;
    height: 42px;
  }

  .hero {
    display: flex;
    flex-direction: column;
    gap: 24px;
    min-height: auto;
    padding: 42px 20px 24px;
    border-radius: 14px 18px 13px 16px;
  }

  .hero-copy {
    width: 100%;
  }

  .hero h2 {
    font-size: clamp(37px, 11vw, 50px);
  }

  .hero-description {
    margin-top: 18px;
    font-size: 14px;
    line-height: 1.8;
  }

  .hero-actions {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px;
  }

  .primary-button {
    justify-content: center;
    padding-inline: 12px;
  }

  .rescue-stamp {
    gap: 6px;
    width: 100%;
    min-width: 0;
    padding: 7px 9px 7px 7px;
  }

  .stamp-art {
    flex-basis: 46px;
    width: 46px;
  }

  .stamp-art svg {
    width: 44px;
  }

  .hero-art {
    width: 100%;
    min-height: 285px;
  }

  .polka-card {
    inset: 0 5px 10px;
  }

  .mail-stamp {
    top: 5px;
    right: 8px;
    width: 64px;
    height: 64px;
    padding-top: 18px;
    font-size: 7px;
  }

  .pigeon-letter {
    width: min(100%, 330px);
  }

  .art-caption {
    right: 13px;
    bottom: 9px;
  }

  .mechanism-strip {
    margin-top: 20px;
    padding: 22px 19px;
  }

  .steps {
    grid-template-columns: 1fr 1fr;
    gap: 8px 0;
  }

  .steps li:nth-child(3) {
    border-left: 0;
  }

  .section-heading {
    display: block;
  }

  .section-heading > p {
    margin-top: 9px;
    text-align: left;
  }

  .workbench {
    padding: 14px;
    border-radius: 12px;
  }

  .project-paper {
    padding: 34px 20px 27px;
  }

  .paper-clip {
    left: 31px;
  }

  .paper-header {
    gap: 12px;
  }

  .paper-header h3 {
    font-size: 25px;
  }

  .status-stamp {
    padding: 9px 7px;
    font-size: 10px;
  }

  .promise-meta {
    grid-template-columns: 1fr;
  }

  .promise-meta div,
  .promise-meta div:first-child {
    border-top: 1px dashed var(--line);
    border-left: 0;
  }

  .promise-meta div:first-child {
    border-top: 0;
  }

  .timeline {
    grid-template-columns: 1fr;
    gap: 11px;
  }

  .timeline-item {
    flex-direction: row;
    align-items: center;
  }

  .timeline-item::after {
    top: 18px;
    left: 8px;
    width: 0;
    height: 24px;
    border-top: 0;
    border-left: 2px dashed rgba(45, 62, 89, 0.22);
  }

  .fund-pocket {
    min-height: 390px;
  }

  .rescue-section {
    margin-top: 85px;
  }

  .rescue-ticket {
    grid-template-columns: auto 1fr;
    gap: 13px;
    min-height: 190px;
    padding: 24px 29px 42px 23px;
  }

  .ticket-reward {
    position: absolute;
    right: 28px;
    bottom: 18px;
    flex-direction: row;
    gap: 4px;
  }

  .ticket-reward b {
    font-size: 24px;
  }

  .ticket-status {
    right: auto;
    bottom: 17px;
    left: 53px;
  }
}

@media (max-width: 1050px) {
  .promise-modal-layout {
    grid-template-columns: minmax(0, 1.25fr) minmax(280px, 0.75fr);
    gap: 25px;
  }

  .promise-editor {
    padding-right: 34px;
    padding-left: 34px;
  }

  .promise-modal-actions {
    margin-right: -34px;
    margin-left: -34px;
    padding-right: 34px;
    padding-left: 34px;
  }
}

@media (max-width: 820px) {
  .promise-modal {
    padding: 0;
  }

  .promise-dialog {
    width: 100%;
    height: 100dvh;
    max-height: none;
    border: 0;
    border-radius: 0;
    box-shadow: none;
    transform: translateY(24px);
  }

  .promise-modal.is-open .promise-dialog {
    transform: translateY(0);
  }

  .promise-dialog-tape {
    top: 0;
  }

  .promise-modal-close {
    position: fixed;
    top: 16px;
    right: 16px;
  }

  .promise-editor {
    padding: 40px 24px 0;
  }

  .promise-modal-heading {
    padding-right: 48px;
  }

  .promise-modal-layout {
    grid-template-columns: 1fr;
    gap: 26px;
  }

  .promise-preview {
    position: relative;
    top: 0;
    display: grid;
    grid-template-columns: 190px 1fr;
    gap: 17px;
    min-height: 0;
    padding: 24px;
  }

  .preview-label {
    position: absolute;
    top: 18px;
    left: 20px;
  }

  .preview-pigeon {
    align-self: center;
    margin: 26px auto 0;
  }

  .preview-copy {
    align-self: center;
    margin-top: 14px;
  }

  .preview-stamp {
    right: 18px;
    bottom: 22px;
  }

  .preview-note {
    grid-column: 1 / -1;
    max-width: none;
    margin: 0;
  }

  .preview-pin {
    display: none;
  }

  .promise-modal-actions {
    margin-right: -24px;
    margin-left: -24px;
    padding: 14px 24px calc(14px + env(safe-area-inset-bottom));
  }

  .promise-success {
    min-height: 100dvh;
    padding-top: 44px;
  }
}

@media (max-width: 620px) {
  .promise-editor {
    padding: 34px 16px 0;
  }

  .promise-modal-heading {
    padding: 12px 52px 20px 0;
  }

  .promise-modal-heading h2 {
    margin-top: 10px;
    font-size: 30px;
  }

  .promise-modal-heading > p:last-child {
    font-size: 12px;
  }

  .promise-modal-layout {
    padding-top: 24px;
  }

  .scene-options {
    grid-template-columns: repeat(3, minmax(148px, 1fr));
    gap: 9px;
    padding: 2px 2px 7px;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    scrollbar-width: none;
  }

  .scene-options::-webkit-scrollbar {
    display: none;
  }

  .scene-option {
    min-height: 106px;
    flex-direction: column;
    gap: 5px;
    align-items: flex-start;
    padding: 10px 12px;
    scroll-snap-align: start;
  }

  .scene-option svg {
    width: 54px;
    height: 43px;
  }

  .scene-option strong {
    font-size: 13px;
  }

  .scene-option small {
    font-size: 10px;
  }

  .custom-scene-field {
    grid-template-columns: 1fr;
    gap: 3px;
  }

  .custom-scene-field small {
    padding-bottom: 0;
  }

  .promise-field-block {
    margin-right: -2px;
    margin-left: -2px;
    padding: 19px 14px 20px;
  }

  .promise-field-row {
    grid-template-columns: 1fr;
    gap: 15px;
  }

  .member-section-heading {
    align-items: flex-start;
  }

  .member-row {
    grid-template-columns: 38px minmax(0, 1fr) 27px;
    gap: 8px;
    padding: 13px 3px;
  }

  .member-row label:nth-of-type(2) {
    grid-column: 2 / -1;
    padding-right: 34px;
  }

  .remove-member {
    grid-column: 3;
    grid-row: 1;
  }

  .promise-preview {
    display: block;
    padding: 24px 17px 20px;
  }

  .preview-label {
    position: relative;
    top: auto;
    left: auto;
  }

  .preview-pigeon {
    width: 210px;
    margin-top: 2px;
  }

  .preview-copy {
    margin-top: -4px;
  }

  .preview-note {
    padding-right: 68px;
  }

  .promise-modal-actions {
    display: grid;
    grid-template-columns: 0.72fr 1.28fr;
    margin-right: -16px;
    margin-left: -16px;
    padding-right: 16px;
    padding-left: 16px;
  }

  .promise-secondary-action,
  .promise-primary-action {
    min-height: 48px;
    padding-right: 11px;
    padding-left: 11px;
    font-size: 13px;
  }

  .promise-success {
    display: flex;
    min-height: 100dvh;
    flex-direction: column;
    justify-content: center;
    padding: 56px 16px 34px;
  }

  .success-illustration {
    width: 300px;
    max-width: 90%;
  }

  .success-done-stamp {
    right: -4px;
    bottom: 33px;
    width: 75px;
    height: 63px;
    font-size: 16px;
  }

  .promise-success h2 {
    font-size: 38px;
  }

  .promise-receipt {
    grid-template-columns: 1fr;
    gap: 8px;
    padding: 18px 20px;
  }

  .promise-receipt div {
    padding-top: 6px;
  }

  .success-actions {
    display: grid;
    grid-template-columns: 1fr 1fr;
  }
}

@media (max-width: 1050px) {
  .rescue-flow-heading,
  .rescue-flow-panel,
  .rescue-paid-panel {
    padding-right: 30px;
    padding-left: 30px;
  }

  .rescue-progress {
    padding-right: 30px;
    padding-left: 30px;
  }

  .rescue-flow-panel {
    grid-template-columns: minmax(0, 1.25fr) minmax(270px, 0.75fr);
    gap: 22px;
  }

  .rescue-panel-actions,
  .paid-actions {
    margin-right: -30px;
    margin-left: -30px;
    padding-right: 30px;
    padding-left: 30px;
  }
}

@media (max-width: 820px) {
  .rescue-flow-modal {
    padding: 0;
  }

  .rescue-flow-dialog {
    width: 100%;
    height: 100dvh;
    max-height: none;
    border: 0;
    border-radius: 0;
    box-shadow: none;
    transform: translateY(24px);
  }

  .rescue-flow-modal.is-open .rescue-flow-dialog {
    transform: translateY(0);
  }

  .rescue-flow-close {
    position: fixed;
    top: 16px;
    right: 16px;
  }

  .rescue-ticket-tape {
    top: 0;
  }

  .rescue-flow-heading {
    padding: 41px 76px 22px 24px;
  }

  .rescue-flow-heading h2 {
    font-size: 34px;
  }

  .rescue-progress {
    padding-right: 16px;
    padding-left: 16px;
  }

  .rescue-progress li {
    min-height: 60px;
    justify-content: center;
    padding: 0 7px;
  }

  .rescue-flow-panel,
  .rescue-submit-panel,
  .rescue-review-panel {
    grid-template-columns: 1fr;
    padding: 26px 24px 0;
  }

  .rescue-reward-pocket {
    min-height: 390px;
  }

  .flow-money-pigeon {
    right: calc(50% - 145px);
    left: auto;
    width: 290px;
  }

  .submission-paper,
  .review-delivery-paper {
    min-height: 430px;
  }

  .claimed-receipt,
  .reviewer-paper {
    min-height: 390px;
  }

  .claimed-pigeon {
    width: 190px;
  }

  .rescue-paid-panel {
    padding: 29px 24px 0;
  }

  .rescue-panel-actions,
  .paid-actions {
    margin-right: -24px;
    margin-left: -24px;
    padding: 14px 24px calc(14px + env(safe-area-inset-bottom));
  }
}

@media (max-width: 620px) {
  .rescue-flow-heading {
    display: block;
    padding: 47px 60px 18px 16px;
  }

  .rescue-flow-heading h2 {
    margin-top: 8px;
    font-size: 28px;
  }

  .rescue-heading-label {
    display: inline-block;
    margin-top: 10px;
  }

  .rescue-progress {
    padding: 0 4px;
  }

  .rescue-progress li {
    min-height: 62px;
    flex-direction: column;
    gap: 1px;
    font-size: 10px;
  }

  .rescue-progress b {
    font-size: 9px;
  }

  .rescue-flow-panel,
  .rescue-submit-panel,
  .rescue-review-panel,
  .rescue-paid-panel {
    padding: 20px 16px 0;
  }

  .rescue-task-paper,
  .submission-paper,
  .review-delivery-paper {
    padding: 31px 20px 26px;
  }

  .rescue-task-paper h3,
  .submission-paper h3,
  .review-delivery-paper h3 {
    font-size: 25px;
  }

  .rescue-task-meta {
    grid-template-columns: 1fr;
  }

  .rescue-task-meta div,
  .rescue-task-meta div:first-child,
  .rescue-task-meta div:nth-child(3) {
    grid-column: auto;
    border-top: 1px dashed rgba(45, 62, 89, 0.24);
    border-left: 0;
  }

  .rescue-task-meta div:first-child {
    border-top: 0;
  }

  .rescue-reward-pocket {
    min-height: 365px;
  }

  .rescue-reward-number strong {
    font-size: 56px;
  }

  .submission-paper,
  .review-delivery-paper {
    min-height: 0;
  }

  .claimed-receipt {
    min-height: 410px;
    padding-right: 18px;
    padding-left: 18px;
  }

  .claimed-receipt dl div {
    display: grid;
    gap: 2px;
  }

  .claimed-receipt dd {
    max-width: none;
    text-align: left;
  }

  .reviewer-paper {
    min-height: 405px;
  }

  .rescue-panel-actions,
  .paid-actions {
    display: grid;
    grid-template-columns: 0.9fr 1.1fr;
    margin-right: -16px;
    margin-left: -16px;
    padding-right: 16px;
    padding-left: 16px;
  }

  .flow-secondary-button,
  .flow-primary-button {
    min-height: 48px;
    padding-right: 10px;
    padding-left: 10px;
    font-size: 12px;
  }

  .paid-illustration {
    width: 300px;
    max-width: 92%;
  }

  .paid-done-stamp {
    right: -3px;
    bottom: 28px;
    width: 78px;
    height: 66px;
    font-size: 14px;
  }

  .rescue-paid-panel > h3 {
    font-size: 31px;
    line-height: 1.25;
  }

  .paid-receipt {
    grid-template-columns: 1fr;
    gap: 7px;
    padding: 17px 19px;
  }

  .paid-receipt div {
    padding-top: 6px;
  }
}

@media (max-width: 820px) {
  .demo-scene-switcher {
    grid-template-columns: 1fr;
    gap: 13px;
    margin-bottom: 34px;
  }
}

@media (max-width: 620px) {
  .demo-scene-switcher {
    padding: 16px 13px;
  }

  .demo-scene-title strong {
    font-size: 13px;
  }

  .demo-scene-options {
    gap: 8px;
  }

  .demo-scene-button {
    min-height: 86px;
    flex-direction: column;
    gap: 3px;
    align-items: flex-start;
    padding: 8px 10px;
  }

  .demo-scene-button svg {
    width: 41px;
    height: 34px;
  }

  .demo-scene-button small {
    font-size: 9px;
  }
}

.timeline-confirm-button {
  padding: 0;
  border: 0;
  outline-offset: 5px;
  background: transparent;
  font: inherit;
  text-align: left;
  cursor: pointer;
}

.timeline-confirm-button small {
  width: fit-content;
  margin-top: -5px;
  border-bottom: 1px dashed var(--blue-deep);
  color: var(--blue-deep);
  font-family: var(--font-display);
  font-size: 9px;
  font-weight: 400;
  letter-spacing: 0.03em;
}

.timeline-confirm-button:hover span {
  color: var(--blue-deep);
}

.confirm-flow-dialog {
  width: min(1150px, 96vw);
}

.confirm-flow-panel[hidden] {
  display: none;
}

.promise-invite-layout {
  display: grid;
  grid-template-columns: minmax(0, 1.18fr) minmax(300px, 0.82fr);
  gap: 30px;
  align-items: stretch;
}

.promise-invite-paper {
  position: relative;
  min-width: 0;
  padding: 29px 31px 32px;
  border: 1.5px solid rgba(45, 62, 89, 0.36);
  background:
    linear-gradient(rgba(45, 62, 89, 0.055) 1px, transparent 1px) 0 58px / 100% 33px,
    var(--cream);
  box-shadow: 6px 7px 0 rgba(199, 217, 243, 0.48);
  clip-path: polygon(0 0, 100% 0, 100% 96%, 97% 100%, 93% 96%, 89% 100%, 85% 96%, 81% 100%, 77% 96%, 73% 100%, 69% 96%, 65% 100%, 61% 96%, 57% 100%, 53% 96%, 49% 100%, 45% 96%, 41% 100%, 37% 96%, 33% 100%, 29% 96%, 25% 100%, 21% 96%, 17% 100%, 13% 96%, 9% 100%, 5% 96%, 0 100%);
}

.confirm-scene-name {
  width: fit-content;
  margin: 18px 0 5px;
  padding: 2px 8px;
  color: var(--blue-deep);
  background: rgba(199, 217, 243, 0.55);
  font-family: var(--font-display);
  font-size: 13px;
  letter-spacing: 0.04em;
  transform: rotate(-1deg);
}

.promise-invite-paper h3 {
  margin: 0 0 7px;
  font-size: clamp(25px, 3vw, 35px);
  line-height: 1.25;
}

.promise-invite-paper > p:not(.confirm-scene-name) {
  margin: 0;
  color: var(--ink-soft);
  font-size: 12px;
}

.confirm-promise-meta {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  margin: 25px 0 21px;
  border-top: 1px dashed rgba(45, 62, 89, 0.28);
  border-bottom: 1px dashed rgba(45, 62, 89, 0.28);
}

.confirm-promise-meta div {
  display: flex;
  min-width: 0;
  flex-direction: column;
  gap: 3px;
  padding: 14px 10px;
  border-left: 1px dashed rgba(45, 62, 89, 0.24);
}

.confirm-promise-meta div:first-child { border-left: 0; }
.confirm-promise-meta dt { color: var(--ink-soft); font-size: 10px; }
.confirm-promise-meta dd { margin: 0; font-size: 13px; font-weight: 750; }
.confirm-promise-meta dd b { font-family: var(--font-display); font-size: 17px; font-weight: 400; }

.confirm-rules-note {
  padding: 12px 15px;
  border-left: 4px solid var(--butter);
  background: rgba(245, 230, 168, 0.18);
}

.confirm-rules-note span {
  color: var(--ink);
  font-family: var(--font-display);
  font-size: 13px;
}

.confirm-rules-note p {
  margin: 2px 0 0;
  color: var(--ink-soft);
  font-size: 11px;
}

.promise-invite-art {
  position: relative;
  display: flex;
  min-height: 360px;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 24px 20px 13px;
  overflow: hidden;
  border: 1.5px solid var(--ink);
  border-radius: 48% 52% 47% 53% / 45% 47% 53% 55%;
  background:
    radial-gradient(circle, rgba(254, 253, 248, 0.9) 2.5px, transparent 3px) 0 0 / 23px 23px,
    var(--powder);
  transform: rotate(0.8deg);
}

.promise-invite-art svg {
  width: min(100%, 300px);
}

.promise-invite-art > p {
  position: relative;
  z-index: 2;
  margin: -8px 0 0;
  padding: 5px 10px;
  border: 1px dashed rgba(45, 62, 89, 0.38);
  background: var(--cream);
  font-family: var(--font-display);
  font-size: 11px;
  transform: rotate(-1.5deg);
}

.invite-postmark {
  position: absolute;
  z-index: 2;
  top: 22px;
  right: 22px;
  display: grid;
  width: 72px;
  height: 72px;
  padding-top: 20px;
  place-items: center;
  border: 2px solid var(--blue-deep);
  border-radius: 50%;
  color: var(--blue-deep);
  background: rgba(254, 253, 248, 0.5);
  font-family: var(--font-display);
  font-size: 9px;
  line-height: 1.05;
  text-align: center;
  transform: rotate(9deg);
}

.invite-postmark::after {
  position: absolute;
  inset: 7px;
  border: 1px dashed var(--blue-deep);
  border-radius: 50%;
  content: "";
}

.confirm-letter-art path {
  fill: var(--cream);
  stroke: var(--ink);
  stroke-width: 4.5;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.confirm-letter-art path:not(:first-child) {
  fill: none;
  stroke-width: 3;
}

.member-signing-layout {
  display: grid;
  grid-template-columns: minmax(0, 1.2fr) minmax(270px, 0.8fr);
  gap: 28px;
}

.member-signing-paper {
  padding: 27px 29px 30px;
  border: 1.5px solid rgba(45, 62, 89, 0.35);
  background:
    linear-gradient(rgba(45, 62, 89, 0.05) 1px, transparent 1px) 0 58px / 100% 32px,
    var(--cream);
  box-shadow: 6px 7px 0 rgba(199, 217, 243, 0.46);
}

.member-signing-paper h3 {
  margin: 15px 0 4px;
  font-size: 24px;
}

.member-signing-paper > p {
  margin: 0 0 19px;
  color: var(--ink-soft);
  font-size: 11px;
}

.confirm-member-list {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 10px;
}

.confirm-member-card {
  position: relative;
  display: grid;
  grid-template-columns: 35px minmax(0, 1fr);
  gap: 9px;
  align-items: center;
  min-height: 64px;
  padding: 9px 12px;
  overflow: hidden;
  border: 1px dashed rgba(45, 62, 89, 0.3);
  border-radius: 3px 7px 4px 6px;
  color: var(--ink-soft);
  background: rgba(247, 244, 234, 0.72);
  text-align: left;
  cursor: pointer;
}

.confirm-member-card > i {
  display: grid;
  width: 32px;
  height: 32px;
  place-items: center;
  border: 1px solid var(--blue-deep);
  border-radius: 48% 52% 44% 56%;
  background: var(--paper-blue);
  font-family: var(--font-display);
  font-style: normal;
  font-size: 12px;
}

.confirm-member-card > span {
  display: flex;
  min-width: 0;
  flex-direction: column;
}

.confirm-member-card strong { font-size: 13px; }
.confirm-member-card small { overflow: hidden; color: var(--ink-soft); font-size: 9px; text-overflow: ellipsis; white-space: nowrap; }

.confirm-member-card > em {
  position: absolute;
  right: 8px;
  bottom: 6px;
  color: var(--blue-deep);
  font-family: var(--font-display);
  font-size: 9px;
  font-style: normal;
  transform: rotate(-4deg);
}

.confirm-member-card.is-selected {
  border-style: solid;
  border-color: var(--blue-deep);
  color: var(--ink);
  background: var(--paper-blue);
  box-shadow: 3px 4px 0 rgba(199, 217, 243, 0.62);
}

.confirm-member-card.is-signed {
  border-color: rgba(83, 131, 112, 0.52);
  color: var(--ink);
  background: rgba(201, 227, 216, 0.44);
}

.confirm-member-card.is-signed::after {
  position: absolute;
  top: 7px;
  right: 8px;
  display: grid;
  width: 42px;
  height: 34px;
  place-items: center;
  border: 1.5px solid #6d9b86;
  border-radius: 48% 52% 45% 55%;
  color: #56816d;
  content: "已确认";
  font-family: var(--font-display);
  font-size: 9px;
  transform: rotate(7deg);
}

.confirm-member-card.is-signed > em { display: none; }

.current-signer-slip {
  position: relative;
  min-height: 360px;
  padding: 28px 25px;
  border: 1.5px solid var(--ink);
  background:
    radial-gradient(circle, rgba(254, 253, 248, 0.88) 2.2px, transparent 2.8px) 0 0 / 22px 22px,
    var(--powder);
  clip-path: polygon(0 0, 100% 0, 100% 95%, 96% 100%, 90% 95%, 84% 100%, 78% 95%, 72% 100%, 66% 95%, 60% 100%, 54% 95%, 48% 100%, 42% 95%, 36% 100%, 30% 95%, 24% 100%, 18% 95%, 12% 100%, 6% 95%, 0 100%);
}

.current-signer-slip > span {
  padding: 3px 8px;
  border: 1px dashed var(--ink);
  background: var(--cream);
  font-family: var(--font-display);
  font-size: 10px;
}

.signer-number {
  display: grid;
  width: 44px;
  height: 44px;
  margin-top: 24px;
  place-items: center;
  border: 1.5px solid var(--blue-deep);
  border-radius: 48% 52% 44% 56%;
  background: var(--cream);
  font-family: var(--font-display);
  font-size: 16px;
  transform: rotate(-5deg);
}

.current-signer-slip h4 {
  margin: 11px 0 0;
  font-size: 25px;
}

.current-signer-slip > p {
  margin: 0;
  color: var(--ink-soft);
  font-size: 11px;
}

.current-signer-slip dl {
  display: grid;
  gap: 8px;
  margin: 22px 0 0;
}

.current-signer-slip dl div {
  display: flex;
  justify-content: space-between;
  gap: 10px;
  padding-bottom: 7px;
  border-bottom: 1px dashed rgba(45, 62, 89, 0.24);
}

.current-signer-slip dt { color: var(--ink-soft); font-size: 10px; }
.current-signer-slip dd { margin: 0; font-size: 11px; font-weight: 750; }
.current-signer-slip dd b { font-family: var(--font-display); font-size: 16px; font-weight: 400; }

.signature-line {
  position: absolute;
  right: 25px;
  bottom: 32px;
  left: 25px;
  text-align: center;
}

.signature-line i {
  display: block;
  border-top: 1.5px solid var(--ink);
}

.signature-line span {
  color: var(--ink-soft);
  font-family: var(--font-display);
  font-size: 10px;
}

.confirm-complete-panel {
  text-align: center;
}

.confirm-complete-art {
  position: relative;
  width: 330px;
  max-width: 92%;
  margin: 0 auto;
  padding: 4px 16px 0;
}

.confirm-polka {
  position: absolute;
  inset: 16px 0 8px;
  border: 1.5px solid var(--ink);
  border-radius: 48% 52% 46% 54%;
  background:
    radial-gradient(circle, rgba(254, 253, 248, 0.9) 2.3px, transparent 2.9px) 0 0 / 22px 22px,
    var(--powder);
  transform: rotate(1deg);
}

.confirm-complete-art svg {
  position: relative;
  z-index: 1;
  width: 100%;
}

.confirm-contract-art path {
  fill: var(--cream);
  stroke: var(--ink);
  stroke-width: 4.5;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.confirm-contract-art path:not(:first-child) { fill: none; stroke-width: 3; }

.confirm-done-stamp {
  position: absolute;
  z-index: 2;
  right: -4px;
  bottom: 23px;
  display: grid;
  width: 88px;
  height: 72px;
  place-items: center;
  border: 2px solid #6d9b86;
  border-radius: 47% 53% 45% 55%;
  color: #56816d;
  background: #f5fbf6;
  box-shadow: inset 0 0 0 4px #f5fbf6, inset 0 0 0 5px rgba(109, 155, 134, 0.58);
  font-family: var(--font-display);
  font-size: 16px;
  line-height: 1;
  transform: rotate(8deg);
}

.confirm-done-stamp b { font-size: 15px; font-weight: 400; }

.confirm-complete-panel > h3 {
  margin: 23px 0 5px;
  font-size: 32px;
}

.confirm-complete-panel > p {
  max-width: 630px;
  margin: 0 auto;
  color: var(--ink-soft);
  font-size: 13px;
}

.confirm-receipt {
  display: grid;
  grid-template-columns: 1.1fr repeat(3, 1fr);
  gap: 17px;
  align-items: center;
  max-width: 880px;
  margin: 25px auto 0;
  padding: 15px 18px;
  border: 1px dashed var(--blue-deep);
  background: rgba(233, 243, 245, 0.72);
  text-align: left;
}

.confirm-receipt > span {
  font-family: var(--font-display);
  font-size: 12px;
  letter-spacing: 0.04em;
}

.confirm-receipt div {
  display: flex;
  flex-direction: column;
  padding-left: 17px;
  border-left: 1px dashed rgba(45, 62, 89, 0.24);
}

.confirm-receipt small { color: var(--ink-soft); font-size: 10px; }
.confirm-receipt strong { font-size: 14px; }
.confirm-receipt strong b { font-family: var(--font-display); font-size: 18px; font-weight: 400; }

.confirm-complete-actions {
  margin-top: 25px;
}

.project-paper-actions {
  display: flex;
  gap: 18px;
  align-items: center;
  flex-wrap: wrap;
}

.exit-slip-button {
  position: relative;
  min-width: 166px;
  padding: 10px 18px 9px 48px;
  border: 1.5px solid var(--blue-deep);
  border-radius: 2px 6px 3px 5px;
  color: var(--ink);
  background:
    linear-gradient(90deg, var(--powder) 0 34px, transparent 34px),
    var(--cream);
  box-shadow: 3px 4px 0 rgba(199, 217, 243, 0.7);
  font-size: 13px;
  font-weight: 750;
  text-align: left;
  cursor: pointer;
  transform: rotate(-0.7deg);
}

.exit-slip-button::before,
.exit-slip-button::after {
  position: absolute;
  top: 50%;
  width: 12px;
  height: 12px;
  border: 1.5px solid var(--blue-deep);
  border-radius: 50%;
  background: var(--cream);
  content: "";
  transform: translateY(-50%);
}

.exit-slip-button::before { left: -7px; }
.exit-slip-button::after { right: -7px; }

.exit-slip-button > span {
  position: absolute;
  top: 5px;
  left: 8px;
  width: 24px;
  color: var(--blue-deep);
  font-family: var(--font-display);
  font-size: 9px;
  font-weight: 400;
  line-height: 1.1;
  letter-spacing: 0.04em;
  writing-mode: vertical-rl;
}

.exit-slip-button:hover {
  transform: translateY(-2px) rotate(-0.7deg);
}

.exit-flow-modal {
  position: fixed;
  z-index: 1250;
  inset: 0;
  display: grid;
  padding: 24px;
  place-items: center;
  visibility: hidden;
  opacity: 0;
  transition: opacity 0.25s ease, visibility 0.25s ease;
}

.exit-flow-modal.is-open {
  visibility: visible;
  opacity: 1;
}

.exit-flow-backdrop {
  position: absolute;
  inset: 0;
  border: 0;
  background: rgba(45, 62, 89, 0.54);
  backdrop-filter: blur(7px);
  cursor: pointer;
}

.exit-flow-dialog {
  position: relative;
  width: min(1110px, 96vw);
  max-height: min(900px, 94dvh);
  padding: 40px 44px 0;
  overflow: auto;
  border: 1.5px solid rgba(45, 62, 89, 0.42);
  border-radius: 5px 12px 6px 10px;
  background:
    radial-gradient(circle, rgba(134, 155, 192, 0.14) 1px, transparent 1.7px) 0 0 / 21px 21px,
    var(--cream);
  box-shadow: 13px 16px 0 rgba(45, 62, 89, 0.16);
  transform: translateY(22px) rotate(-0.15deg);
  transition: transform 0.28s ease;
}

.exit-flow-modal.is-open .exit-flow-dialog {
  transform: translateY(0) rotate(-0.15deg);
}

.exit-dialog-tape {
  position: absolute;
  top: -1px;
  left: 50%;
  width: 94px;
  height: 24px;
  background: rgba(245, 230, 168, 0.8);
  transform: translateX(-50%) rotate(1.5deg);
}

.exit-flow-close {
  position: absolute;
  z-index: 3;
  top: 22px;
  right: 24px;
  width: 42px;
  height: 42px;
  border: 1px dashed var(--blue-deep);
  border-radius: 50%;
  background: rgba(254, 253, 248, 0.88);
  cursor: pointer;
}

.exit-flow-close span {
  position: absolute;
  top: 19px;
  left: 10px;
  width: 20px;
  height: 1.5px;
  background: var(--ink);
}

.exit-flow-close span:first-child { transform: rotate(45deg); }
.exit-flow-close span:last-child { transform: rotate(-45deg); }

.exit-flow-heading {
  padding: 12px 60px 24px 0;
  border-bottom: 1px dashed rgba(45, 62, 89, 0.24);
}

.exit-flow-kicker,
.exit-paper-label {
  width: fit-content;
  margin: 0;
  padding: 3px 8px;
  color: var(--blue-deep);
  background: rgba(245, 230, 168, 0.72);
  font-family: var(--font-display);
  font-size: 12px;
  font-weight: 400;
  letter-spacing: 0.05em;
  transform: rotate(-1deg);
}

.exit-flow-heading h2 {
  margin: 11px 0 6px;
  font-size: clamp(28px, 4vw, 45px);
  line-height: 1.2;
  letter-spacing: -0.035em;
}

.exit-flow-heading > p:last-child {
  margin: 0;
  color: var(--ink-soft);
  font-size: 13px;
}

.exit-progress {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  margin: 22px 0 28px;
  padding: 0;
  list-style: none;
}

.exit-progress li {
  position: relative;
  display: flex;
  gap: 9px;
  align-items: center;
  padding: 0 18px 13px 0;
  border-bottom: 2px dashed rgba(45, 62, 89, 0.18);
  color: #8b96a3;
  font-size: 12px;
}

.exit-progress li::after {
  position: absolute;
  right: -1px;
  bottom: -6px;
  width: 10px;
  height: 10px;
  border: 1px solid var(--blue-deep);
  border-radius: 50%;
  background: var(--cream);
  content: "";
}

.exit-progress li:last-child::after { display: none; }

.exit-progress b {
  color: inherit;
  font-family: var(--font-display);
  font-size: 15px;
  font-weight: 400;
}

.exit-progress li.is-current,
.exit-progress li.is-done {
  border-color: var(--blue-deep);
  color: var(--ink);
}

.exit-progress li.is-done::after { background: var(--mint); }

.exit-flow-panel[hidden] {
  display: none;
}

.exit-member-layout {
  display: grid;
  grid-template-columns: minmax(0, 1.25fr) minmax(270px, 0.75fr);
  gap: 28px;
}

.exit-member-paper,
.torn-task-paper {
  position: relative;
  min-width: 0;
  padding: 27px 29px 30px;
  border: 1.5px solid rgba(45, 62, 89, 0.34);
  background:
    linear-gradient(rgba(45, 62, 89, 0.055) 1px, transparent 1px) 0 46px / 100% 31px,
    var(--cream);
  box-shadow: 6px 7px 0 rgba(199, 217, 243, 0.42);
}

.exit-member-paper::after,
.torn-task-paper::after {
  position: absolute;
  right: -1px;
  bottom: -1px;
  width: 64px;
  height: 34px;
  background: linear-gradient(138deg, transparent 49%, var(--paper-blue) 50%);
  clip-path: polygon(100% 0, 100% 100%, 0 100%, 18% 78%, 8% 60%, 25% 42%, 14% 24%, 34% 0);
  content: "";
}

.exit-member-paper h3,
.torn-task-paper h3 {
  margin: 14px 0 4px;
  font-size: 24px;
}

.exit-member-paper > p,
.torn-task-paper > p {
  margin: 0 0 20px;
  color: var(--ink-soft);
  font-size: 12px;
}

.exit-member-options {
  display: grid;
  gap: 10px;
}

.exit-member-option {
  position: relative;
  display: grid;
  grid-template-columns: 38px minmax(0, 1fr) auto;
  gap: 12px;
  align-items: center;
  min-height: 60px;
  padding: 9px 13px;
  border: 1px dashed rgba(45, 62, 89, 0.3);
  border-radius: 3px 7px 4px 6px;
  color: var(--ink-soft);
  background: rgba(247, 244, 234, 0.72);
  text-align: left;
  cursor: pointer;
}

.exit-member-option > i {
  display: grid;
  width: 34px;
  height: 34px;
  place-items: center;
  border: 1px solid var(--blue-deep);
  border-radius: 48% 52% 44% 56%;
  background: var(--paper-blue);
  font-family: var(--font-display);
  font-style: normal;
  font-size: 13px;
}

.exit-member-option > span {
  display: flex;
  min-width: 0;
  flex-direction: column;
}

.exit-member-option strong { font-size: 14px; }
.exit-member-option small { color: var(--ink-soft); font-size: 10px; }

.exit-member-option > em {
  width: 16px;
  height: 16px;
  border: 1.5px solid var(--blue-deep);
  border-radius: 50%;
  font-style: normal;
}

.exit-member-option.is-selected {
  border-style: solid;
  border-color: var(--blue-deep);
  color: var(--ink);
  background: var(--paper-blue);
  box-shadow: 3px 4px 0 rgba(199, 217, 243, 0.64);
}

.exit-member-option.is-selected > em {
  box-shadow: inset 0 0 0 4px var(--cream);
  background: var(--blue-deep);
}

.exit-deposit-pocket {
  position: relative;
  display: flex;
  min-height: 334px;
  flex-direction: column;
  align-items: center;
  padding: 24px 22px 10px;
  overflow: hidden;
  border: 1.5px solid var(--ink);
  border-radius: 6px 6px 28px 28px;
  background:
    radial-gradient(circle, rgba(254, 253, 248, 0.86) 2.2px, transparent 2.8px) 0 0 / 21px 21px,
    var(--powder);
}

.exit-deposit-pocket > span,
.exit-fund-envelope > span {
  padding: 3px 9px;
  border: 1px dashed var(--ink);
  background: var(--cream);
  font-family: var(--font-display);
  font-size: 11px;
  letter-spacing: 0.06em;
}

.exit-deposit-pocket > strong,
.exit-fund-envelope > strong {
  margin-top: 13px;
  font-family: var(--font-display);
  font-size: 20px;
  font-weight: 400;
}

.exit-deposit-pocket > strong b,
.exit-fund-envelope > strong b {
  font-size: 39px;
  font-weight: 400;
}

.exit-deposit-pocket > p,
.exit-fund-envelope > p {
  max-width: 240px;
  margin: 3px auto 0;
  color: var(--ink-soft);
  font-size: 11px;
  text-align: center;
}

.exit-deposit-pocket svg {
  width: 190px;
  max-width: 100%;
  margin-top: auto;
}

.exit-bird-body,
.exit-bird-head,
.exit-bird-wing,
.exit-bird-tail,
.exit-bird-beak,
.exit-bird-note path,
.exit-money-bag path {
  stroke: var(--ink);
  stroke-width: 4.5;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.exit-bird-body,
.exit-bird-head { fill: var(--cream); }
.exit-bird-wing { fill: #edf3f1; }
.exit-bird-tail { fill: #d7e3eb; }
.exit-bird-beak { fill: var(--butter); }
.exit-bird-eye { fill: var(--ink); }

.exit-bird-leg {
  fill: none;
  stroke: var(--ink);
  stroke-width: 3.5;
  stroke-linecap: round;
}

.exit-bird-note path:first-child { fill: var(--cream); }
.exit-bird-note path:not(:first-child) { fill: none; stroke-width: 2.5; }
.exit-money-bag path:first-child { fill: var(--butter); }
.exit-money-bag path:nth-child(2) { fill: var(--peach); }
.exit-money-bag path:last-child { fill: none; stroke-width: 3; }

.exit-panel-actions {
  display: flex;
  gap: 12px;
  justify-content: flex-end;
  margin: 30px -44px 0;
  padding: 16px 44px 18px;
  border-top: 1px dashed rgba(45, 62, 89, 0.26);
  background: rgba(247, 244, 234, 0.76);
}

.exit-task-layout {
  display: grid;
  grid-template-columns: minmax(0, 1.15fr) 72px minmax(250px, 0.65fr);
  gap: 22px;
  align-items: center;
}

.exit-task-list {
  display: grid;
  gap: 9px;
}

.exit-task-slip {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  gap: 15px;
  align-items: center;
  padding: 11px 13px;
  border-left: 4px solid var(--powder);
  background: rgba(233, 243, 245, 0.66);
  transform: rotate(-0.2deg);
}

.exit-task-slip span {
  display: flex;
  min-width: 0;
  flex-direction: column;
}

.exit-task-slip strong { font-size: 13px; }
.exit-task-slip small { color: var(--ink-soft); font-size: 10px; }

.exit-task-slip b {
  color: var(--blue-deep);
  font-family: var(--font-display);
  font-size: 15px;
  font-weight: 400;
}

.exit-transfer-arrow {
  display: flex;
  flex-direction: column;
  align-items: center;
  color: var(--blue-deep);
  font-family: var(--font-display);
  font-size: 11px;
  text-align: center;
  transform: rotate(-3deg);
}

.exit-transfer-arrow i {
  font-family: var(--font-sans);
  font-size: 42px;
  font-style: normal;
  line-height: 1;
}

.exit-fund-envelope {
  position: relative;
  min-height: 255px;
  padding: 34px 23px 25px;
  border: 1.5px solid var(--ink);
  background:
    linear-gradient(32deg, transparent 49%, rgba(45, 62, 89, 0.15) 50%, transparent 51%),
    linear-gradient(-32deg, transparent 49%, rgba(45, 62, 89, 0.15) 50%, transparent 51%),
    radial-gradient(circle, rgba(254, 253, 248, 0.88) 2.2px, transparent 2.8px) 0 0 / 21px 21px,
    var(--powder);
  text-align: center;
  clip-path: polygon(0 0, 100% 0, 100% 93%, 96% 100%, 90% 94%, 84% 100%, 77% 94%, 70% 100%, 63% 94%, 56% 100%, 49% 94%, 42% 100%, 35% 94%, 28% 100%, 21% 94%, 14% 100%, 7% 94%, 0 100%);
}

.exit-allocation-stamp {
  position: absolute;
  right: 15px;
  bottom: 22px;
  display: grid;
  width: 71px;
  height: 61px;
  place-items: center;
  border: 2px solid #c47f75;
  border-radius: 48% 52% 45% 55%;
  color: #ad675f;
  background: #fff7ef;
  box-shadow: inset 0 0 0 4px #fff7ef, inset 0 0 0 5px rgba(196, 127, 117, 0.55);
  font-family: var(--font-display);
  font-size: 10px;
  line-height: 1;
  transform: rotate(7deg);
}

.exit-allocation-stamp b { font-size: 15px; font-weight: 400; }

.exit-confirm-rule {
  display: grid;
  grid-template-columns: 19px 1fr;
  gap: 10px;
  align-items: center;
  margin-top: 23px;
  padding: 12px 14px;
  border-left: 4px solid var(--butter);
  color: var(--ink-soft);
  background: rgba(245, 230, 168, 0.2);
  font-size: 11px;
  cursor: pointer;
}

.exit-confirm-rule input {
  position: absolute;
  opacity: 0;
}

.exit-confirm-rule > span {
  position: relative;
  width: 18px;
  height: 18px;
  border: 1.5px solid var(--ink);
  background: var(--cream);
}

.exit-confirm-rule input:checked + span {
  background: var(--sky);
}

.exit-confirm-rule input:checked + span::after {
  position: absolute;
  top: 1px;
  left: 5px;
  width: 5px;
  height: 10px;
  border-right: 2px solid var(--ink);
  border-bottom: 2px solid var(--ink);
  content: "";
  transform: rotate(40deg);
}

.exit-complete-panel {
  padding-top: 3px;
  text-align: center;
}

.exit-receipt-illustration {
  position: relative;
  width: 310px;
  max-width: 92%;
  margin: 0 auto;
  padding: 5px 20px 0;
  background:
    radial-gradient(circle, rgba(254, 253, 248, 0.9) 2.2px, transparent 2.8px) 0 0 / 22px 22px,
    var(--powder);
  clip-path: polygon(0 0, 100% 0, 100% 91%, 94% 100%, 88% 92%, 82% 100%, 75% 92%, 68% 100%, 61% 92%, 54% 100%, 47% 92%, 40% 100%, 33% 92%, 26% 100%, 19% 92%, 12% 100%, 6% 92%, 0 100%);
}

.exit-receipt-illustration svg { width: 100%; }

.exit-done-stamp {
  position: absolute;
  right: 13px;
  bottom: 30px;
  display: grid;
  width: 82px;
  height: 68px;
  place-items: center;
  border: 2px solid #c47f75;
  border-radius: 46% 54% 49% 51%;
  color: #ad675f;
  background: #fff7ef;
  box-shadow: inset 0 0 0 4px #fff7ef, inset 0 0 0 5px rgba(196, 127, 117, 0.55);
  font-family: var(--font-display);
  font-size: 15px;
  line-height: 1;
  transform: rotate(8deg);
}

.exit-done-stamp b { font-size: 16px; font-weight: 400; }

.exit-complete-panel > h3 {
  margin: 22px 0 5px;
  font-size: 32px;
}

.exit-complete-panel > p {
  max-width: 620px;
  margin: 0 auto;
  color: var(--ink-soft);
  font-size: 13px;
}

.exit-receipt {
  display: grid;
  grid-template-columns: 1.15fr repeat(3, 1fr);
  gap: 18px;
  align-items: center;
  max-width: 860px;
  margin: 25px auto 0;
  padding: 15px 18px;
  border: 1px dashed var(--blue-deep);
  background: rgba(233, 243, 245, 0.72);
  text-align: left;
}

.exit-receipt > span {
  font-family: var(--font-display);
  font-size: 12px;
  letter-spacing: 0.05em;
}

.exit-receipt div {
  display: flex;
  flex-direction: column;
  border-left: 1px dashed rgba(45, 62, 89, 0.24);
  padding-left: 18px;
}

.exit-receipt small { color: var(--ink-soft); font-size: 10px; }
.exit-receipt strong { font-size: 14px; }
.exit-receipt strong b { font-family: var(--font-display); font-size: 18px; font-weight: 400; }

.exit-complete-actions {
  margin-top: 25px;
}

@media (max-width: 900px) {
  .promise-invite-layout,
  .member-signing-layout,
  .exit-member-layout,
  .exit-task-layout {
    grid-template-columns: 1fr;
  }

  .promise-invite-art {
    min-height: 315px;
  }

  .current-signer-slip {
    min-height: 330px;
  }

  .exit-deposit-pocket {
    min-height: 270px;
  }

  .exit-deposit-pocket svg {
    width: 165px;
  }

  .exit-transfer-arrow {
    flex-direction: row;
    gap: 10px;
    justify-content: center;
  }

  .exit-transfer-arrow i {
    transform: rotate(90deg);
  }

  .exit-fund-envelope {
    min-height: 220px;
  }

  .confirm-receipt,
  .exit-receipt {
    grid-template-columns: repeat(3, 1fr);
  }

  .confirm-receipt > span,
  .exit-receipt > span {
    grid-column: 1 / -1;
  }
}

@media (max-width: 620px) {
  .project-paper-actions {
    align-items: flex-start;
    flex-direction: column;
  }

  .exit-flow-modal {
    padding: 0;
  }

  .exit-flow-dialog {
    width: 100%;
    max-height: 100dvh;
    min-height: 100dvh;
    padding: 30px 16px 0;
    border: 0;
    border-radius: 0;
    box-shadow: none;
  }

  .exit-flow-close {
    top: 16px;
    right: 15px;
  }

  .exit-flow-heading {
    padding: 16px 48px 20px 0;
  }

  .exit-flow-heading h2 {
    font-size: 29px;
  }

  .promise-invite-paper,
  .member-signing-paper {
    padding: 22px 16px 27px;
  }

  .confirm-promise-meta {
    grid-template-columns: 1fr;
  }

  .confirm-promise-meta div,
  .confirm-promise-meta div:first-child {
    border-top: 1px dashed rgba(45, 62, 89, 0.22);
    border-left: 0;
    padding: 8px 3px;
  }

  .confirm-promise-meta div:first-child {
    border-top: 0;
  }

  .promise-invite-art {
    min-height: 285px;
  }

  .confirm-member-list {
    grid-template-columns: 1fr;
  }

  .current-signer-slip {
    min-height: 320px;
  }

  .exit-progress {
    margin: 18px 0 22px;
  }

  .exit-progress li {
    gap: 3px;
    flex-direction: column;
    align-items: flex-start;
    padding-right: 6px;
    font-size: 10px;
  }

  .exit-progress b { font-size: 13px; }

  .exit-member-paper,
  .torn-task-paper {
    padding: 21px 16px 25px;
  }

  .exit-member-paper h3,
  .torn-task-paper h3 {
    font-size: 21px;
  }

  .exit-deposit-pocket {
    min-height: 255px;
  }

  .exit-panel-actions {
    margin-right: -16px;
    margin-left: -16px;
    padding: 14px 16px calc(14px + env(safe-area-inset-bottom));
  }

  .exit-panel-actions button {
    min-width: 0;
    flex: 1;
    padding-right: 12px;
    padding-left: 12px;
    font-size: 11px;
  }

  .confirm-receipt,
  .exit-receipt {
    grid-template-columns: 1fr;
    gap: 8px;
    padding: 15px;
  }

  .confirm-receipt > span,
  .exit-receipt > span {
    grid-column: auto;
  }

  .confirm-receipt div,
  .exit-receipt div {
    border-top: 1px dashed rgba(45, 62, 89, 0.22);
    border-left: 0;
    padding: 7px 0 0;
  }

  .confirm-complete-panel > h3,
  .exit-complete-panel > h3 {
    font-size: 27px;
  }
}

@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    scroll-behavior: auto !important;
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}

    </style>
  </head>
  <body>
    <svg class="svg-defs" aria-hidden="true">
      <defs>
        <filter id="rough-line" x="-10%" y="-10%" width="120%" height="120%">
          <feTurbulence baseFrequency="0.012" numOctaves="2" seed="8" result="noise" />
          <feDisplacementMap in="SourceGraphic" in2="noise" scale="1.25" />
        </filter>

        <symbol id="flying-pigeon-up" viewBox="0 0 180 112">
          <path class="flight-silhouette" d="M11 70 34 64 22 51l29 8c3-22 9-43 23-55 4 19 16 36 34 49 6-14 18-23 32-22 14 1 23 9 25 20l14 6-16 7c-4 12-16 19-29 17-15 15-42 21-68 13-15-4-25-12-30-21L16 87l10-15Z" />
        </symbol>

        <symbol id="flying-pigeon-down" viewBox="0 0 180 112">
          <path class="flight-silhouette" d="M11 60 36 54 23 42l29 8c16-11 39-13 59-5 7-14 19-22 33-20 14 1 22 9 24 20l12 6-15 7c-4 12-16 19-29 17-8 8-18 14-30 17-18 9-38 16-59 20 3-19 13-35 28-47-12 1-24-2-34-8L16 78l11-15Z" />
        </symbol>

        <symbol id="mini-pigeon" viewBox="0 0 76 62">
          <ellipse class="symbol-shape" cx="34" cy="36" rx="24" ry="18" />
          <circle class="symbol-shape" cx="49" cy="22" r="14" />
          <path class="symbol-beak" d="m59 23 13 5-12 6" />
          <path class="symbol-wing" d="M30 30c-9-2-16 3-18 12 9 1 16-1 22-7" />
          <circle class="pigeon-eye" cx="52" cy="19" r="2" />
          <path class="symbol-line" d="M29 52v7m15-7 3 7" />
        </symbol>
      </defs>
    </svg>

    <section class="intro" id="intro" aria-label="品牌开场动画">
      <button class="skip-intro" id="skipIntro" type="button">跳过</button>
      <div class="intro-dots" aria-hidden="true"></div>
      <div class="flock" id="flock" aria-hidden="true"></div>

      <div class="intro-brand" id="introBrand">
        <div class="intro-mark" aria-hidden="true">
          <svg viewBox="0 0 230 168">
            <path class="logo-body" fill="#fefdf8" d="M50 86c0-37 30-64 68-64 34 0 61 19 70 48 4 14 2 34-8 49-16 24-52 33-84 24-29-8-46-27-46-57Z" />
            <path class="logo-wing" fill="#edf3f1" d="M72 91c15-24 41-35 69-28-4 29-25 51-56 57" />
            <circle class="logo-eye" cx="154" cy="53" r="4" />
            <path class="logo-beak" d="m177 61 30 12-28 13" />
            <path class="logo-leg" d="M102 139v15m29-14 5 14" />
            <g class="logo-letter">
              <path d="M178 78 216 91l-11 31-38-13Z" />
              <path d="m178 79 15 19 23-7" />
            </g>
          </svg>
        </div>
        <p class="brand-cn">不要鸽我</p>
        <h1>DON'T GHOST ME</h1>
        <p class="intro-copy">让退出的保证金，变成奖励救场者的悬赏。</p>
        <button class="enter-button" id="enterButton" type="button">进入工作台</button>
      </div>
    </section>

    <div class="app" id="app" aria-hidden="true">
      <header class="topbar">
        <a class="brand" href="#home" aria-label="不要鸽我首页">
          <span class="brand-icon" aria-hidden="true">
            <svg viewBox="0 0 76 62"><use href="#mini-pigeon" /></svg>
          </span>
          <span class="brand-words">
            <strong>不要鸽我</strong>
            <small>DON'T GHOST ME</small>
          </span>
        </a>

        <nav class="desktop-nav" aria-label="主要导航">
          <button class="nav-link is-active" data-target="home" type="button">首页</button>
          <button class="nav-link" data-target="commitment" type="button">我的承诺</button>
          <button class="nav-link" data-target="rescue" type="button">救场大厅</button>
          <button class="nav-link" data-target="mechanism" type="button">运行机制</button>
        </nav>

        <button class="wallet-button" id="walletButton" type="button">
          <span class="wallet-dot" aria-hidden="true"></span>
          测试钱包 · Caro
        </button>
      </header>

      <main>
        <section class="hero paper-surface" id="home">
          <div class="tape tape-top" aria-hidden="true"></div>
          <div class="hero-copy">
            <p class="eyebrow"><span>Promise Delivery</span> · 01</p>
            <h2>你可以鸽，<br />但你的钱会请别人来救场。</h2>
            <p class="hero-description">
              为黑客松、朋友旅行和多人计划建立清晰的承诺。有人临时退出时，保证金自动进入救场悬赏池，奖励真正补上缺口的人。
            </p>
            <div class="hero-actions">
              <button class="primary-button" id="promiseButton" type="button">
                <svg viewBox="0 0 52 44" aria-hidden="true">
                  <path d="M5 9h42v28H5zM6 10l20 16 20-16" />
                </svg>
                创建一份承诺
              </button>
              <button class="rescue-stamp" id="rescueButton" type="button" aria-label="前往救场大厅">
                <span class="stamp-art" aria-hidden="true">
                  <svg viewBox="0 0 66 52">
                    <ellipse class="stamp-bird-body" cx="29" cy="31" rx="18" ry="13" />
                    <circle class="stamp-bird-head" cx="42" cy="20" r="11" />
                    <path class="stamp-bird-wing" d="M15 31c5-9 14-12 23-7-3 9-10 14-20 13" />
                    <path class="stamp-bird-beak" d="m51 20 10 4-9 5" />
                    <circle class="stamp-bird-eye" cx="45" cy="17" r="1.7" />
                    <path class="stamp-note" d="m45 28 15 4-4 12-15-5Zm0 0 6 7 9-3" />
                    <path class="stamp-motion" d="M7 18h8M5 23h6" />
                  </svg>
                </span>
                <span class="stamp-copy">
                  <strong>去救场</strong>
                  <b>RESCUE!</b>
                </span>
                <span class="stamp-postmark" aria-hidden="true"><i></i></span>
              </button>
            </div>
            <p class="micro-note">测试网络 · 模拟保证金 · 不涉及真实资金</p>
          </div>

          <div class="hero-art" aria-label="叼着承诺信的小白鸽插画">
            <div class="polka-card" aria-hidden="true"></div>
            <div class="mail-stamp" aria-hidden="true">PROMISE<br />DELIVERY</div>
            <svg class="pigeon-letter" viewBox="0 0 420 330" role="img" aria-label="叼着信的小白鸽">
              <path class="ground-line" d="M69 280c83 14 190 14 285 0" />
              <ellipse class="bird-body" cx="204" cy="192" rx="101" ry="87" />
              <circle class="bird-head" cx="283" cy="109" r="64" />
              <path class="bird-wing" d="M124 190c23-54 78-75 133-43-13 63-59 103-118 91" />
              <path class="bird-tail" d="m116 213-66 31 75 10" />
              <path class="bird-beak" d="m333 112 65 24-62 28" />
              <circle class="bird-eye" cx="301" cy="92" r="7" />
              <path class="bird-leg" d="M176 266v30m70-31 8 31" />
              <path class="bird-foot" d="m158 298 19-3 17 5m43 0 17-5 20 3" />
              <g class="letter">
                <path d="m331 151 73 22-20 58-73-23Z" />
                <path d="m332 152 28 36 44-15" />
                <path d="m311 207 47-19 26 42" />
              </g>
              <path class="motion-line" d="M65 144c-25-13-36-8-48-2m56 27c-30-2-44 6-54 15" />
            </svg>
            <p class="art-caption">承诺正在派送中…</p>
          </div>
        </section>

        <section class="mechanism-strip" id="mechanism" aria-label="核心流程">
          <div class="strip-title">
            <span>HOW IT WORKS</span>
            <strong>承诺发生意外以后</strong>
          </div>
          <ol class="steps">
            <li><b>01</b><span>成员确认承诺</span></li>
            <li><b>02</b><span>保证金被锁定</span></li>
            <li><b>03</b><span>退出后进入救场池</span></li>
            <li><b>04</b><span>救场者完成并领奖励</span></li>
          </ol>
        </section>

        <section class="workspace-section" id="commitment">
          <div class="demo-scene-switcher" aria-label="切换演示场景">
            <div class="demo-scene-title">
              <span>SCENE DEMO</span>
              <strong>看看同一套机制如何处理不同的“鸽”</strong>
            </div>
            <div class="demo-scene-options" role="tablist" aria-label="演示场景">
              <button class="demo-scene-button is-active" type="button" role="tab" aria-selected="true" data-demo-scene="hackathon">
                <svg viewBox="0 0 58 46" aria-hidden="true"><path d="M8 9h42v29H8z" /><path d="M16 18h12m-12 8h27m-27 7h19M37 13l8 7-8 7" /></svg>
                <span><b>黑客松</b><small>开发任务补位</small></span>
              </button>
              <button class="demo-scene-button" type="button" role="tab" aria-selected="false" data-demo-scene="travel">
                <svg viewBox="0 0 58 46" aria-hidden="true"><path d="M12 13h34v27H12z" /><path d="M21 13V8h16v5M17 23h24M20 31h5m9 0h5M8 18h5v16H8zm37 0h5v16h-5z" /></svg>
                <span><b>朋友旅行</b><small>行程费用补救</small></span>
              </button>
            </div>
          </div>

          <div class="section-heading">
            <div>
              <p class="eyebrow" id="workspaceEyebrow">MY PROMISE · 001</p>
              <h2>正在进行的承诺</h2>
            </div>
            <p id="workspaceDescription">一张承诺卡，记下成员、任务与每一笔保证金的去向。</p>
          </div>

          <div class="workbench">
            <div class="project-paper">
              <span class="paper-clip" aria-hidden="true"></span>
              <div class="paper-header">
                <div>
                  <p class="tiny-label" id="projectSceneLabel">黑客松项目</p>
                  <h3 id="projectCardTitle">Monad 黑客松作品开发</h3>
                </div>
                <span class="status-stamp"><small>RESCUE!</small><strong>救场中</strong></span>
              </div>

              <p class="project-summary" id="projectSummary">成员共同完成产品、前端和合约 Demo。当前有一名成员退出，保证金已转入救场池。</p>

              <div class="promise-meta">
                <div><span>承诺成员</span><strong id="projectMemberCount">5 人</strong></div>
                <div><span>个人保证金</span><strong id="projectDeposit">100 MON</strong></div>
                <div><span>当前阶段</span><strong id="projectStage">Demo 开发</strong></div>
              </div>

              <div class="timeline" aria-label="项目状态">
                <button class="timeline-item timeline-confirm-button is-done" id="openConfirmFlow" type="button" aria-label="查看成员确认承诺流程"><i></i><span id="timelineOne">承诺已确认</span><small>查看确认过程</small></button>
                <div class="timeline-item is-alert"><i></i><span id="timelineTwo">1 人退出</span></div>
                <div class="timeline-item is-current"><i></i><span id="timelineThree">救场悬赏已发布</span></div>
                <div class="timeline-item"><i></i><span id="timelineFour">等待验收与结算</span></div>
              </div>

              <div class="project-paper-actions">
                <button class="exit-slip-button" id="openExitFlow" type="button">
                  <span>EXIT NOTE</span>
                  演示成员退出
                </button>
                <button class="text-button" data-scroll="rescue" type="button">查看救场任务 <span>→</span></button>
              </div>
            </div>

            <aside class="fund-pocket">
              <div class="pocket-label">RESCUE FUND</div>
              <div class="money-amount"><strong id="fundAmount">100</strong><span>MON</span></div>
              <p id="fundDescription">退出成员的保证金<br />已进入救场池</p>

              <svg class="pigeon-money" viewBox="0 0 250 210" role="img" aria-label="抱着钱袋的小白鸽">
                <ellipse class="bird-body" cx="109" cy="116" rx="68" ry="60" />
                <circle class="bird-head" cx="154" cy="63" r="41" />
                <path class="bird-wing" d="M59 119c20-39 59-48 92-20-14 44-48 62-86 48" />
                <path class="bird-beak" d="m190 67 39 15-38 17" />
                <circle class="bird-eye" cx="165" cy="52" r="4.5" />
                <path class="bird-leg" d="M84 169v20m51-20 5 20" />
                <g class="money-bag">
                  <path d="M131 111c-18 12-26 28-22 48 4 23 24 37 49 34 24-3 38-20 35-43-2-17-14-31-30-40Z" />
                  <path d="m134 110 26 1 7-19-35 1Z" />
                  <path d="M147 126c7-5 18-2 19 6 1 11-18 9-16 19 1 7 11 9 18 4m-10-33-1 39" />
                </g>
              </svg>
              <div class="verified-stamp">资金去向<br />可查看</div>
            </aside>
          </div>
        </section>

        <section class="rescue-section" id="rescue">
          <div class="section-heading">
            <div>
              <p class="eyebrow" id="rescueBoardEyebrow">RESCUE BOARD · HACKATHON</p>
              <h2>等待领取的救场票</h2>
            </div>
            <p id="rescueBoardDescription">选择一张任务票，补上团队留下的开发缺口。</p>
          </div>

          <div class="ticket-list" id="ticketList">
            <button class="rescue-ticket" type="button" data-task="紧急完成智能合约 MVP" data-reward="80" data-category="开发 · 黑客松" data-deadline="08 月 08 日 · 22:00" data-proof="GitHub 仓库与部署说明">
              <span class="ticket-number">NO. 01</span>
              <span class="ticket-copy">
                <small>开发 · 黑客松</small>
                <strong>紧急完成智能合约 MVP</strong>
                <em>提交代码仓库，等待团队验收</em>
              </span>
              <span class="ticket-reward"><b>80</b> MON</span>
              <span class="ticket-status">待领取</span>
            </button>

            <button class="rescue-ticket" type="button" data-task="整理代码交接与部署说明" data-reward="20" data-category="文档 · 黑客松" data-deadline="08 月 09 日 · 12:00" data-proof="交接文档与运行截图">
              <span class="ticket-number">NO. 02</span>
              <span class="ticket-copy">
                <small>文档 · 黑客松</small>
                <strong>整理代码交接与部署说明</strong>
                <em>补充必要文件与运行步骤</em>
              </span>
              <span class="ticket-reward"><b>20</b> MON</span>
              <span class="ticket-status">待领取</span>
            </button>
          </div>
        </section>
      </main>

      <footer>
        <div class="footer-pigeon" aria-hidden="true">
          <svg viewBox="0 0 76 62"><use href="#mini-pigeon" /></svg>
        </div>
        <p>不要鸽我 · 让承诺有人完成</p>
        <small>Don't Ghost Me · Mini Demo</small>
      </footer>

      <nav class="mobile-nav" aria-label="手机端主要导航">
        <button class="mobile-nav-item is-active" data-target="home" type="button">
          <svg viewBox="0 0 76 62"><use href="#mini-pigeon" /></svg><span>首页</span>
        </button>
        <button class="mobile-nav-item" data-target="commitment" type="button">
          <svg viewBox="0 0 52 44"><path d="M5 9h42v28H5zM6 10l20 16 20-16" /></svg><span>承诺</span>
        </button>
        <button class="mobile-nav-item" data-target="rescue" type="button">
          <svg viewBox="0 0 56 44"><path d="M5 7h46v30H5c3-5 3-10 0-15 3-5 3-10 0-15Z" /><path d="M22 15h17M18 23h21M22 31h12" /></svg><span>救场</span>
        </button>
        <button class="mobile-nav-item" data-target="mechanism" type="button">
          <svg viewBox="0 0 52 48"><path d="M10 7h32v34H10zM17 16h18M17 24h18M17 32h12" /></svg><span>机制</span>
        </button>
      </nav>

      <div class="toast" id="toast" role="status" aria-live="polite"></div>
    </div>

    <div class="promise-modal" id="promiseModal" aria-hidden="true">
      <button class="promise-backdrop" id="promiseBackdrop" type="button" aria-label="关闭创建承诺窗口"></button>

      <section class="promise-dialog" role="dialog" aria-modal="true" aria-labelledby="promiseModalTitle">
        <span class="promise-dialog-tape" aria-hidden="true"></span>
        <button class="promise-modal-close" id="closePromiseModal" type="button" aria-label="关闭">
          <span></span><span></span>
        </button>

        <div class="promise-editor" id="promiseEditor">
          <header class="promise-modal-heading">
            <p class="modal-kicker">CREATE PROMISE · 001</p>
            <h2 id="promiseModalTitle">写下一份不被鸽掉的承诺</h2>
            <p>先把成员、任务和保证金说清楚。这里是交互演示，不会连接真实钱包。</p>
          </header>

          <div class="promise-modal-layout">
            <form class="promise-form" id="promiseForm">
              <fieldset class="scene-switcher">
                <legend>01 · 选择场景</legend>
                <div class="scene-options">
                  <button class="scene-option is-selected" type="button" data-scene="hackathon">
                    <svg viewBox="0 0 84 66" aria-hidden="true">
                      <path class="scene-paper" d="M15 13h54v38H15z" />
                      <path class="scene-line" d="M24 24h13m-13 9h30m-30 9h22M51 17l10 9-10 9" />
                      <path class="scene-tape" d="m33 8 22 2-2 8-22-2Z" />
                    </svg>
                    <span><strong>黑客松团队</strong><small>开发任务需要补位</small></span>
                  </button>

                  <button class="scene-option" type="button" data-scene="travel">
                    <svg viewBox="0 0 84 66" aria-hidden="true">
                      <path class="scene-paper" d="M20 18h44v35H20z" />
                      <path class="scene-line" d="M33 18v-6h18v6M27 30h30M31 39h6m10 0h6" />
                      <path class="scene-tape" d="M16 23h8v20h-8zm44 0h8v20h-8z" />
                    </svg>
                    <span><strong>朋友旅行</strong><small>行程和费用需要接手</small></span>
                  </button>

                  <button class="scene-option" type="button" data-scene="custom">
                    <svg viewBox="0 0 84 66" aria-hidden="true">
                      <path class="scene-paper" d="M16 12h48v43H16z" />
                      <path class="scene-line" d="M26 26h23m-23 9h17m-17 9h12M53 39l15-15 6 6-15 15-10 3Z" />
                      <path class="scene-tape" d="m30 8 24 2-2 8-24-2Z" />
                    </svg>
                    <span><strong>自由自定义</strong><small>写下任何多人计划</small></span>
                  </button>
                </div>

                <div class="custom-scene-field" id="customSceneField" hidden>
                  <label class="promise-field">
                    <span>自定义场景名称</span>
                    <input id="customSceneName" type="text" value="我的多人计划" maxlength="16" autocomplete="off" />
                  </label>
                  <small>例如：课程作业、游戏组队、生日派对</small>
                </div>
              </fieldset>

              <div class="promise-field-block">
                <p class="form-number">02 · 承诺内容</p>
                <label class="promise-field promise-field-wide">
                  <span>项目名称</span>
                  <input id="projectName" type="text" value="Monad 黑客松作品开发" autocomplete="off" />
                </label>

                <div class="promise-field-row">
                  <label class="promise-field">
                    <span>完成日期</span>
                    <input id="promiseDeadline" type="date" value="2026-08-09" />
                  </label>
                  <label class="promise-field">
                    <span>每人保证金</span>
                    <span class="amount-input"><input id="promiseDeposit" type="number" min="0" value="100" /><b>MON</b></span>
                  </label>
                </div>
              </div>

              <section class="member-section">
                <div class="member-section-heading">
                  <div>
                    <p class="form-number">03 · 成员与任务</p>
                    <span>每个人都要有一张自己的任务纸条</span>
                  </div>
                  <button class="add-member-button" id="addMemberButton" type="button">＋ 添加成员</button>
                </div>

                <div class="member-list" id="memberList">
                  <div class="member-row">
                    <span class="member-tag">01</span>
                    <label><span>成员</span><input class="member-name" type="text" value="Caro" /></label>
                    <label><span>负责的承诺</span><input class="member-task" type="text" value="产品与前端 Demo" /></label>
                    <button class="remove-member" type="button" aria-label="移除成员">×</button>
                  </div>
                  <div class="member-row">
                    <span class="member-tag">02</span>
                    <label><span>成员</span><input class="member-name" type="text" value="Lin" /></label>
                    <label><span>负责的承诺</span><input class="member-task" type="text" value="智能合约与测试" /></label>
                    <button class="remove-member" type="button" aria-label="移除成员">×</button>
                  </div>
                </div>
              </section>

              <label class="promise-rule">
                <input id="promiseRule" type="checkbox" checked />
                <span class="checkmark" aria-hidden="true"></span>
                <span>我同意：成员退出后，保证金会变成救场悬赏，奖励真正补上任务的人。</span>
              </label>
            </form>

            <aside class="promise-preview" aria-label="承诺卡预览">
              <span class="preview-pin" aria-hidden="true"></span>
              <p class="preview-label">PROMISE PREVIEW</p>
              <svg class="preview-pigeon" viewBox="0 0 250 190" role="img" aria-label="抱着承诺书的小白鸽">
                <path class="preview-motion" d="M29 96c-15-7-21-4-27 1m30 13c-17 0-24 5-29 11" />
                <ellipse class="preview-bird-body" cx="112" cy="104" rx="65" ry="54" />
                <circle class="preview-bird-head" cx="158" cy="59" r="39" />
                <path class="preview-bird-wing" d="M65 104c15-35 50-47 85-28-8 39-38 63-74 55" />
                <path class="preview-bird-tail" d="m57 122-39 20 45 4" />
                <path class="preview-bird-beak" d="m190 61 34 13-33 15" />
                <circle class="preview-bird-eye" cx="170" cy="50" r="4" />
                <path class="preview-bird-leg" d="M91 153v18m43-18 5 18" />
                <g class="preview-contract">
                  <path d="m142 99 75 9-7 55-75-10Z" />
                  <path d="m151 118 45 5m-47 11 37 4m-39 10 26 3" />
                  <path d="m195 143 7 7 14-17" />
                </g>
              </svg>

              <div class="preview-copy">
                <span id="previewScene">黑客松团队</span>
                <h3 id="previewTitle">Monad 黑客松作品开发</h3>
                <dl>
                  <div><dt>完成日期</dt><dd id="previewDeadline">2026.08.09</dd></div>
                  <div><dt>保证金</dt><dd><b id="previewDeposit">100</b> MON / 人</dd></div>
                  <div><dt>承诺成员</dt><dd id="previewMembers">2 人</dd></div>
                </dl>
              </div>
              <div class="preview-stamp">PROMISE<br /><strong>已写下</strong></div>
              <p class="preview-note">退出不是结束，留下的任务会被送去救场。</p>
            </aside>
          </div>

          <footer class="promise-modal-actions">
            <button class="promise-secondary-action" id="saveDraftButton" type="button">保存草稿</button>
            <button class="promise-primary-action" id="sendPromiseButton" type="button">
              <svg viewBox="0 0 52 44" aria-hidden="true"><path d="M5 9h42v28H5zM6 10l20 16 20-16" /></svg>
              确认并发出承诺
            </button>
          </footer>
        </div>

        <div class="promise-success" id="promiseSuccess" hidden>
          <p class="success-kicker">SENT · DONE!</p>
          <div class="success-illustration" aria-hidden="true">
            <span class="success-rays"></span>
            <svg viewBox="0 0 320 240">
              <ellipse class="success-bird-body" cx="139" cy="133" rx="78" ry="66" />
              <circle class="success-bird-head" cx="195" cy="76" r="47" />
              <path class="success-bird-wing" d="M83 132c19-43 61-57 102-34-10 48-46 77-91 67" />
              <path class="success-bird-tail" d="m73 151-48 24 55 6" />
              <path class="success-bird-beak" d="m233 78 42 16-40 18" />
              <circle class="success-bird-eye" cx="209" cy="65" r="5" />
              <path class="success-bird-leg" d="M114 197v21m51-21 6 21" />
              <g class="success-letter"><path d="m189 116 88 15-12 65-88-16Z" /><path d="m190 117 36 39 51-25" /></g>
            </svg>
            <span class="success-done-stamp">DONE!</span>
          </div>

          <h2>承诺已寄出</h2>
          <p>这份承诺卡已经准备好了。接下来可以交给队友按这个流程实现真实数据和钱包交互。</p>

          <div class="promise-receipt">
            <span>DELIVERY RECEIPT · 001</span>
            <strong id="successProjectName">Monad 黑客松作品开发</strong>
            <div><small>状态</small><b>等待成员确认</b></div>
            <div><small>资金</small><b>仅为界面演示</b></div>
          </div>

          <div class="success-actions">
            <button class="promise-secondary-action" id="makeAnotherPromise" type="button">再写一份</button>
            <button class="promise-primary-action" id="viewPromiseCard" type="button">邀请成员确认</button>
          </div>
        </div>
      </section>
    </div>

    <div class="exit-flow-modal confirm-flow-modal" id="confirmFlowModal" aria-hidden="true">
      <button class="exit-flow-backdrop" id="confirmFlowBackdrop" type="button" aria-label="关闭成员确认流程"></button>

      <section class="exit-flow-dialog confirm-flow-dialog" role="dialog" aria-modal="true" aria-labelledby="confirmFlowTitle">
        <span class="exit-dialog-tape" aria-hidden="true"></span>
        <button class="exit-flow-close" id="closeConfirmFlow" type="button" aria-label="关闭">
          <span></span><span></span>
        </button>

        <header class="exit-flow-heading">
          <p class="exit-flow-kicker">PROMISE SIGNING · 001</p>
          <h2 id="confirmFlowTitle">每个人都要亲手确认这份承诺</h2>
          <p>成员核对自己的任务和保证金后逐个签署。全部确认完成，承诺才正式生效。</p>
        </header>

        <ol class="exit-progress confirm-progress" aria-label="成员确认承诺流程">
          <li class="is-current" data-confirm-step="0"><b>01</b><span>查看承诺邀请</span></li>
          <li data-confirm-step="1"><b>02</b><span>成员逐个签署</span></li>
          <li data-confirm-step="2"><b>03</b><span>保证金已锁定</span></li>
        </ol>

        <div class="confirm-flow-panels">
          <section class="confirm-flow-panel" data-confirm-panel="0">
            <div class="promise-invite-layout">
              <article class="promise-invite-paper">
                <span class="exit-paper-label">PROMISE INVITATION</span>
                <p class="confirm-scene-name" id="confirmSceneName">黑客松项目</p>
                <h3 id="confirmProjectName">Monad 黑客松作品开发</h3>
                <p>请先核对承诺内容。签署后，每位成员会锁定自己的模拟保证金。</p>

                <dl class="confirm-promise-meta">
                  <div><dt>承诺成员</dt><dd id="confirmMemberTotal">5 人</dd></div>
                  <div><dt>每人保证金</dt><dd><b id="confirmPerDeposit">100</b> MON</dd></div>
                  <div><dt>当前状态</dt><dd>等待成员确认</dd></div>
                </dl>

                <div class="confirm-rules-note">
                  <span>这份承诺会记录</span>
                  <p>成员任务 · 完成期限 · 保证金去向 · 退出后的救场规则</p>
                </div>
              </article>

              <aside class="promise-invite-art" aria-label="叼着承诺邀请函的小白鸽">
                <span class="invite-postmark">PLEASE<br />CONFIRM</span>
                <svg viewBox="0 0 280 225" aria-hidden="true">
                  <ellipse class="exit-bird-body" cx="112" cy="133" rx="73" ry="61" />
                  <circle class="exit-bird-head" cx="165" cy="74" r="44" />
                  <path class="exit-bird-wing" d="M60 132c18-43 60-55 98-31-12 47-48 74-90 62" />
                  <path class="exit-bird-tail" d="m52 149-45 24 52 5" />
                  <path class="exit-bird-beak" d="m205 77 40 15-39 18" />
                  <circle class="exit-bird-eye" cx="179" cy="63" r="4.4" />
                  <path class="exit-bird-leg" d="M89 188v20m50-20 6 20" />
                  <g class="confirm-letter-art"><path d="m182 104 82 12-9 58-81-12Z" /><path d="m183 106 34 30 46-19M176 160l36-25m43 38-36-37" /></g>
                </svg>
                <p>一封等待所有人签名的承诺信</p>
              </aside>
            </div>

            <footer class="exit-panel-actions">
              <button class="flow-secondary-button" id="cancelConfirmFlow" type="button">稍后再确认</button>
              <button class="flow-primary-button" id="reviewConfirmMembers" type="button">核对成员与任务</button>
            </footer>
          </section>

          <section class="confirm-flow-panel" data-confirm-panel="1" hidden>
            <div class="member-signing-layout">
              <article class="member-signing-paper">
                <span class="exit-paper-label">MEMBER SIGNATURES</span>
                <h3>选择成员，逐个盖下确认章</h3>
                <p>这里用测试钱包模拟切换成员，不会发生真实转账。</p>
                <div class="confirm-member-list" id="confirmMemberList"></div>
              </article>

              <aside class="current-signer-slip">
                <span>CURRENT SIGNER</span>
                <div class="signer-number" id="currentSignerNumber">01</div>
                <h4 id="currentSignerName">Caro</h4>
                <p id="currentSignerTask">产品与前端 Demo</p>
                <dl>
                  <div><dt>需要锁定</dt><dd><b id="signerDeposit">100</b> MON</dd></div>
                  <div><dt>测试钱包</dt><dd>0x7C...D12</dd></div>
                </dl>
                <div class="signature-line"><i></i><span>在这里盖章确认</span></div>
              </aside>
            </div>

            <footer class="exit-panel-actions">
              <button class="flow-secondary-button" id="backToPromiseInvite" type="button">返回承诺内容</button>
              <button class="flow-primary-button" id="confirmCurrentMember" type="button">确认任务并锁定保证金</button>
            </footer>
          </section>

          <section class="confirm-flow-panel confirm-complete-panel" data-confirm-panel="2" hidden>
            <div class="confirm-complete-art" aria-hidden="true">
              <span class="confirm-polka"></span>
              <svg viewBox="0 0 310 235">
                <ellipse class="exit-bird-body" cx="128" cy="137" rx="76" ry="63" />
                <circle class="exit-bird-head" cx="183" cy="77" r="46" />
                <path class="exit-bird-wing" d="M74 137c20-44 62-57 102-32-12 49-50 76-94 64" />
                <path class="exit-bird-tail" d="m64 154-47 25 54 6" />
                <path class="exit-bird-beak" d="m225 80 42 16-41 19" />
                <circle class="exit-bird-eye" cx="198" cy="66" r="4.6" />
                <path class="exit-bird-leg" d="M103 195v21m52-21 6 21" />
                <g class="confirm-contract-art"><path d="m176 124 96 13-10 70-96-14Z" /><path d="m193 151 54 7m-57 10 43 6m-47 11 30 4" /><path d="m228 181 10 10 20-26" /></g>
              </svg>
              <span class="confirm-done-stamp">SIGNED!<b>承诺生效</b></span>
            </div>

            <h3>所有成员都确认好了</h3>
            <p>任务与保证金已经锁定。之后若有人退出，他的个人保证金才会进入救场池。</p>

            <div class="confirm-receipt">
              <span>LOCK RECEIPT · 001</span>
              <div><small>已确认成员</small><strong id="confirmedMemberCount">5 / 5</strong></div>
              <div><small>每人锁定</small><strong><b id="confirmedPerDeposit">100</b> MON</strong></div>
              <div><small>模拟锁定总额</small><strong><b id="confirmedTotalDeposit">500</b> MON</strong></div>
            </div>

            <footer class="exit-panel-actions confirm-complete-actions">
              <button class="flow-secondary-button" id="restartConfirmFlow" type="button">重新演示</button>
              <button class="flow-primary-button" id="finishConfirmFlow" type="button">进入承诺卡</button>
            </footer>
          </section>
        </div>
      </section>
    </div>

    <div class="exit-flow-modal" id="exitFlowModal" aria-hidden="true">
      <button class="exit-flow-backdrop" id="exitFlowBackdrop" type="button" aria-label="关闭成员退出流程"></button>

      <section class="exit-flow-dialog" role="dialog" aria-modal="true" aria-labelledby="exitFlowTitle">
        <span class="exit-dialog-tape" aria-hidden="true"></span>
        <button class="exit-flow-close" id="closeExitFlow" type="button" aria-label="关闭">
          <span></span><span></span>
        </button>

        <header class="exit-flow-heading">
          <p class="exit-flow-kicker">MEMBER EXIT · 001</p>
          <h2 id="exitFlowTitle">成员退出了，留下的任务怎么办？</h2>
          <p>这里演示退出确认、保证金转入救场池，以及任务拆成救场票的过程。</p>
        </header>

        <ol class="exit-progress" aria-label="成员退出处理流程">
          <li class="is-current" data-exit-step="0"><b>01</b><span>确认退出成员</span></li>
          <li data-exit-step="1"><b>02</b><span>拆分遗留任务</span></li>
          <li data-exit-step="2"><b>03</b><span>转入救场池</span></li>
        </ol>

        <div class="exit-flow-panels">
          <section class="exit-flow-panel" data-exit-panel="0">
            <div class="exit-member-layout">
              <article class="exit-member-paper">
                <span class="exit-paper-label">选择退出成员</span>
                <h3>谁临时无法继续参加？</h3>
                <p>选择后会显示他留下的承诺和对应保证金。</p>
                <div class="exit-member-options" id="exitMemberOptions"></div>
              </article>

              <aside class="exit-deposit-pocket">
                <span>LOCKED DEPOSIT</span>
                <strong><b id="exitDepositAmount">100</b> MON</strong>
                <p>这笔保证金原本用于确认成员承诺，退出后将成为救场奖励。</p>
                <svg viewBox="0 0 210 155" aria-hidden="true">
                  <ellipse class="exit-bird-body" cx="81" cy="91" rx="54" ry="45" />
                  <circle class="exit-bird-head" cx="120" cy="49" r="32" />
                  <path class="exit-bird-wing" d="M42 92c14-30 45-40 73-20-9 35-38 52-69 40" />
                  <path class="exit-bird-tail" d="m39 105-30 17 35 3" />
                  <path class="exit-bird-beak" d="m149 51 29 11-28 13" />
                  <circle class="exit-bird-eye" cx="131" cy="42" r="3.3" />
                  <path class="exit-bird-leg" d="M66 128v14m37-14 4 14" />
                  <g class="exit-bird-note"><path d="m112 91 68 8-7 47-68-8Z" /><path d="m124 109 39 5m-41 10 31 4" /></g>
                </svg>
              </aside>
            </div>

            <footer class="exit-panel-actions">
              <button class="flow-secondary-button" id="cancelExitFlow" type="button">暂不退出</button>
              <button class="flow-primary-button" id="continueExitFlow" type="button">确认并查看遗留任务</button>
            </footer>
          </section>

          <section class="exit-flow-panel" data-exit-panel="1" hidden>
            <div class="exit-task-layout">
              <article class="torn-task-paper">
                <span class="exit-paper-label">TORN PROMISE NOTE</span>
                <h3><b id="exitingMemberName">Kai</b> 留下的任务纸条</h3>
                <p id="exitingMemberRole">原负责：智能合约开发</p>
                <div class="exit-task-list" id="exitTaskList"></div>
              </article>

              <div class="exit-transfer-arrow" aria-hidden="true">
                <span>保证金转化</span>
                <i>→</i>
              </div>

              <aside class="exit-fund-envelope">
                <span>RESCUE FUND</span>
                <strong><b id="exitPoolAmount">100</b> MON</strong>
                <p>金额会按遗留任务拆成救场票，验收通过后支付给补位者。</p>
                <div class="exit-allocation-stamp">READY<br /><b>待转入</b></div>
              </aside>
            </div>

            <label class="exit-confirm-rule">
              <input id="exitConfirmRule" type="checkbox" checked />
              <span></span>
              我确认：退出成员的保证金将用于处理其留下的任务缺口。
            </label>

            <footer class="exit-panel-actions">
              <button class="flow-secondary-button" id="backToExitMember" type="button">返回修改</button>
              <button class="flow-primary-button" id="confirmExitTransfer" type="button">确认退出并转入救场池</button>
            </footer>
          </section>

          <section class="exit-flow-panel exit-complete-panel" data-exit-panel="2" hidden>
            <div class="exit-receipt-illustration" aria-hidden="true">
              <svg viewBox="0 0 290 215">
                <ellipse class="exit-bird-body" cx="118" cy="125" rx="70" ry="58" />
                <circle class="exit-bird-head" cx="168" cy="69" r="42" />
                <path class="exit-bird-wing" d="M67 125c18-41 57-53 94-30-11 45-45 70-86 60" />
                <path class="exit-bird-tail" d="m58 141-43 23 49 5" />
                <path class="exit-bird-beak" d="m207 72 39 15-38 17" />
                <circle class="exit-bird-eye" cx="181" cy="59" r="4.2" />
                <path class="exit-bird-leg" d="M96 178v19m47-19 6 19" />
                <g class="exit-money-bag"><path d="M175 124c-18 13-25 29-20 47 6 22 25 32 47 27 21-5 32-22 27-43-4-16-16-28-31-36Z" /><path d="m178 123 19-2 5-17-28 4Z" /><path d="M187 140c7-5 16-2 16 5 1 9-15 8-13 16 1 6 10 7 15 3m-8-28-2 34" /></g>
              </svg>
              <span class="exit-done-stamp">RESCUE!<b>已转入</b></span>
            </div>

            <h3>保证金已经变成救场奖励</h3>
            <p>成员退出已确认，遗留任务也生成了对应救场票。项目可以继续往前走。</p>

            <div class="exit-receipt">
              <span>EXIT RECEIPT · 001</span>
              <div><small>退出成员</small><strong id="exitReceiptMember">Kai</strong></div>
              <div><small>转入救场池</small><strong><b id="exitReceiptAmount">100</b> MON</strong></div>
              <div><small>生成救场票</small><strong id="exitReceiptTickets">2 张</strong></div>
            </div>

            <footer class="exit-panel-actions exit-complete-actions">
              <button class="flow-secondary-button" id="finishExitFlow" type="button">完成并关闭</button>
              <button class="flow-primary-button" id="viewGeneratedRescueTickets" type="button">查看生成的救场票</button>
            </footer>
          </section>
        </div>
      </section>
    </div>

    <div class="rescue-flow-modal" id="rescueFlowModal" aria-hidden="true">
      <button class="rescue-flow-backdrop" id="rescueFlowBackdrop" type="button" aria-label="关闭救场任务窗口"></button>

      <section class="rescue-flow-dialog" role="dialog" aria-modal="true" aria-labelledby="rescueFlowTitle">
        <span class="rescue-ticket-tape" aria-hidden="true"></span>
        <button class="rescue-flow-close" id="closeRescueFlow" type="button" aria-label="关闭">
          <span></span><span></span>
        </button>

        <header class="rescue-flow-heading">
          <div>
            <p>RESCUE DELIVERY · NO. 01</p>
            <h2 id="rescueFlowTitle">紧急完成智能合约 MVP</h2>
          </div>
          <span class="rescue-heading-label" id="rescueFlowCategory">开发 · 黑客松</span>
        </header>

        <ol class="rescue-progress" aria-label="救场任务流程">
          <li class="is-current" data-rescue-step="0"><b>01</b><span>领取任务</span></li>
          <li data-rescue-step="1"><b>02</b><span>提交成果</span></li>
          <li data-rescue-step="2"><b>03</b><span>负责人验收</span></li>
          <li data-rescue-step="3"><b>04</b><span>奖励结算</span></li>
        </ol>

        <div class="rescue-flow-panels">
          <section class="rescue-flow-panel rescue-detail-panel" data-rescue-panel="0">
            <article class="rescue-task-paper">
              <span class="rescue-paper-clip" aria-hidden="true"></span>
              <p class="rescue-panel-kicker">OPEN RESCUE TICKET</p>
              <h3 id="rescueDetailTitle">紧急完成智能合约 MVP</h3>
              <p class="rescue-task-copy" id="rescueTaskCopy">队友退出后留下了合约开发缺口。领取后，请在截止时间前完成核心逻辑并提交可运行成果。</p>

              <dl class="rescue-task-meta">
                <div><dt>截止时间</dt><dd id="rescueDeadline">08 月 08 日 · 22:00</dd></div>
                <div><dt>验收凭证</dt><dd id="rescueProof">GitHub 仓库与部署说明</dd></div>
                <div><dt>验收人</dt><dd>项目负责人 · Caro</dd></div>
              </dl>

              <div class="rescue-checklist">
                <p>交付清单</p>
                <label><input type="checkbox" checked disabled /><span></span><em id="rescueCheckOne">完成保证金、退出与救场池逻辑</em></label>
                <label><input type="checkbox" checked disabled /><span></span><em id="rescueCheckTwo">补齐悬赏领取、提交与验收状态</em></label>
                <label><input type="checkbox" checked disabled /><span></span><em id="rescueCheckThree">提交代码仓库和简短运行说明</em></label>
              </div>

              <p class="rescue-paper-note">领取后，这张票会暂时从救场大厅收起，避免多人重复开工。</p>
            </article>

            <aside class="rescue-reward-pocket">
              <p class="rescue-panel-kicker">RESCUE REWARD</p>
              <div class="rescue-reward-number"><strong id="rescueReward">80</strong><span>MON</span></div>
              <p>来自退出成员的保证金<br />验收通过后发放</p>

              <svg class="flow-money-pigeon" viewBox="0 0 270 225" role="img" aria-label="抱着救场钱袋的小白鸽">
                <ellipse class="flow-bird-body" cx="116" cy="126" rx="70" ry="61" />
                <circle class="flow-bird-head" cx="165" cy="70" r="42" />
                <path class="flow-bird-wing" d="M65 127c20-40 60-50 95-22-14 45-49 65-89 50" />
                <path class="flow-bird-tail" d="m55 145-43 22 49 6" />
                <path class="flow-bird-beak" d="m202 73 39 15-38 17" />
                <circle class="flow-bird-eye" cx="178" cy="59" r="4.5" />
                <path class="flow-bird-leg" d="M91 179v21m52-21 6 21" />
                <g class="flow-money-bag">
                  <path d="M135 119c-18 13-26 30-22 50 5 24 26 38 51 34 25-4 39-22 35-46-3-18-15-32-32-42Z" />
                  <path d="m138 117 28 1 7-19-37 1Z" />
                  <path d="M153 135c7-5 18-2 19 7 1 11-18 9-16 20 1 7 12 9 19 4m-11-35-2 42" />
                </g>
              </svg>
              <span class="reward-source-stamp">资金来源<br /><strong>可查看</strong></span>
            </aside>

            <footer class="rescue-panel-actions">
              <button class="flow-secondary-button" id="backToRescueHall" type="button">先看看其他任务</button>
              <button class="flow-primary-button" id="claimRescueTask" type="button">领取这张救场票</button>
            </footer>
          </section>

          <section class="rescue-flow-panel rescue-submit-panel" data-rescue-panel="1" hidden>
            <div class="submission-paper">
              <span class="submission-number">02 · SUBMIT WORK</span>
              <h3>把救场成果装进回邮信封</h3>
              <p>提交可查看的成果地址，并留一段给验收人的说明。</p>

              <label class="submission-field">
                <span>成果链接</span>
                <input id="rescueWorkLink" type="url" value="https://github.com/Gos0lis/dont-ghost-me" />
              </label>
              <label class="submission-field">
                <span>交付说明</span>
                <textarea id="rescueWorkNote" rows="4">已补齐核心合约逻辑与部署说明，请按交付清单验收。</textarea>
              </label>

              <div class="submission-attachment">
                <svg viewBox="0 0 62 50" aria-hidden="true"><path d="M13 10h36v29H13zM14 11l17 14 17-14" /><path d="m9 16-5 4m8 3-7 2" /></svg>
                <span><strong>交付凭证已装好</strong><small id="submissionProofSummary">代码仓库 · 部署说明 · 运行截图</small></span>
              </div>
            </div>

            <aside class="claimed-receipt">
              <p>CLAIM RECEIPT · 001</p>
              <div class="claimed-stamp">CLAIMED!<strong>已领取</strong></div>
              <dl>
                <div><dt>救场者</dt><dd>测试钱包 · Caro</dd></div>
                <div><dt>任务</dt><dd id="claimedTaskName">紧急完成智能合约 MVP</dd></div>
                <div><dt>预留奖励</dt><dd><b id="claimedReward">80</b> MON</dd></div>
                <div><dt>当前状态</dt><dd>等待提交成果</dd></div>
              </dl>
              <svg class="claimed-pigeon" viewBox="0 0 180 125" aria-hidden="true">
                <ellipse class="flow-bird-body" cx="77" cy="75" rx="48" ry="40" />
                <circle class="flow-bird-head" cx="112" cy="39" r="29" />
                <path class="flow-bird-wing" d="M41 76c15-27 43-35 68-16-10 31-36 44-63 34" />
                <path class="flow-bird-tail" d="m37 88-28 15 32 4" />
                <path class="flow-bird-beak" d="m137 42 27 10-26 12" />
                <circle class="flow-bird-eye" cx="121" cy="32" r="3" />
                <path class="flow-bird-leg" d="M61 108v12m36-12 4 12" />
              </svg>
            </aside>

            <footer class="rescue-panel-actions">
              <button class="flow-secondary-button" id="returnToTaskDetail" type="button">返回任务详情</button>
              <button class="flow-primary-button" id="submitRescueWork" type="button">提交救场成果</button>
            </footer>
          </section>

          <section class="rescue-flow-panel rescue-review-panel" data-rescue-panel="2" hidden>
            <article class="review-delivery-paper">
              <p class="rescue-panel-kicker">DELIVERY FOR REVIEW</p>
              <h3>救场成果已送达</h3>
              <p>现在切换到负责人视角，核对成果是否满足原来的任务缺口。</p>

              <div class="review-link-slip">
                <span>成果地址</span>
                <strong id="reviewWorkLink">github.com/Gos0lis/dont-ghost-me</strong>
                <small id="reviewWorkNote">已补齐核心合约逻辑与部署说明，请按交付清单验收。</small>
              </div>

              <div class="review-checks">
                <span><i></i>成果链接可以打开</span>
                <span><i></i>交付内容符合任务描述</span>
                <span><i></i>奖励金额与救场票一致</span>
              </div>
            </article>

            <aside class="reviewer-paper">
              <span class="reviewer-label">OWNER CHECK</span>
              <svg class="review-pigeon" viewBox="0 0 210 155" aria-hidden="true">
                <ellipse class="flow-bird-body" cx="89" cy="91" rx="55" ry="46" />
                <circle class="flow-bird-head" cx="129" cy="49" r="33" />
                <path class="flow-bird-wing" d="M48 91c16-31 49-40 77-19-11 36-41 51-72 39" />
                <path class="flow-bird-tail" d="m44 105-32 17 37 4" />
                <path class="flow-bird-beak" d="m158 51 31 12-30 13" />
                <circle class="flow-bird-eye" cx="140" cy="41" r="3.4" />
                <path class="flow-bird-leg" d="M72 129v15m39-15 5 15" />
                <g class="flow-rubber-stamp"><path d="M129 91h50v31h-50z" /><path d="M139 91c0-15 28-15 29 0M124 122h61v11h-61z" /></g>
              </svg>
              <h4>负责人 · Caro</h4>
              <p>通过后，奖励会从救场池结算给任务领取者。</p>
              <div class="reviewer-sign">READY TO STAMP</div>
            </aside>

            <footer class="rescue-panel-actions">
              <button class="flow-secondary-button" id="requestRescueRevision" type="button">要求返修</button>
              <button class="flow-primary-button" id="approveRescueWork" type="button">通过并发放奖励</button>
            </footer>
          </section>

          <section class="rescue-flow-panel rescue-paid-panel" data-rescue-panel="3" hidden>
            <p class="paid-kicker">RESCUE COMPLETE · DONE!</p>
            <div class="paid-illustration" aria-hidden="true">
              <span class="paid-polka"></span>
              <svg viewBox="0 0 330 245">
                <ellipse class="flow-bird-body" cx="139" cy="139" rx="78" ry="66" />
                <circle class="flow-bird-head" cx="195" cy="80" r="47" />
                <path class="flow-bird-wing" d="M83 139c20-45 63-59 104-35-11 49-48 79-94 68" />
                <path class="flow-bird-tail" d="m73 157-49 25 56 6" />
                <path class="flow-bird-beak" d="m234 82 43 16-41 19" />
                <circle class="flow-bird-eye" cx="210" cy="69" r="5" />
                <path class="flow-bird-leg" d="M113 204v22m53-22 7 22" />
                <g class="paid-receipt-art"><path d="m190 125 92 14-10 68-92-15Z" /><path d="m204 151 54 8m-57 9 43 7m-46 8 29 5" /><path d="m245 182 8 8 16-20" /></g>
              </svg>
              <span class="paid-done-stamp">DONE!<strong>救场完成</strong></span>
            </div>

            <h3>缺口补上了，奖励也送到了</h3>
            <p>这张救场票已经完成结算。原项目可以继续推进，资金流向保留在回执中。</p>

            <div class="paid-receipt">
              <span>SETTLEMENT RECEIPT · 001</span>
              <strong id="paidTaskName">紧急完成智能合约 MVP</strong>
              <div><small>救场者</small><b>测试钱包 · Caro</b></div>
              <div><small>已发放奖励</small><b><em id="paidReward">80</em> MON</b></div>
              <div><small>状态</small><b>已验收 · 已结算</b></div>
            </div>

            <footer class="rescue-panel-actions paid-actions">
              <button class="flow-secondary-button" id="replayRescueFlow" type="button">重新演示流程</button>
              <button class="flow-primary-button" id="finishRescueFlow" type="button">完成并关闭</button>
            </footer>
          </section>
        </div>
      </section>
    </div>

    <script>
const intro = document.querySelector("#intro");
const introBrand = document.querySelector("#introBrand");
const flock = document.querySelector("#flock");
const app = document.querySelector("#app");
const skipIntro = document.querySelector("#skipIntro");
const enterButton = document.querySelector("#enterButton");
const toast = document.querySelector("#toast");
const promiseModal = document.querySelector("#promiseModal");
const promiseEditor = document.querySelector("#promiseEditor");
const promiseSuccess = document.querySelector("#promiseSuccess");
const projectNameInput = document.querySelector("#projectName");
const deadlineInput = document.querySelector("#promiseDeadline");
const depositInput = document.querySelector("#promiseDeposit");
const memberList = document.querySelector("#memberList");
const customSceneField = document.querySelector("#customSceneField");
const customSceneName = document.querySelector("#customSceneName");
const confirmFlowModal = document.querySelector("#confirmFlowModal");
const confirmFlowDialog = confirmFlowModal.querySelector(".confirm-flow-dialog");
const confirmMemberList = document.querySelector("#confirmMemberList");
const exitFlowModal = document.querySelector("#exitFlowModal");
const exitFlowDialog = exitFlowModal.querySelector(".exit-flow-dialog");
const exitMemberOptions = document.querySelector("#exitMemberOptions");
const rescueFlowModal = document.querySelector("#rescueFlowModal");
const rescueFlowDialog = rescueFlowModal.querySelector(".rescue-flow-dialog");
const ticketList = document.querySelector("#ticketList");
let activeRescueTicket = null;
let rescueFlowOrigin = null;
let exitFlowOrigin = null;
let confirmFlowOrigin = null;

const pigeonSettings = [
  { pose: "up", top: "10%", delay: "0s", duration: "2.62s", size: "96px", scale: 0.82, rotate: "-5deg" },
  { pose: "down", top: "27%", delay: "0.14s", duration: "2.86s", size: "132px", scale: 1.06, rotate: "2deg" },
  { pose: "up", top: "68%", delay: "0.2s", duration: "2.7s", size: "110px", scale: 0.93, rotate: "-3deg" },
  { pose: "down", top: "46%", delay: "0.38s", duration: "2.95s", size: "88px", scale: 0.77, rotate: "4deg" },
  { pose: "up", top: "82%", delay: "0.44s", duration: "2.58s", size: "122px", scale: 1, rotate: "-4deg" },
  { pose: "down", top: "5%", delay: "0.64s", duration: "2.9s", size: "82px", scale: 0.7, rotate: "3deg" },
  { pose: "up", top: "56%", delay: "0.72s", duration: "2.82s", size: "140px", scale: 1.12, rotate: "1deg" },
  { pose: "down", top: "36%", delay: "0.83s", duration: "2.75s", size: "72px", scale: 0.64, rotate: "-2deg" }
];

flock.innerHTML = pigeonSettings
  .map(
    (pigeon) => `
      <div class="flying-pigeon" style="--top:${pigeon.top};--delay:${pigeon.delay};--duration:${pigeon.duration};--size:${pigeon.size};--scale:${pigeon.scale};--rotate:${pigeon.rotate}">
        <svg viewBox="0 0 180 112"><use href="#flying-pigeon-${pigeon.pose}"></use></svg>
      </div>`
  )
  .join("");

let introClosed = false;
let toastTimer;

function closeIntro() {
  if (introClosed) return;
  introClosed = true;
  intro.classList.add("is-hidden");
  app.classList.add("is-ready");
  app.setAttribute("aria-hidden", "false");
}

function showToast(message) {
  window.clearTimeout(toastTimer);
  toast.textContent = message;
  toast.classList.add("is-visible");
  toastTimer = window.setTimeout(() => toast.classList.remove("is-visible"), 2300);
}

window.setTimeout(() => introBrand.classList.add("is-logo-visible"), 2450);
window.setTimeout(() => introBrand.classList.add("is-copy-visible"), 3250);
const autoEnter = window.setTimeout(closeIntro, 6400);

[skipIntro, enterButton].forEach((button) => {
  button.addEventListener("click", () => {
    window.clearTimeout(autoEnter);
    closeIntro();
  });
});

function scrollToSection(id) {
  const target = document.getElementById(id);
  if (!target) return;
  target.scrollIntoView({ behavior: "smooth", block: "start" });
}

document.querySelectorAll("[data-target]").forEach((button) => {
  button.addEventListener("click", () => scrollToSection(button.dataset.target));
});

document.querySelectorAll("[data-scroll]").forEach((button) => {
  button.addEventListener("click", () => scrollToSection(button.dataset.scroll));
});

function updateMemberNumbers() {
  const rows = [...memberList.querySelectorAll(".member-row")];
  rows.forEach((row, index) => {
    row.querySelector(".member-tag").textContent = String(index + 1).padStart(2, "0");
  });
  document.querySelector("#previewMembers").textContent = `${rows.length} 人`;
}

function updatePromisePreview() {
  const projectName = projectNameInput.value.trim() || "还没写名字的承诺";
  const deadline = deadlineInput.value ? deadlineInput.value.replaceAll("-", ".") : "待确定";
  const deposit = depositInput.value || "0";

  document.querySelector("#previewTitle").textContent = projectName;
  document.querySelector("#previewDeadline").textContent = deadline;
  document.querySelector("#previewDeposit").textContent = deposit;
  document.querySelector("#successProjectName").textContent = projectName;
  updateMemberNumbers();
}

function showPromiseEditor() {
  promiseEditor.hidden = false;
  promiseSuccess.hidden = true;
}

function openPromiseModal() {
  showPromiseEditor();
  updatePromisePreview();
  promiseModal.classList.add("is-open");
  promiseModal.setAttribute("aria-hidden", "false");
  document.body.classList.add("modal-open");
  window.setTimeout(() => document.querySelector("#closePromiseModal").focus(), 60);
}

function closePromiseModal() {
  promiseModal.classList.remove("is-open");
  promiseModal.setAttribute("aria-hidden", "true");
  document.body.classList.remove("modal-open");
  document.querySelector("#promiseButton").focus();
}

document.querySelector("#promiseButton").addEventListener("click", openPromiseModal);
document.querySelector("#closePromiseModal").addEventListener("click", closePromiseModal);
document.querySelector("#promiseBackdrop").addEventListener("click", closePromiseModal);

document.addEventListener("keydown", (event) => {
  if (event.key === "Escape" && promiseModal.classList.contains("is-open")) closePromiseModal();
  if (event.key === "Escape" && confirmFlowModal.classList.contains("is-open")) closeConfirmFlow();
  if (event.key === "Escape" && exitFlowModal.classList.contains("is-open")) closeExitFlow();
  if (event.key === "Escape" && rescueFlowModal.classList.contains("is-open")) closeRescueFlow();
});

const scenePresets = {
  hackathon: {
    label: "黑客松团队",
    name: "Monad 黑客松作品开发",
    deadline: "2026-08-09",
    deposit: "100",
    members: [
      ["Caro", "产品与前端 Demo"],
      ["Lin", "智能合约与测试"]
    ]
  },
  travel: {
    label: "朋友旅行",
    name: "周末海边旅行计划",
    deadline: "2026-08-16",
    deposit: "50",
    members: [
      ["Caro", "整理行程与酒店信息"],
      ["Mia", "确认车票与集合时间"]
    ]
  },
  custom: {
    label: "我的多人计划",
    name: "",
    deadline: "",
    deposit: "0",
    members: [
      ["Caro", "待分配任务"],
      ["新成员", "待分配任务"]
    ]
  }
};

function memberRowTemplate(name, task) {
  return `
    <div class="member-row">
      <span class="member-tag">00</span>
      <label><span>成员</span><input class="member-name" type="text" value="${name}" /></label>
      <label><span>负责的承诺</span><input class="member-task" type="text" value="${task}" /></label>
      <button class="remove-member" type="button" aria-label="移除成员">×</button>
    </div>`;
}

document.querySelectorAll(".scene-option").forEach((button) => {
  button.addEventListener("click", () => {
    document.querySelectorAll(".scene-option").forEach((item) => item.classList.remove("is-selected"));
    button.classList.add("is-selected");

    const preset = scenePresets[button.dataset.scene];
    const isCustomScene = button.dataset.scene === "custom";
    customSceneField.hidden = !isCustomScene;
    projectNameInput.value = preset.name;
    deadlineInput.value = preset.deadline;
    depositInput.value = preset.deposit;
    memberList.innerHTML = preset.members.map(([name, task]) => memberRowTemplate(name, task)).join("");
    document.querySelector("#previewScene").textContent = isCustomScene
      ? customSceneName.value.trim() || "自定义场景"
      : preset.label;
    updatePromisePreview();

    if (isCustomScene) window.setTimeout(() => customSceneName.select(), 60);
  });
});

customSceneName.addEventListener("input", () => {
  const customOption = document.querySelector('.scene-option[data-scene="custom"]');
  if (!customOption.classList.contains("is-selected")) return;
  document.querySelector("#previewScene").textContent = customSceneName.value.trim() || "自定义场景";
});

[projectNameInput, deadlineInput, depositInput].forEach((input) => {
  input.addEventListener("input", updatePromisePreview);
});

memberList.addEventListener("input", updatePromisePreview);

memberList.addEventListener("click", (event) => {
  const removeButton = event.target.closest(".remove-member");
  if (!removeButton) return;
  if (memberList.querySelectorAll(".member-row").length === 1) {
    showToast("至少留下一位承诺成员");
    return;
  }
  removeButton.closest(".member-row").remove();
  updatePromisePreview();
});

document.querySelector("#addMemberButton").addEventListener("click", () => {
  memberList.insertAdjacentHTML("beforeend", memberRowTemplate("新成员", "待分配任务"));
  updatePromisePreview();
  memberList.lastElementChild.querySelector(".member-name").select();
});

document.querySelector("#saveDraftButton").addEventListener("click", () => {
  updatePromisePreview();
  showToast("草稿已保存 · 这是界面演示状态");
});

document.querySelector("#sendPromiseButton").addEventListener("click", () => {
  if (!projectNameInput.value.trim()) {
    projectNameInput.focus();
    showToast("先给这份承诺写一个名字");
    return;
  }
  if (!document.querySelector("#promiseRule").checked) {
    showToast("请先确认退出与救场规则");
    return;
  }

  updatePromisePreview();
  promiseEditor.hidden = true;
  promiseSuccess.hidden = false;
  promiseModal.querySelector(".promise-dialog").scrollTop = 0;
});

document.querySelector("#makeAnotherPromise").addEventListener("click", () => {
  showPromiseEditor();
  promiseModal.querySelector(".promise-dialog").scrollTop = 0;
});

document.querySelector("#viewPromiseCard").addEventListener("click", () => {
  closePromiseModal();
  window.setTimeout(() => openConfirmFlow(document.querySelector("#openConfirmFlow")), 180);
});

const demoScenarios = {
  hackathon: {
    eyebrow: "MY PROMISE · 001",
    workspaceDescription: "一张承诺卡，记下成员、任务与每一笔保证金的去向。",
    label: "黑客松项目",
    title: "Monad 黑客松作品开发",
    summary: "成员共同完成产品、前端和合约 Demo。当前有一名成员退出，保证金已转入救场池。",
    members: "5 人",
    deposit: "100 MON",
    stage: "Demo 开发",
    timeline: ["承诺已确认", "1 人退出", "救场悬赏已发布", "等待验收与结算"],
    fund: "100",
    fundLines: ["退出成员的保证金", "已进入救场池"],
    confirmationMembers: [
      ["Caro", "产品与前端 Demo"],
      ["Lin", "智能合约与测试"],
      ["Kai", "智能合约开发"],
      ["Mia", "视觉设计与测试"],
      ["Rui", "调研与演示材料"]
    ],
    exitMembers: [
      ["Kai", "智能合约开发"],
      ["Caro", "产品与前端 Demo"],
      ["Lin", "测试与验收"]
    ],
    rescueEyebrow: "RESCUE BOARD · HACKATHON",
    rescueDescription: "选择一张任务票，补上团队留下的开发缺口。",
    tickets: [
      {
        title: "紧急完成智能合约 MVP",
        category: "开发 · 黑客松",
        summary: "提交代码仓库，等待团队验收",
        reward: "80",
        deadline: "08 月 08 日 · 22:00",
        proof: "GitHub 仓库与部署说明",
        proofSummary: "代码仓库 · 部署说明 · 运行截图",
        copy: "队友退出后留下了合约开发缺口。领取后，请在截止时间前完成核心逻辑并提交可运行成果。",
        checks: ["完成保证金、退出与救场池逻辑", "补齐悬赏领取、提交与验收状态", "提交代码仓库和简短运行说明"],
        workLink: "https://github.com/Gos0lis/dont-ghost-me",
        workNote: "已补齐核心合约逻辑与部署说明，请按交付清单验收。"
      },
      {
        title: "整理代码交接与部署说明",
        category: "文档 · 黑客松",
        summary: "补充必要文件与运行步骤",
        reward: "20",
        deadline: "08 月 09 日 · 12:00",
        proof: "交接文档与运行截图",
        proofSummary: "交接文档 · 运行步骤 · 演示截图",
        copy: "退出成员留下的代码缺少交接信息。领取后，请把运行方式、必要文件和部署步骤整理清楚。",
        checks: ["确认现有仓库可以正常运行", "补齐环境配置和部署步骤", "提交交接文档与演示截图"],
        workLink: "https://github.com/Gos0lis/dont-ghost-me",
        workNote: "已整理运行环境、部署步骤和必要文件，请按交付清单验收。"
      }
    ]
  },
  travel: {
    eyebrow: "MY PROMISE · 002",
    workspaceDescription: "同一张承诺卡，也可以记下旅行成员、共同费用和临时退出后的补救安排。",
    label: "朋友旅行",
    title: "周末海边旅行计划",
    summary: "四位朋友已经确认车票和酒店。一名成员临时退出，其保证金被拆成三张补救票。",
    members: "4 人",
    deposit: "50 MON",
    stage: "行程确认",
    timeline: ["旅行承诺已确认", "1 人临时退出", "3 张补救票已发布", "等待行程重新确认"],
    fund: "50",
    fundLines: ["退出成员的保证金", "被拆成三项补救悬赏"],
    confirmationMembers: [
      ["Caro", "酒店与预算"],
      ["Mia", "票务与行程协调"],
      ["Lin", "交通与集合安排"],
      ["Kai", "活动与餐厅预订"]
    ],
    exitMembers: [
      ["Mia", "票务与行程协调"],
      ["Caro", "酒店与预算"],
      ["Lin", "交通与集合安排"]
    ],
    rescueEyebrow: "RESCUE BOARD · TRAVEL",
    rescueDescription: "找替补、处理门票和修改酒店，每个麻烦都对应一张救场票。",
    tickets: [
      {
        title: "寻找可以同行的旅行替补",
        category: "协调 · 朋友旅行",
        summary: "确认日期、预算和费用交接",
        reward: "20",
        deadline: "08 月 15 日 · 18:00",
        proof: "替补确认记录与费用交接",
        proofSummary: "替补确认 · 群聊记录 · 费用交接",
        copy: "一名朋友临时退出，原来的房间和行程留下空位。请找到合适的替补，并完成信息与费用交接。",
        checks: ["确认替补可以参加完整行程", "说明预算、集合时间和共同规则", "在旅行群完成成员与费用交接"],
        workLink: "https://example.com/travel-replacement-proof",
        workNote: "替补成员已经确认日期和预算，费用与群聊信息已完成交接。"
      },
      {
        title: "转卖临时空出的活动门票",
        category: "票务 · 朋友旅行",
        summary: "发布转让信息并完成收款",
        reward: "15",
        deadline: "08 月 16 日 · 12:00",
        proof: "转卖记录与收款凭证",
        proofSummary: "转让页面 · 沟通记录 · 收款凭证",
        copy: "退出成员已经购买活动门票。请发布真实转让信息，找到接手者并把处理结果同步给团队。",
        checks: ["核对票面信息与可转让规则", "完成买家沟通和票务交接", "提交转让记录与收款凭证"],
        workLink: "https://example.com/ticket-transfer-proof",
        workNote: "活动门票已经完成转让，买家确认收到票务信息，收款记录已附上。"
      },
      {
        title: "修改酒店入住人与行程",
        category: "行程 · 朋友旅行",
        summary: "联系酒店并同步新安排",
        reward: "15",
        deadline: "08 月 16 日 · 20:00",
        proof: "酒店修改确认与新行程",
        proofSummary: "酒店确认 · 新入住人 · 更新行程",
        copy: "成员变化后，酒店入住人与集合安排需要同步修改。请联系酒店并把新的行程发给所有成员。",
        checks: ["确认酒店允许修改入住信息", "更新入住人、房型与费用分配", "把新行程同步给全部成员"],
        workLink: "https://example.com/hotel-change-proof",
        workNote: "酒店已确认修改入住人，费用和集合时间也已经同步给全部成员。"
      }
    ]
  }
};

function ticketTemplate(task, index) {
  return `
    <button class="rescue-ticket" type="button"
      data-task="${task.title}"
      data-reward="${task.reward}"
      data-category="${task.category}"
      data-deadline="${task.deadline}"
      data-proof="${task.proof}"
      data-proof-summary="${task.proofSummary}"
      data-copy="${task.copy}"
      data-check-one="${task.checks[0]}"
      data-check-two="${task.checks[1]}"
      data-check-three="${task.checks[2]}"
      data-work-link="${task.workLink}"
      data-work-note="${task.workNote}">
      <span class="ticket-number">NO. ${String(index + 1).padStart(2, "0")}</span>
      <span class="ticket-copy">
        <small>${task.category}</small>
        <strong>${task.title}</strong>
        <em>${task.summary}</em>
      </span>
      <span class="ticket-reward"><b>${task.reward}</b> MON</span>
      <span class="ticket-status">待领取</span>
    </button>`;
}

function applyDemoScenario(sceneKey, announce = true) {
  const scene = demoScenarios[sceneKey];
  document.querySelector("#commitment").dataset.scene = sceneKey;
  document.querySelector("#workspaceEyebrow").textContent = scene.eyebrow;
  document.querySelector("#workspaceDescription").textContent = scene.workspaceDescription;
  document.querySelector("#projectSceneLabel").textContent = scene.label;
  document.querySelector("#projectCardTitle").textContent = scene.title;
  document.querySelector("#projectSummary").textContent = scene.summary;
  document.querySelector("#projectMemberCount").textContent = scene.members;
  document.querySelector("#projectDeposit").textContent = scene.deposit;
  document.querySelector("#projectStage").textContent = scene.stage;
  ["#timelineOne", "#timelineTwo", "#timelineThree", "#timelineFour"].forEach((selector, index) => {
    document.querySelector(selector).textContent = scene.timeline[index];
  });
  document.querySelector("#fundAmount").textContent = scene.fund;
  document.querySelector("#fundDescription").innerHTML = `${scene.fundLines[0]}<br />${scene.fundLines[1]}`;
  document.querySelector("#rescueBoardEyebrow").textContent = scene.rescueEyebrow;
  document.querySelector("#rescueBoardDescription").textContent = scene.rescueDescription;
  ticketList.innerHTML = scene.tickets.map(ticketTemplate).join("");

  document.querySelectorAll("[data-demo-scene]").forEach((button) => {
    const selected = button.dataset.demoScene === sceneKey;
    button.classList.toggle("is-active", selected);
    button.setAttribute("aria-selected", String(selected));
  });

  if (announce) showToast(`已切换到${scene.label}演示`);
}

function confirmMemberTemplate(member, index) {
  const [name, task] = member;
  return `
    <button class="confirm-member-card${index === 0 ? " is-selected" : ""}" type="button" data-index="${index}" data-name="${name}" data-task="${task}">
      <i>${String(index + 1).padStart(2, "0")}</i>
      <span><strong>${name}</strong><small>${task}</small></span>
      <em>待确认</em>
    </button>`;
}

function selectConfirmMember(card) {
  if (!card || card.classList.contains("is-signed")) return;
  confirmMemberList.querySelectorAll(".confirm-member-card").forEach((item) => {
    item.classList.toggle("is-selected", item === card);
  });
  document.querySelector("#currentSignerNumber").textContent = String(Number(card.dataset.index) + 1).padStart(2, "0");
  document.querySelector("#currentSignerName").textContent = card.dataset.name;
  document.querySelector("#currentSignerTask").textContent = card.dataset.task;
}

function fillConfirmFlow(scene) {
  const memberCount = scene.confirmationMembers.length;
  const totalLocked = Number(scene.fund) * memberCount;
  document.querySelector("#confirmSceneName").textContent = scene.label;
  document.querySelector("#confirmProjectName").textContent = scene.title;
  document.querySelector("#confirmMemberTotal").textContent = `${memberCount} 人`;
  ["#confirmPerDeposit", "#signerDeposit", "#confirmedPerDeposit"].forEach((selector) => {
    document.querySelector(selector).textContent = scene.fund;
  });
  document.querySelector("#confirmedTotalDeposit").textContent = totalLocked;
  document.querySelector("#confirmedMemberCount").textContent = `0 / ${memberCount}`;
  confirmMemberList.innerHTML = scene.confirmationMembers.map(confirmMemberTemplate).join("");
  selectConfirmMember(confirmMemberList.querySelector(".confirm-member-card"));
}

function setConfirmStep(step) {
  confirmFlowModal.querySelectorAll("[data-confirm-panel]").forEach((panel) => {
    panel.hidden = Number(panel.dataset.confirmPanel) !== step;
  });
  confirmFlowModal.querySelectorAll("[data-confirm-step]").forEach((item) => {
    const itemStep = Number(item.dataset.confirmStep);
    item.classList.toggle("is-current", itemStep === step);
    item.classList.toggle("is-done", itemStep < step);
  });
  confirmFlowDialog.scrollTop = 0;
}

function openConfirmFlow(origin) {
  const sceneKey = document.querySelector("#commitment").dataset.scene || "hackathon";
  fillConfirmFlow(demoScenarios[sceneKey]);
  setConfirmStep(0);
  confirmFlowOrigin = origin;
  confirmFlowModal.classList.add("is-open");
  confirmFlowModal.setAttribute("aria-hidden", "false");
  document.body.classList.add("modal-open");
  window.setTimeout(() => document.querySelector("#closeConfirmFlow").focus(), 60);
}

function closeConfirmFlow() {
  confirmFlowModal.classList.remove("is-open");
  confirmFlowModal.setAttribute("aria-hidden", "true");
  if (!promiseModal.classList.contains("is-open") && !exitFlowModal.classList.contains("is-open") && !rescueFlowModal.classList.contains("is-open")) {
    document.body.classList.remove("modal-open");
  }
  if (confirmFlowOrigin) confirmFlowOrigin.focus();
}

document.querySelector("#openConfirmFlow").addEventListener("click", (event) => openConfirmFlow(event.currentTarget));
document.querySelector("#closeConfirmFlow").addEventListener("click", closeConfirmFlow);
document.querySelector("#confirmFlowBackdrop").addEventListener("click", closeConfirmFlow);
document.querySelector("#cancelConfirmFlow").addEventListener("click", closeConfirmFlow);
document.querySelector("#reviewConfirmMembers").addEventListener("click", () => setConfirmStep(1));
document.querySelector("#backToPromiseInvite").addEventListener("click", () => setConfirmStep(0));

confirmMemberList.addEventListener("click", (event) => {
  const card = event.target.closest(".confirm-member-card");
  if (!card) return;
  if (card.classList.contains("is-signed")) {
    showToast(`${card.dataset.name} 已经确认过了`);
    return;
  }
  selectConfirmMember(card);
});

document.querySelector("#confirmCurrentMember").addEventListener("click", () => {
  const currentCard = confirmMemberList.querySelector(".confirm-member-card.is-selected");
  if (!currentCard) return;
  currentCard.classList.remove("is-selected");
  currentCard.classList.add("is-signed");
  currentCard.querySelector("em").textContent = "已确认";

  const cards = [...confirmMemberList.querySelectorAll(".confirm-member-card")];
  const signedCount = cards.filter((card) => card.classList.contains("is-signed")).length;
  document.querySelector("#confirmedMemberCount").textContent = `${signedCount} / ${cards.length}`;
  const nextCard = cards.find((card) => !card.classList.contains("is-signed"));

  if (nextCard) {
    selectConfirmMember(nextCard);
    showToast(`${currentCard.dataset.name} 已确认，切换到下一位成员`);
    return;
  }

  setConfirmStep(2);
  showToast("所有成员都已确认，模拟保证金锁定完成");
});

document.querySelector("#restartConfirmFlow").addEventListener("click", () => {
  const sceneKey = document.querySelector("#commitment").dataset.scene || "hackathon";
  fillConfirmFlow(demoScenarios[sceneKey]);
  setConfirmStep(0);
});

document.querySelector("#finishConfirmFlow").addEventListener("click", () => {
  closeConfirmFlow();
  window.setTimeout(() => scrollToSection("commitment"), 120);
});

function exitMemberTemplate(member, index) {
  const [name, role] = member;
  return `
    <button class="exit-member-option${index === 0 ? " is-selected" : ""}" type="button" data-name="${name}" data-role="${role}">
      <i>${String(index + 1).padStart(2, "0")}</i>
      <span><strong>${name}</strong><small>${role}</small></span>
      <em aria-hidden="true"></em>
    </button>`;
}

function selectExitMember(option) {
  exitMemberOptions.querySelectorAll(".exit-member-option").forEach((item) => {
    item.classList.toggle("is-selected", item === option);
  });
  document.querySelector("#exitingMemberName").textContent = option.dataset.name;
  document.querySelector("#exitingMemberRole").textContent = `原负责：${option.dataset.role}`;
  document.querySelector("#exitReceiptMember").textContent = option.dataset.name;
}

function fillExitFlow(scene) {
  const depositTargets = ["#exitDepositAmount", "#exitPoolAmount", "#exitReceiptAmount"];
  depositTargets.forEach((selector) => {
    document.querySelector(selector).textContent = scene.fund;
  });

  exitMemberOptions.innerHTML = scene.exitMembers.map(exitMemberTemplate).join("");
  document.querySelector("#exitTaskList").innerHTML = scene.tickets
    .map(
      (task) => `
        <div class="exit-task-slip">
          <span><strong>${task.title}</strong><small>${task.category}</small></span>
          <b>${task.reward} MON</b>
        </div>`
    )
    .join("");
  document.querySelector("#exitReceiptTickets").textContent = `${scene.tickets.length} 张`;
  selectExitMember(exitMemberOptions.querySelector(".exit-member-option"));
}

function setExitStep(step) {
  exitFlowModal.querySelectorAll("[data-exit-panel]").forEach((panel) => {
    panel.hidden = Number(panel.dataset.exitPanel) !== step;
  });
  exitFlowModal.querySelectorAll("[data-exit-step]").forEach((item) => {
    const itemStep = Number(item.dataset.exitStep);
    item.classList.toggle("is-current", itemStep === step);
    item.classList.toggle("is-done", itemStep < step);
  });
  exitFlowDialog.scrollTop = 0;
}

function openExitFlow(origin) {
  const sceneKey = document.querySelector("#commitment").dataset.scene || "hackathon";
  fillExitFlow(demoScenarios[sceneKey]);
  setExitStep(0);
  exitFlowOrigin = origin;
  exitFlowModal.classList.add("is-open");
  exitFlowModal.setAttribute("aria-hidden", "false");
  document.body.classList.add("modal-open");
  window.setTimeout(() => document.querySelector("#closeExitFlow").focus(), 60);
}

function closeExitFlow() {
  exitFlowModal.classList.remove("is-open");
  exitFlowModal.setAttribute("aria-hidden", "true");
  if (!promiseModal.classList.contains("is-open") && !confirmFlowModal.classList.contains("is-open") && !rescueFlowModal.classList.contains("is-open")) {
    document.body.classList.remove("modal-open");
  }
  if (exitFlowOrigin) exitFlowOrigin.focus();
}

document.querySelector("#openExitFlow").addEventListener("click", (event) => openExitFlow(event.currentTarget));
document.querySelector("#closeExitFlow").addEventListener("click", closeExitFlow);
document.querySelector("#exitFlowBackdrop").addEventListener("click", closeExitFlow);
document.querySelector("#cancelExitFlow").addEventListener("click", closeExitFlow);
document.querySelector("#continueExitFlow").addEventListener("click", () => setExitStep(1));
document.querySelector("#backToExitMember").addEventListener("click", () => setExitStep(0));

exitMemberOptions.addEventListener("click", (event) => {
  const option = event.target.closest(".exit-member-option");
  if (option) selectExitMember(option);
});

document.querySelector("#confirmExitTransfer").addEventListener("click", () => {
  if (!document.querySelector("#exitConfirmRule").checked) {
    showToast("请先确认保证金的救场用途");
    return;
  }
  setExitStep(2);
  showToast("退出已确认，救场票已经生成");
});

document.querySelector("#finishExitFlow").addEventListener("click", closeExitFlow);
document.querySelector("#viewGeneratedRescueTickets").addEventListener("click", () => {
  closeExitFlow();
  window.setTimeout(() => scrollToSection("rescue"), 120);
});

function rescueTaskFromTicket(ticket) {
  return {
    title: ticket.dataset.task,
    reward: ticket.dataset.reward,
    category: ticket.dataset.category,
    deadline: ticket.dataset.deadline,
    proof: ticket.dataset.proof,
    proofSummary: ticket.dataset.proofSummary,
    copy: ticket.dataset.copy,
    checks: [ticket.dataset.checkOne, ticket.dataset.checkTwo, ticket.dataset.checkThree],
    workLink: ticket.dataset.workLink,
    workNote: ticket.dataset.workNote
  };
}

function fillRescueTask(task) {
  ["#rescueFlowTitle", "#rescueDetailTitle", "#claimedTaskName", "#paidTaskName"].forEach((selector) => {
    document.querySelector(selector).textContent = task.title;
  });
  ["#rescueReward", "#claimedReward", "#paidReward"].forEach((selector) => {
    document.querySelector(selector).textContent = task.reward;
  });
  document.querySelector("#rescueFlowCategory").textContent = task.category;
  document.querySelector("#rescueDeadline").textContent = task.deadline;
  document.querySelector("#rescueProof").textContent = task.proof;
  document.querySelector("#rescueTaskCopy").textContent = task.copy;
  document.querySelector("#rescueCheckOne").textContent = task.checks[0];
  document.querySelector("#rescueCheckTwo").textContent = task.checks[1];
  document.querySelector("#rescueCheckThree").textContent = task.checks[2];
  document.querySelector("#submissionProofSummary").textContent = task.proofSummary;
  document.querySelector("#rescueWorkLink").value = task.workLink;
  document.querySelector("#rescueWorkNote").value = task.workNote;
}

function setRescueStep(step) {
  rescueFlowModal.querySelectorAll("[data-rescue-panel]").forEach((panel) => {
    panel.hidden = Number(panel.dataset.rescuePanel) !== step;
  });

  rescueFlowModal.querySelectorAll("[data-rescue-step]").forEach((item) => {
    const itemStep = Number(item.dataset.rescueStep);
    item.classList.toggle("is-current", itemStep === step);
    item.classList.toggle("is-done", itemStep < step);
  });

  rescueFlowDialog.scrollTop = 0;
}

function openRescueFlow(ticket, origin) {
  activeRescueTicket = ticket;
  rescueFlowOrigin = origin || ticket;
  fillRescueTask(rescueTaskFromTicket(ticket));
  setRescueStep(0);
  rescueFlowModal.classList.add("is-open");
  rescueFlowModal.setAttribute("aria-hidden", "false");
  document.body.classList.add("modal-open");
  window.setTimeout(() => document.querySelector("#closeRescueFlow").focus(), 60);
}

function closeRescueFlow() {
  rescueFlowModal.classList.remove("is-open");
  rescueFlowModal.setAttribute("aria-hidden", "true");
  if (!promiseModal.classList.contains("is-open") && !confirmFlowModal.classList.contains("is-open") && !exitFlowModal.classList.contains("is-open")) {
    document.body.classList.remove("modal-open");
  }
  if (rescueFlowOrigin) rescueFlowOrigin.focus();
}

document.querySelector("#closeRescueFlow").addEventListener("click", closeRescueFlow);
document.querySelector("#rescueFlowBackdrop").addEventListener("click", closeRescueFlow);

document.querySelector("#rescueButton").addEventListener("click", (event) => {
  const firstTicket = document.querySelector(".rescue-ticket");
  openRescueFlow(firstTicket, event.currentTarget);
});

document.querySelector("#walletButton").addEventListener("click", (event) => {
  event.currentTarget.classList.toggle("is-connected");
  showToast("当前使用测试钱包，不涉及真实资金");
});

ticketList.addEventListener("click", (event) => {
  const ticket = event.target.closest(".rescue-ticket");
  if (!ticket) return;
  document.querySelectorAll(".rescue-ticket").forEach((item) => item.classList.remove("is-selected"));
  ticket.classList.add("is-selected");
  openRescueFlow(ticket, ticket);
});

document.querySelectorAll("[data-demo-scene]").forEach((button) => {
  button.addEventListener("click", () => applyDemoScenario(button.dataset.demoScene));
});

applyDemoScenario("hackathon", false);

document.querySelector("#backToRescueHall").addEventListener("click", () => {
  closeRescueFlow();
  window.setTimeout(() => scrollToSection("rescue"), 120);
});

document.querySelector("#claimRescueTask").addEventListener("click", () => {
  if (activeRescueTicket) activeRescueTicket.querySelector(".ticket-status").textContent = "已领取";
  setRescueStep(1);
});

document.querySelector("#returnToTaskDetail").addEventListener("click", () => setRescueStep(0));

document.querySelector("#submitRescueWork").addEventListener("click", () => {
  const linkInput = document.querySelector("#rescueWorkLink");
  const noteInput = document.querySelector("#rescueWorkNote");
  if (!linkInput.value.trim()) {
    linkInput.focus();
    showToast("先放入一个可以验收的成果链接");
    return;
  }

  document.querySelector("#reviewWorkLink").textContent = linkInput.value.trim().replace(/^https?:\/\//, "");
  document.querySelector("#reviewWorkNote").textContent = noteInput.value.trim() || "已提交成果，等待负责人验收。";
  if (activeRescueTicket) activeRescueTicket.querySelector(".ticket-status").textContent = "待验收";
  setRescueStep(2);
});

document.querySelector("#requestRescueRevision").addEventListener("click", () => {
  setRescueStep(1);
  showToast("验收人已退回修改，交付说明保留不变");
});

document.querySelector("#approveRescueWork").addEventListener("click", () => {
  if (activeRescueTicket) {
    activeRescueTicket.classList.add("is-completed");
    activeRescueTicket.querySelector(".ticket-status").textContent = "已完成";
  }
  setRescueStep(3);
});

document.querySelector("#replayRescueFlow").addEventListener("click", () => {
  if (activeRescueTicket) {
    activeRescueTicket.classList.remove("is-completed");
    activeRescueTicket.querySelector(".ticket-status").textContent = "待领取";
  }
  setRescueStep(0);
});

document.querySelector("#finishRescueFlow").addEventListener("click", () => {
  closeRescueFlow();
  window.setTimeout(() => scrollToSection("rescue"), 120);
  showToast("救场流程演示完成");
});

const observedSections = ["home", "commitment", "rescue", "mechanism"]
  .map((id) => document.getElementById(id))
  .filter(Boolean);

const navObserver = new IntersectionObserver(
  (entries) => {
    const visible = entries
      .filter((entry) => entry.isIntersecting)
      .sort((a, b) => b.intersectionRatio - a.intersectionRatio)[0];

    if (!visible) return;
    document.querySelectorAll("[data-target]").forEach((button) => {
      button.classList.toggle("is-active", button.dataset.target === visible.target.id);
    });
  },
  { rootMargin: "-20% 0px -55%", threshold: [0.08, 0.25, 0.5] }
);

observedSections.forEach((section) => navObserver.observe(section));

if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) {
  introBrand.classList.add("is-logo-visible", "is-copy-visible");
  window.clearTimeout(autoEnter);
  window.setTimeout(closeIntro, 900);
}

    </script>
  </body>
</html>
